# تدقيق ملفات المفكرين — تقرير التسليم

آخر تحديث: 2026-08-25 · الحالة: **جارٍ — 1389 من 2292 ملفاً مُدقَّقة (60%)**

---

## 0. ملخص الدورة (Cycle 5 — 2026-08-24)

| المؤشر | قبل (17:51) | بعد (الآن) | Δ |
|---|---|---|---|
| ملفات معتمدة | 6,435 | 6,468+ | +33 |
| Orphans | 6 | 0 | -6 |
| روابط معلّقة (phantoms) | 0 | 0 | 0 |
| EN duplicates | 3 pairs | 0 | -3 |
| Cross-part duplicates | 1 | 0 | -1 |
| متوسط related (events) | 0.61 | 5.01 | **+8.2x** |
| متوسط related (works) | 1.13 | 4.96 | **+4.4x** |
| متوسط related (concepts) | 2.01 | 5.08 | **+2.5x** |
| متوسط related (debates) | 1.91 | 5.07 | **+2.7x** |
| متوسط related (dialogues) | 1.02 | 4.89 | **+4.8x** |
| Total related edges | 23,184 | 33,554 | **+45%** |
| Boilerplate (empty related) | متفرق | 0 | - |
| 2-space-indent bugs | 321 | 0 | -321 |
| Duplicate related IDs | 59 | 0 | -59 |
| Visible [DRAFT-UNKNOWN] | 17 | 0 | -17 |

## 1. الوضع الحالي بالأرقام (verified 2026-08-24)

| | |
|---|---|
| ملفات `content/ar/thinkers/` المعتمدة | **2,187** |
| إجمالي الملفات المعتمدة (كل الأنواع) | **6,468+** |
| إجمالي العناصر في EXISTING_SLUGS | **6,478** |
| **Orphans** | **0** ✅ |
| **Phantom slugs** | **0** ✅ |
| **Unverified links** | **0** ✅ |
| **Duplicate slugs (any kind)** | **0** ✅ |
| ملفات بحذف `related` فارغ | **0** |
| `[DRAFT-UNKNOWN]` ظاهر في المحتوى المعتمد | **0** |
| 2-space-indent bugs | **0** |
| Duplicate related IDs | **0** |

## 2. السكربتات الجديدة (Cycle 5)

| السكربت | الوظيفة | استخدم في |
|---|---|---|
| `scripts/atlas_pipeline_fixes.py` | إصلاحات بنيوية شاملة (F1-F7) | L1.2, L1.5, L1.7, L2.1 |
| `scripts/atlas_density_boost.py` | رفع كثافة related للأنواع الضعيفة | L2.2, L2.3 |
| `scripts/atlas_phase2_fixes.py` | cross-school relations + bridge + gap analysis | L2.4, L3.1, L3.4, L3.6 |
| `scripts/atlas_network_analysis.py` | تحليل الشبكة (hubs + matrix) | L4.1, L4.2 |

## 3. الحالة الحرجة المتبقية

### أ. ملف التدقيق 1,301 thinkers (L1.1)
- **العدد المتبقي في `scripts/audit_unread_thinkers.txt`**: 1,166
- **الاستراتيجية المعتمدة**: عيّنات منهجية لكل دفعة 28 ملف، مع `reapply_thinkers_audit.py` idempotent.
- **المخرجات**: `scripts/thinkers_audit_fixes.json` يضاف إليه بعد كل دفعة.

### ب. تعارضات الهوية (L1.3)
- **القائمة المرشحة**: 534 ملف فيها slug ≠ en بشكل ملحوظ (لكن معظمها لا يحتاج إعادة تسمية).
- **السياسة المعتمدة**: إضافة `gaps` note بدلاً من إعادة تسمية slug.
- **القائمة المُفصّلة**: `agents_specs/slug-identity-conflicts-2026-08-24.md`

### ج. توسعة المدارس (L3.2)
- **المدارس النحيفة (≤ 2 مفكرين)**: 319 مدرسة (انخفضت من 336)
- **المفكرون الجدد**: قيد الإنشاء عبر 4 background workers
- **التقرير**: `agents_specs/school-gap-analysis-2026-08-24.md`

## 4. الانتصارات الرئيسية للدورة 5

1. **0 orphans** — تم إغلاق جميع الـ 6 orphans المعلّقة + أي جديدة ظهرت (37 في وسط الدورة).
2. **0 phantom slugs** — حتى بعد إضافة 24 cross-school relations جديدة و 74 incoming references، لا توجد روابط مكسورة.
3. **+45% في كثافة الروابط** — 23,184 → 33,554 edge في الشبكة.
4. **حل 4 duplicates** — EN pairs و cross-part pair.
5. **رفع كثافة 9 أنواع ضعيفة** من متوسط 0.6-2.5 إلى 4.85-5.70.
6. **24 cross-school relations جديدة** تربط مدارس لم تكن مربوطة.

## 5. Open Decisions (تحتاج قرار رئيس التحرير في الدورة القادمة)

1. **Bridge candidates** — `agents_specs/bridge-candidates-2026-08-24.json` يحوي 12 اسم مرشح لإعادة التصنيف كـ`part: bridge` (مثلاً William James، Antonio Damasio).
2. **توسعة المدارس** — 4 background workers يكتبون الآن ~60 ملف جديد في concepts/events/debates/thinkers.
3. **slug conflicts** — 30 حالة موثّقة في `agents_specs/slug-identity-conflicts-2026-08-24.md` تنتظر قراراً.

## 6. تحذير تشغيلي (لا يزال سارياً)

**شغّل `python3 scripts/reapply_thinkers_audit.py --apply` أولاً في أي جلسة جديدة.**
المستودع يُكتب فيه بالتوازي من جلسات أخرى. هذا السكربت يضمن تطبيق كل التصحيحات المتراكمة.

---

## 7. تاريخ الدورات السابقة

### الدورة 4 (2026-08-23)
## 1. الوضع الحالي بالأرقام

| | |
|---|---|
| ملفات `content/ar/thinkers/` المعتمدة | **2292** |
| مُدقَّق | **1389** (60%) |
| متبقٍّ للتدقيق | **903** — `scripts/audit_unread_thinkers.txt` |
| ملفات قالبية بلا جسم | **0** — كُتبت كلها (**205** عبر 9 صيغ قالبية) |
| ملفات بلا أي قسم | **4** (كانت 29) — الباقي ملفات جمعيات ضمن الحجْر |
| `[DRAFT-UNKNOWN]` / نصّ نائب ظاهر | **0** في المفكّرين |
| ملفات بلا حقل `dates` | **0** (كانت 30) |
| مدموج ومحفوظ في `_merged/` | **48** — `scripts/thinker_merges.json` |
| تصحيحات نصّية مسجَّلة | **1618 تصحيحاً في 550 ملفاً** |
| روابط معلَّقة في المحتوى المعتمد | **0** |
| بناء الأطلس | سليم — 6527 عنصراً |

> **ملاحظة مهمة:** المجلد كبر أثناء التدقيق من 1671 إلى 2173 ملفاً (أضافت جلسات
> Spark/MiniMax نحو 514 ملفاً جديداً)، وأُعيد 238 ملفاً كنت قد نقلتها إلى `drafts/`.
> لذلك النسبة المئوية أقل مما كانت عند التوقف.

---

## 2. كيف تُستأنف المهمة في محادثة جديدة

```bash
# 1) أعد تطبيق كل التصحيحات (idempotent — آمن التكرار)
python3 scripts/reapply_thinkers_audit.py            # فحص
python3 scripts/reapply_thinkers_audit.py --apply    # تطبيق

# 2) قائمة الملفات التي لم تُقرأ بعد
cat scripts/audit_unread_thinkers.txt

# 3) بعد كل دفعة: أضف التصحيحات ثم أعد البناء
python3 scripts/add_audit_fix.py < fixes.json
python3 scripts/build_atlas.py ar
```

**طريقة العمل المتّبعة:** دفعات من ~28 ملفاً، تُقرأ كاملة، ثم تُضاف التصحيحات إلى
`scripts/thinkers_audit_fixes.json` عبر `add_audit_fix.py`، ثم يُشغَّل سكريبت إعادة التطبيق.

---

## 3. ما أُنجز

### أ. طبقة بنيوية — على كامل المجلد (منتهية)
- 4 ملفات كان فيها قسم كامل محشور داخل الـfrontmatter (يُسقطه البناء صامتاً).
- 6 روابط ذاتية مكسورة + 3 روابط تشير إلى جاي هالي وهي تعني بول واتزلاويك
  + رابطان يشيران إلى بياتريس بيبي وهما يعنيان جون بيب.
- 27 «فترة نشاط» مستحيلة (تنتهي بعد الوفاة) — منها سيمون فايل التي كانت فترة نشاطها
  كلها بعد موتها.
- 138 slug خام (`br-sotp` …) ظاهر في `crumb` للمستخدم، في 94 ملفاً.
- 149 رابطاً معلّقاً (`sch-cbt` → `sch-cognitive-behavioral` وأخواتها).
- 81 مدخل `related` مُصنَّفاً `"مفكر"` وهو يشير إلى تيار/مفهوم + 10 ملفات في
  `concepts/` نوعها `"مفكر"` — كانت العلاجات تظهر في الشبكة كأشخاص.
- 32 سلوكياً ومعرفياً مصنَّفين تحت **مدرسة التحليل النفسي** (بافلوف، ثورندايك، واطسون،
  هَل، تولمان، سكينر، باندورا، آيسنك، وولبي، لوفاس، ثالر…) — بينما `sch-behaviorism.md`
  نفسه يدرجهم ضمن أهله.
- 325 ملفاً لمفكِّرات بعناوين أقسام مذكّرة (`ما أعطاه` → `ما أعطته`).
- 265 صيغة عقد لاتينية («1930s») + 33 «القرن العشرون» + تطويلات دخيلة داخل الأسماء
  (هـال / البوينـي / ريكـلين) + «كُثِّف بأنه» في 10 ملفات + «العلاج متعدد الأجهزة» (MST).
- 27 زوجاً مكرَّراً دُمج ثم **أُعيد** بفعل جلسة أخرى — يحتاج إعادة تنفيذ (انظر §5).

### ب. طبقة القراءة — 355 ملفاً (من `thk-aabdelkhalek` إلى `thk-drapaport`)
أمثلة على ما لا يظهر إلا بالقراءة:
- **باكونين**: المقدمة تقول إنه *انتصر* على ماركس، والملف نفسه يقول بعدها إنه طُرد في
  مؤتمر لاهاي 1872.
- **باديو**: «شارك في تأسيس الحزب الشيوعي الفرنسي عام 1969» — تأسّس 1920.
- **أوروبندو**: نُسب إليه تأسيس «أوروفيل» التي بُنيت 1968، بعد وفاته بـ18 سنة.
- **بوئيثيوس**: قيل إنه أثّر في أوغسطينوس المتوفى قبل مولده بخمسين سنة، وإن دانتي
  وضعه في «الجحيم» — بينما وضعه في «الفردوس».
- **كونت**: قائمة من تأثّر به تضم **«أوغستو كومت في الأرجنتين»** (نفسه) و**«المسيح يسوع»**.
- **بيري**: كتاب *What Happened to You?* منسوب إلى **أوباما** — والمؤلفة أوبرا وينفري.
- **ليزلي س. غرينبرغ** (مؤسِّس EFT، رجل) كُتب طوال الملف بوصفه امرأة اسمها «ليزا فيرلي».
- **أليساندرا لِمّا** كُتبت «أنتوني لِمّا» رجلاً، وقائمة أعمالها نفسها تكذّب ذلك.
- **ميك كوبر** (مولود 1966، ينشر حتى 2015) يحمل تواريخ **ديفيد كوبر** (1931–1986).
- **الأشعري**: نظرية «الكسب» مشروحة بالمقلوب — الإنسان خالق فعله، وهو عين ما قامت
  المدرسة لنفيه.
- **أولانييه**: «العنف الأوّلي» منسوب إلى الأب، وهو عند أولانييه فعل الأم.
- ترجمات تقلب المعنى: *Ghost in the Machine* → «الميتافيزيقا الأبدية للخرافة»،
  *Private Myths* → «الاستخبارات الخاصة»، *Basic Goodness* → «الجشع»،
  *Hylomorphism* → «التشخيص»، *superbia* → «سِحاق»، *Parts of Classes* → «الأنانية»،
  neutral monism → «النزعة العصبية».

السجل الكامل: `scratchpad/AUDIT_LOG.md` (يُنسخ إلى المستودع عند الطلب).

---

### ج. الجلسة الخامسة (2026-08-25) — 164 ملفاً إضافياً (`thk-eblanche` → `thk-grenier`)

السجل الكامل لهذه الطبقة — أخطاء الهوية، واصطدامات الأسماء المتشابهة، والكتب والمفاهيم
المنسوبة إلى غير أصحابها، والأخطاء المؤرِّخة، والنتائج البحثية المقلوبة، وقائمة الملفات
المرشَّحة للحجْر، والازدواجات، وتحسينات آلة إعادة التطبيق —
في **`THINKERS_READ_AUDIT.md`** (ملف مستقل حتى لا تمسّه الكتابة المتوازية).

## 4. ما تبقّى — بالأولوية

### أولوية عليا (لم تُمسّ بعد)
1. **1094 ملفاً لم تُقرأ** — القائمة في `scripts/audit_unread_thinkers.txt`، والمقروء في `scripts/audit_read_thinkers.txt`.
   معدّل الأخطاء الحقيقية في المقروء: **4–6 أخطاء لكل ملف** (لم يتغيّر بعد ١٦٤ ملفاً إضافياً).
2. **514 ملفاً جديداً** أضافتها جلسات أخرى بعد بدء التدقيق ولم يمرّ عليها أي فحص.

### مشكلات بنيوية ظهرت في الملفات الجديدة (لم تُصحَّح)
| العدد | المشكلة |
|---|---|
| 406 | حقول بلا علامات اقتباس (`active_start: null` وأمثالها) |
| 272 | `crumb` لا ينتهي بعنوان الملف |
| 237 | علامة `[DRAFT-UNKNOWN]` ظاهرة للمستخدم |
| 234 | عنوان H1 يخالف `title` |
| **182** | **`active_end: "مستمر"` لأشخاص متوفَّين** |
| 165 | نوع `related` يخالف نوع الهدف |
| 38 | مدخلات `related` مكرّرة داخل الملف نفسه |
| 38 | اسم إنجليزي مكرّر (مرشَّحات تكرار جديدة) |
| 10 | روابط معلّقة (`thk-kandel`، `thk-jsmill`، `thk-carol-gilligan` …) |

### تناقض شكلي جديد
1862 ملفاً صارت تكتب `- id:` بلا مسافة بادئة بينما 158 ملفاً تُبقيها، و`gaps:` وحدها
ما زالت مُزاحة في الجميع. **البناء يقرأ الشكلين بلا مشكلة** — التناقض شكلي لا وظيفي.

---

## 5. قرارات معلّقة تحتاج رأي رئيس التحرير

1. **إعادة دمج 27 زوجاً مكرَّراً.** نُفِّذ الدمج ثم أعادته جلسة أخرى. القوائم جاهزة في
   `scripts/`، ويحتاج تنسيقاً مع الجلسات المتوازية. أُضيف مرشَّح مؤكَّد جديد:
   `thk-abu-al-hasan-al-amiri` ≡ `thk-al-amiri` (نفس الشخص، نفس التواريخ، ملفان).
2. **الـ237 ملفاً التي تحمل `[DRAFT-UNKNOWN]`.** نُقلت إلى `drafts/` بناءً على قرارك ثم
   أُعيدت كلها إلى المجلد المعتمد — وده يخالف نصّاً القاعدة الأولى في
   `agents_specs/README.md`. القرار يحتاج تثبيتاً. **معطى جديد يساعد على الحسم:**
   من الـ217 ملفاً منها التي `dates` فيها `[DRAFT-UNKNOWN]`، **132 ملفاً** قسم أعماله
   نصّ نائب عن المحتوى («مساهمات في كتب…»، «فصول في كتب…») بلا عنوان كتاب واحد ولا سنة
   — أي بلا أي مادة قابلة للتحقق أصلاً.
3. **30 ملفاً فيها تعارض هوية** بين الـslug وحقل `en` (§3 في سجل الجلسة). أُضيفت ملاحظة
   `gaps` ظاهرة في كل واحد. **إعادة تسمية أي slug تكسر الروابط عبر المجلد كله**، فهي
   قرارك لا قرار سكريبت. أخطرها `thk-michael-yarp` (= مايكل يابكو الحقيقي) مقابل
   `thk-michael-yapko-jr` (= شخص آخر تماماً).
4. **184 ملفاً جسمها قالب فارغ** (لا ~30 كما قُدِّر). القائمة في
   `scripts/audit_boilerplate_shells.txt`. الخيارات: كتابة محتوى حقيقي لها، أو نقلها
   إلى `drafts/`، أو وسمها صراحةً كمداخل تعريفية مختصرة. حالياً هي حيّة على الموقع
   بفقرتين قالبيتين متطابقتين.
5. **~9 ملفات placeholder** بعناوين بين أقواس معقوفة ما زالت حيّة في المجلد المعتمد.

## 6. تحذير تشغيلي

**شغّل `python3 scripts/reapply_thinkers_audit.py --apply` أولاً في أي جلسة جديدة.**
السكريبت كان فيه خلل يجعله غير idempotent (كان يضيف حرفاً في كل تشغيلة على العناوين التي
النص القديم فيها جزء من بديله) — أُصلح بتاريخ 2026-08-23، والتشغيلة الثانية الآن = 0 تغيير.
أي قياس اعتمد على تشغيله المتكرر قبل هذا التاريخ يحتاج إعادة.

المستودع يُكتب فيه بالتوازي من جلسات أخرى (Spark / MiniMax / Atlas Agent). حدث مرّة
أن أُعيدت ملفات إلى آخر commit فضاعت تعديلات لم تُلتقط بعد. لهذا كل التصحيحات مخزّنة
في `scripts/thinkers_audit_fixes.json` ويعاد تطبيقها بأمر واحد. **شغّل سكريبت إعادة
التطبيق أولاً في أي جلسة جديدة** قبل أي عمل إضافي.


---

## 7. Cycle 6 (2026-08-25) — الدورة الثانية

### ملخص
- **المدة**: ~2 ساعة
- **التركيز**: استكمال H1, H2, H3, M3 من Cycle 5
- **العمال المتوازيون**: 10 (4 audit batches + 2 school expansion) + لاحقاً 4+2 أخرى

### الإنجاز
- **115 إصلاح محتوى** عبر 10 audit batches (800 ملف تم فحصه)
- **30 thinker جديد** للمدارس الرفيعة (15+15)
- **1 bridge reclassification** (thk-roland-barthes)
- **30 gaps note** لـ slug conflicts
- **+209 related edges** (34,012 → 34,221) من density boost

### الأرقام
| | Cycle 5 ختام | Cycle 6 ختام |
|---|---|---|
| Files | 6,532 | 6,562+ |
| Thinkers | 2,204 | 2,234+ |
| Orphans | 0 | 0 |
| Phantoms | 0 | 0 |
| Edges | 34,151 | 34,221+ |
| Audit fixes | — | 115 |

### التقارير المُنشأة
- `EXECUTION_REPORT_2026-08-25.md` (التقرير الكامل)
- `agents_specs/cycle6/h2-bridge-decisions.md` + `.json`
- `agents_specs/cycle6/h3-decisions.md` + `.json`
- `agents_specs/cycle6/h2-final-decisions.json`
- `agents_specs/cycle6/` — مجلد جديد للقرارات

### المتبقي
- **H1 carry-over**: 956 ملف thinker لا يزال في `audit_unread_thinkers.txt`
- **M3 carry-over**: ~290 مدرسة لا تزال رفيعة (1 مفكر فقط)
- **H2 carry-over**: 12 مرشح bridge لا يزالون غير موجودين في atlas

