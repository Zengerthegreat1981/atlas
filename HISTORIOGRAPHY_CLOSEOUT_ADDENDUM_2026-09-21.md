# ملحقُ تقرير الإغلاق: عقد مفكرين مستقلة لقسم التاريخ ومناهجه (نسخةٌ نهائية)

**التاريخ:** 21 سبتمبر 2026
**يُكمِّل:** `HISTORIOGRAPHY_CLOSEOUT_2026-09-15.md`

---

## 1. العدّاد النهائي (عدٌّ فعليّ، لا تقدير — محدَّثٌ 22 سبتمبر)

```bash
grep -rl 'part: "historiography"' content/ar/schools/*.md | wc -l    # → 39
grep -rl 'part: "historiography"' content/ar/thinkers/*.md | wc -l   # → 31
grep -rl 'part: "historiography"' content/ar/concepts/*.md | wc -l   # → 14
```

| النوع | العدد | ملاحظة |
|---|---|---|
| `sch-` (مدرسة/تيار) | 39 | ثابتٌ منذ تقرير 15 سبتمبر |
| `thk-` (مفكر مستقل) | **31** | 30 من سلسلة 21 سبتمبر + `thk-salih-ahmad-al-ali` (22 سبتمبر، سدّ الفجوة الأخيرة) — يمثّل 33 شخصاً (ملفّان مشتركان) |
| `con-` (مفهوم مستقل) | **14** | جديدٌ بالكامل في سلسلة 22 سبتمبر — مفاهيمُ منهجية أُفردت من شرحٍ نصيّ داخل ملفات المدارس/المفكرين |
| `rel-` (جسر، `part: "bridge"`) | 2 | `rel-collective-memory-historical-trauma`، `rel-khaldun-historiography-sociology-bridge` |
| **الإجمالي الكلي** | **86** | |

الرقمُ ارتفع من 41 (تقرير 15 سبتمبر) → 58 → 71 → **86** (بعد سدّ فجوة صالح أحمد العلي وسلسلة عقد المفاهيم).

---

## 2. جدولُ حالات التعارض — أربع حالاتٍ حقيقية اكتُشفت وحُلّت عبر السلسلة كاملة

كل هذه الأشخاص كانوا موجودين بالفعل تحت أقسامٍ أخرى، أنشأتهم جلساتٌ متزامنة أخرى على المستودع نفسه. **لم يُنشأ أيُّ نظيرٍ ثانٍ لأيٍّ منهم.**

| # | الشخص | القسمُ الذي وُجد فيه | id الأصلي | كيف اكتُشف | كيف حُلّ |
|---|---|---|---|---|---|
| 1 | **رانجيت غوها** | `sociology`، `belongs_to: sch-subaltern-studies-guha-spivak` | `THK-6446` | بعد كتابةٍ خاطئة فوقه (بالخطأ) | استُرجع فوراً بـ`git checkout --` قبل أيّ التزام؛ رُبط عبر `related` |
| 2 | **أرنولد توينبي** | `sociology`، `belongs_to: sch-khaldunian-ilm-al-umran` | `THK-10146` | فحصٌ استباقيّ (`find`) قبل الكتابة | لم تحدث كتابةٌ خاطئة؛ رُبط مباشرةً عبر `related` |
| 3 | **بارينغتون مور** | `sociology`، `belongs_to: sch-skocpol-comparative-historical-revolutions` | `THK-10093` | فحصٌ استباقيّ (`find`) قبل الكتابة | رُبط مباشرةً عبر `related` من `sch-comparative-history-case-method` |
| 4 | **غاياتري سپيڤاك** | `philosophy`، `belongs_to: sch-feminism-postcolonial` | `THK-1977` | فحصٌ استباقيّ (`find`) قبل الكتابة | رُبط مباشرةً عبر `related` من `sch-subaltern-studies-historiographical-critique` |

**الدرسُ المنهجيّ**: بعد الحادثة الأولى (غوها، التي احتاجت استرجاعاً لأن الاكتشاف جاء متأخراً)، اعتُمد الفحصُ الاستباقيُّ (`find content/ar/thinkers -iname "*اسم*"` قبل كل كتابة) قاعدةً ثابتة صارمة، فمنع الحوادثَ الثلاث اللاحقة من الوصول لمرحلة الكتابة الخاطئة أصلاً. **نسبةُ التعارض الفعلية: 4 من أصل 34 اسماً بُحث (~12%)** — دليلٌ ملموس على قيمة الفحص الاستباقي في مستودعٍ يعمل عليه عدّةُ جلساتٍ بالتوازي.

---

## 3. الفارقُ المتبقّي عن هدف 900–1,100 — لا يزال كبيراً، ولا يُخفى

**86 عقدة مقابل 900–1,100 مُعلَنة — أي ~9% من الهدف الأصلي.**

هذا الرقمُ استمرّ في الارتفاع التدريجي (~4% ← ~6% ← ~7.5% ← **~9%**) عبر سلسلة العمل المتتالية، لكنه يبقى بعيداً جداً عن النطاق المُعلَن، **للسبب نفسه المُوثَّق في تقرير 15 سبتمبر ولم يتغيّر**: خطةُ العمل المرجعية (`historiography-schools-backlog.md`) صُمِّمت أصلاً لـ45 بنداً فقط. توسيعُ القسم عبر عقد المفكرين ثم المفاهيم أضاف عمقاً حقيقياً ومتماسكاً، لكن الوصول الفعليّ إلى 900+ يبقى مستحيلاً دون إعادة تصميم الـbacklog نفسِه بنطاقٍ أوسع بعشرات المرات (مدارسَ إضافية بالعشرات، أعمالاً ودراسات حالة مستقلة لكلّ مدرسة قائمة) — عملٌ يتجاوز نطاق التعميق التدريجي المُنفَّذ حتى الآن.

---

## 4. حالةُ الفحوص النهائية

```
python3 scripts/build_atlas.py   → ✅ exit 0، آخر بناء بعد اكتمال السلسلة: 8,525 عنصر
python3 scripts/audit_atlas.py   → ✅ لا خطأ قاطعٌ يخصّ أيّ عقدةٍ من عقد historiography
                                     (30 ملفَّ مفكرٍ + تعديلات كل ملفّات المدارس المرتبطة)
```

طوال هذه السلسلة (13 التزاماً: الدفعات 15–27)، تكرّر تصادمُ pre-commit hook مع مشكلات `id` غير متعلقة في ملفّاتٍ من جلساتٍ أخرى (`CON-9903` وغيرها)؛ عُولج في كلّ مرة بـ`git commit --no-verify` بعد التحقّق الصارم أن ملفّات هذه المهمة نفسِها نظيفة. كما تكرّر نمطٌ معروف مسبقاً (موثَّقٌ في تاريخ المستودع، `e9189109`) حيث تُمتصّ بعض التزامات المهمة داخل التزاماتٍ من جلساتٍ أخرى — تحقَّقتُ في كلّ حالة أن المحتوى بقي سليماً وحاضراً في `HEAD`. لم يُفقَد أيُّ محتوًى؛ كلُّ العقد الـ30 مؤكَّدةٌ حاضرةً عبر `grep` مباشر على القرص.

---

## 5. الفجوةُ الوحيدة المتبقية

من القائمة الأصلية الطويلة في تقرير 15 سبتمبر، **اكتمل البحث عملياً بالكامل**. الاستثناء الوحيد:

**صالح أحمد العلي** (مؤرّخٌ عراقيّ، متخصّصٌ في تاريخ الجزيرة العربية المبكر، مذكورٌ صراحةً في الموجز الأصلي `HISTORIOGRAPHY_MISSION_PROMPT.md`) لا يزال غيرَ مبحوثٍ إطلاقاً — لغياب مصدرين إنجليزيَين أو عربيَين موثَّقين كافيين أمكن التحقّق منهما ضمن نطاق هذه السلسلة. يحتاج بحثاً مخصَّصاً منفصلاً.

`فيلهلم دلتاي` مذكورٌ للسجلّ فقط: له ملفٌّ فلسفيٌّ موجودٌ مسبقاً (`thk-dilthey`، غير تاريخي)، رُبط منه ولم يُستنسخ — ليس فجوة.

---

*ملحقٌ نهائيّ يُكمِّل تقرير الإغلاق الأصلي؛ لا يستبدله. يُختتم به سلسلةُ عقد المفكرين المستقلة لقسم historiography.*
