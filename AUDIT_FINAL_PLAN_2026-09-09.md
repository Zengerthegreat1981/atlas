# المراجعة الختامية للأطلس — النتائج وخطة الإصلاح

**التاريخ:** 2026-09-09 · **النطاق:** قسما علم النفس والفلسفة (6,670 عقدة حيّة في `content/ar/`) · **`audit_atlas.py` يخرج بصفر** — لكنّ الفحوص الجديدة أدناه كشفت ما لا يغطّيه.

كلُّ قائمةٍ مُشار إليها محفوظةٌ في `scripts/audit_2026-09-09/` (ملفات `.txt` مفصولة بـ tab) مع السكربتات التي أنتجتها (`deep_audit.py`, `deep2.py` … `deep5.py`) لإعادة التشغيل بعد كلِّ دفعة إصلاح.

---

## أ. ما هو سليم (تحقّقتُ منه ولا يحتاج عملاً)

| الفحص | النتيجة |
|---|---|
| كلُّ ملفٍّ له frontmatter، وكلُّ `slug` يطابق اسمَ الملفّ، وكلُّ ملفٍّ في `data.json` وبالعكس | 6,670 / 6,670 |
| إحالات `related` كلُّها تُحلّ إلى ملفٍّ قائم، لا إشارة ذاتية، لا تكرار | صفر خلل |
| `id` فريدٌ في كلِّ الملفات | صفر تكرار |
| شجرة `belongs_to` بلا حلقات، لا أبٌ مهجور، لا أبٌ نصّي غير محلول | سليم |
| `data.json` و`index.html` مبنيّان بعد آخر تعديل في المحتوى | لا ملفٌّ أحدثُ منهما |
| الواجهة تُحمَّل بلا أخطاء console، و`renderNode` لا يرمي استثناءً على عيّنة العقد | سليم |
| روابط الويكيبيديا التي تنتهي بقوسٍ (19 حالة) | **إنذارٌ كاذب** — الملفات صحيحة |

---

## ب. الأخطاء المكتشفة — مرتّبةٌ بالخطورة

### المستوى 0 — أخطاءٌ تُضلّل القارئ (معنى خاطئ، لا شكل)

**0.1 — أنسابٌ مختومةٌ قالبياً: 177 متلازمة + 5 اضطرابات + حدثٌ واحد تنتمي إلى «العلاج المعرفي السلوكي»**
`syn-amok` و`syn-tarantism` و`syn-st-vitus-dance` (1374م!) وغيرها كلُّها `belongs_to: sch-cognitive-behavioral`. لا يذكر متنُ CBT واحدةً منها، ومتونُ 171 منها لا تذكر CBT أصلاً. ختمٌ قالبيٌّ من دفعة إنشاء المتلازمات.
- كذلك 10 سياقاتٍ تحت «علم النفس الإنساني» منها `ctx-reformation-printing-press-europe` (1517) و`ctx-byzantine-constantinople-preservation` (330م)، و18 سياقاً تحت «علم النفس الاجتماعي».
- القائمة: `anachronistic_belongs_to.txt` (99 سطراً) + استخراج `syn-`/`dis-` من `data.json`.

**0.2 — حروفُ نسبٍ تاريخيةٍ خاطئة بين المدارس (evolved_into / superseded_by / absorbed_by / split_into)**
الفحصُ اليدويُّ لـ 657 حرفَ نسبٍ (`school_edges.txt`) كشف نمطاً: حروفُ `superseded_by` و`absorbed_by` (30 حرفاً) تبدو مولَّدةً بخوارزمية «أقرب مدرسةٍ لاحقة» لا بقراءة. أوضحُ الأخطاء:

| الحرف | لماذا خاطئ |
|---|---|
| `sch-stoicism` evolved_into `sch-aristotelianism` | المشائية (335 ق.م) أقدمُ من الرواقية (300 ق.م) |
| `sch-stoicism` / `sch-epicureanism` / `sch-neoplatonism` absorbed_by/superseded_by `sch-existentialism-religious` | فجوةُ 1,700 سنة، لا سند في المتن |
| `sch-ashariyya` superseded_by `sch-mutazila` | معكوس (الحرف الصحيح موجودٌ في ملفّ المعتزلة) |
| `sch-comtian-positivism` evolved_into `sch-utilitarianism` | النفعية (1789) أقدمُ من الكومتية (1830) |
| `sch-hegel-right` / `sch-british-idealism` / `sch-american-idealism` superseded_by `sch-pragmatism-classical` | لا سند؛ المثاليةُ البريطانية خلفتها التحليلية (مور/رَسل) |
| `sch-analytic-metaphysics` ⇄ `sch-scientific-realism` superseded_by (في الاتجاهين) | حلقةُ تجاوزٍ متبادلة |
| `sch-experimental-philosophy` (2001) superseded_by `sch-quinean-naturalism` (1951) | معكوس |
| `sch-academic-skepticism` evolved_into `sch-academy-platonic` | معكوس (الأكاديمية هي الأصل) |
| `sch-german-idealism` split_into `sch-kant-critical` (1781 < 1785) | كانط سابقٌ على المثالية لا فرعٌ منها |
| `sch-feminism-marxist` (1884) evolved_from `sch-feminism-radical` (1967) | معكوس |
| `sch-civic-republicanism` (1400) / `sch-deism` (1620) / `sch-social-contract` (1640) evolved_from `sch-enlightenment` (1680) | التنويرُ لاحقٌ عليها |
| `sch-new-materialism` (1994) evolved_from `sch-speculative-realism` (2007) | معكوس |
| `sch-bon` evolved_from `sch-shramana` | البون تبتيةٌ لا شرامنية |
| `sch-donghak` evolved_into `sch-donghak` | إشارةٌ ذاتية |
| `sch-virtue-ethics` superseded_by `sch-utilitarianism-contemporary`؛ `sch-kantian-ethics-contemporary` و`sch-utilitarianism-contemporary` superseded_by `sch-contractualism` | تياراتٌ متعاصرةٌ حيّة، لا يتجاوز أحدُها الآخر |
| `sch-british-empiricism` / `sch-continental-rationalism` / `sch-leibnizianism` evolved_from `br-scholasticism-second-salamanca` | نسبٌ مشكوكٌ فيه بلا سند |
| **علم النفس:** `sch-existential-therapy` evolved_from `br-ego-psychology`؛ `sch-biological-neuro` evolved_from `sch-developmental`؛ `sch-social-psychology` (1908) evolved_from `sch-behaviorism` (1913)؛ `sch-emdr` evolved_from `sch-behaviorism`؛ `sch-ipt` / `sch-liberation-psychology` / `sch-somatic-experiencing` / `sch-sensorimotor-psychotherapy` evolved_from `sch-psychoanalysis` | بلا سند أو معكوسٌ زمنياً |
| **7 حروف نسبٍ هدفُها ليس مدرسةً:** `br-logotherapy` split_into **`thk-langle`** (مفكّر!)، `sch-dbt` evolved_from `con-biosocial-dbt`، `sch-behaviorism` evolved_into `con-applied-behavior-analysis`، `sch-transpersonal` split_into `tec-holotropic-breathwork`، `sch-systemic-family` split_into تقنيتان، `br-nlp-systemic` split_into تقنية | نوعُ الهدف خاطئ |
| `sch-mbct` evolved_from **`sch-cbt`** (ملفُّ إحالةٍ مهجور) | يجب أن يشير إلى `sch-cognitive-behavioral` |

القوائم: `evolution_anachronism.txt` (15 آلياً) + `school_edges.txt` (للمراجعة اليدوية الكاملة).

**0.3 — روابطُ أعضاءٍ خاطئة: الشخصُ الخطأ أو العصرُ الخطأ**
- **`sch-stoicism` يربط `thk-zeno-elea`** (زينون الإيلي، صاحب المفارقات) بدل زينون الرواقي مؤسِّس المدرسة — **ولا ملفَّ لزينون الرواقي أصلاً** (ملفٌّ ناقصٌ يجب إنشاؤه: `thk-zeno-citium`).
- **`rel-mbct-cbt` يربط `thk-segal`** = حنا سيغال (كلاينية) بعنوانٍ مكتوب «زندل سيغال»؛ الصحيح `thk-zsegal` الموجود فعلاً. — هذا يعني أنّ قائمةَ **371 عنواناً في `related` يخالف عنوانَ الهدف مخالفةً جوهرية** (`related_stale_substantive.txt`) ليست تجميلية: بعضُها يخفي **هدفاً خاطئاً**. يجب فحصُها سطراً سطراً.
- 33 مدرسةً تربط مفكّراً خارج زمنها (`school_thinker_date_mismatch.txt`): الأوغسطينية ↔ بونافنتورا (1248)، التشاؤمية ↔ تشيكسنتميهاي، السكولاستية ↔ بيرلمان (1942)، الليشيويه ↔ وانغ فوزهي، الماركسية الكلاسيكية ↔ ماركوزه/أدورنو (موضعُهم فرانكفورت)…
- 20 مفكّراً `belongs_to` مدرسةٍ بدأت بعد وفاته بأكثر من 30 سنة. **نصفُها خطأٌ في تاريخ المدرسة لا في النسب**: `sch-akbari` مؤرَّخة 1580 (ابن عربي توفّي 1240 — القونوي والجيلي أعضاءٌ صحيحون)، `sch-hermeneutics` 1960 (يُقصي شلايرماخر ودلتاي)، `sch-zoroastrian-philosophy` −600 (زرادشت −1000/−1500)، `sch-samkhya` −400 (كابيلا −600)، `sch-phil-science` 1920 (غاليليو ونيوتن وروجر بيكون). والنصفُ الآخر نسبٌ خاطئ: دوستويفسكي → الوجودية، سوجورنر تروث → النسوية السوداء، إريجينا → السكولاستية.

**0.4 — عقدٌ مكرَّرة: 51 مجموعةً كلا طرفيها ملفٌّ كاملٌ (لا إحالة)** — `dup_groups.txt`
- **~30 زوجَ مفكّرين:** ليفين، بيرن، تريفي (**بتاريخَي وفاةٍ متناقضَين: 2008 / 2011**)، برينر، إيكمان، كارنيجي، أساجيولي، يابكو، تشومسكي، فستنجر، شابيرو، هندريكس، هانت، رايس، لوبورسكي، مادانيس، أولفيوس، بيرسيلي، بيكو، غريفيث، إيبرغ، مياسو، ثالر، غروف، هايدغر (`thk-heidegger-technology`)، دي بونو، كابات-زين، إديث شتاين، روزنبرغ، أيمز، **زرادشت مرّتين** (`thk-zarathushtra` + `thk-zoroaster`).
- 3 أعمال (ملغرام، ميرلوبونتي، نيف)، 12 زوجَ مفاهيم (أهيمسا، أوبونتو، ماعت، الداو، اللي، الاغتراب، القلق، الأتاراكسيا، نافذة التحمّل، الجوهر والعرض، الرؤية بعينين، إرادة الحياة)، 3 أزواج مفهوم/عمل (الذكاء العاطفي، الاستشراق، إبادة الطبقية)، 8 أزواج مفهوم/مدرسة-تيار (الديمقراطية التداولية، النظرية الكويرية، علاج الأسر الرملية…)، 5 أزواج مدرسة/تقنية (DBT، MBCT، REBT، FFT، MST)، `dis-mdd`/`dis-major-depressive`، مقياس هاملتون مرّتين، لانغله-فرانكل مرّتين.
- 9 أزواج تقنياتٍ متماثلةٍ عبر CBT/DBT/ACT (التقبّل الجذري، مسح الجسد…) — **قد تكون مقصودةً** كصياغاتٍ خاصّة بكلّ مدرسة؛ تحتاج قراراً تحريرياً لا دمجاً آلياً.

**0.5 — 59 ملفَّ مفكّرٍ يحمل slug شخصٍ آخر** — `slug_name_mismatch.txt`
`thk-jmertz` = ماريو تريفي، `thk-jboss` = ديفيد بيرسيلي، `thk-francesharville` = فرانسين شابيرو، `thk-klew` = كورت لوين، `thk-aabrer` = كارل أبراهام، `thk-lsaari` = لوسيان ليفي-برول، `thk-hkrystal` = هيروشي كاواشيما، `thk-jkadden` = كاثلين كارول… يبدو أنّ دفعةً قديمةً أعادت كتابةَ محتوى الملفّ دون تغيير اسمه. أكثرُها تتقاطع مع المكرَّرات في 0.4 (الملفُّ ذو الـslug الصحيح موجودٌ أيضاً).

**0.6 — `part` خاطئ لـ 533 عقدة** — `part_vs_parent.txt`
480 عقدةً موسومةً `philosophy` وأبوها مدرسةٌ نفسية (197 تحت التحليل النفسي، 39 الإنسانية، 24 علم النفس الاجتماعي، 17 التجربة الجسدية…)، و53 بالعكس. و218 عقدةً مسارُها يبدأ بـ«التحليل النفسي» لكنّ `part: philosophy` (175 منها مفكّرون). يؤثّر على التصفية والفهارس في الواجهة.

**0.7 — رابطٌ مكسورٌ في الواجهة: `thk-buber`**
بياناتُ القالب الثابتة (`scripts/template/data_extras.json`: people, gnodes, gantt, threads) تشير إلى `thk-buber`، والملفُّ الفعليُّ هو `thk-mbuber`. كلُّ نقرةٍ على بوبر في الخريطة/الخطّ الزمني/الخيوط تفتح عقدةً غيرَ موجودة. (الـslug القديم باقٍ فقط في `content/en/`.)

### المستوى 1 — ملفاتٌ ناقصةٌ وإحالاتٌ معلَّقة

**1.1 — 34 حرفَ `edges` هدفُه نصٌّ لا slug** — `edge_text_targets.txt`
- **يُحلّ الآن** بالمدارس التي أُنشئت أمس: «علم النفس المعرفي» → `sch-cognitive-psychology`، «علم الأعصاب المعرفي» → `sch-cognitive-neuroscience`، «طب الأعصاب السلوكي» → `sch-behavioral-neurology`، «علم نفس الصدمة» → `sch-trauma-psychology`، «علم النفس عبر الثقافي» → `sch-cross-cultural-psychology`، «العلاج الأسري الاستراتيجي» → `tec-strategic-family-therapy`.
- **ملفاتٌ ناقصةٌ فعلاً (يجب إنشاؤها أو حذفُ الحرف):** إرفينغ غوفمان، هوارد غاردنر، تشارلز سبيرمان (كـ`authored_by` لأعمالٍ قائمة)، زينون الرواقي (0.3)، «السقراطية»، «التفاعلية الرمزية»، «علم نفس الدين»، «علم الموت»، «علم النفس العصبي الإكلينيكي»، «علم الأدوية النفسية»، وفروعُ علم النفس النمائي الخمسة (`sch-developmental` split_into نصوص).
- 27 slug مذكورٌ في `gaps` و8 في المتن بلا ملفّ (غوفمان، بورديو، توكفيل، غارفينكل، دانيال سيغل، لوس إيريغاراي، جيرد غيغرنزر، `sch-adlerian`، `wrk-phenomenology-of-spirit-hegel`…). بعضُها سوسيولوجيٌّ خارجَ النطاق — يُقرَّر لكلٍّ: إنشاء / إعادة توجيه إلى موجود / حذف الذكر.

**1.2 — ملفاتُ الإحالة (redirect): 59 ملفاً، منها 12 غيرُ موسومةٍ في العنوان**
- 45 رابطَ `related` + 5 حروفَ `edges` ما زالت تشير إلى إحالاتٍ بدل أهدافها (`links_to_deprecated.txt`) — منها `wrk-nonviolent-communication` written_by `thk-marshall-rosenberg` (إحالة) والهدفُ `thk-msrosenberg`.
- 18 إحالةً بلا حقل `redirect_to` قابلٍ للقراءة آلياً (الهدفُ مذكورٌ في العنوان فقط أو غائب).
- 12 إحالةً لا تحمل وسمَ «إحالة/حجر» في العنوان فلا يلتقطها `audit_atlas.py` (`thk-peter-levine`, `trm-archetype`, `wrk-being-nothingness`, `thk-pdeegan`, `thk-mwhitehouse`…).

**1.3 — 118 مدرسة/تيار بلا أعضاءٍ وبأقلّ من 3 روابط** — `empty_schools.txt`
أغلبُها تياراتٌ فلسفيةٌ فرعية أُنشئت دفعةً واحدة (`br-yogacara-classical`, `br-neoplatonism-athenian`, `br-mutazila-basra`, `br-christian-patristics-latin`…) بصفر `related`. طُرقٌ مسدودةٌ في التصفّح. وعموماً: **3,167 من 4,562 ابناً في `belongs_to` لا يظهر في `related` أبيه** — فصفحةُ المدرسة لا تُظهر أعضاءها إلا عبر الحروف.

### المستوى 2 — بياناتٌ وصفيةٌ غيرُ متّسقة (تُصلَح بسكربت)

| الخلل | العدد | الملفّ |
|---|---|---|
| `target_type` في `edges` يخالف نوعَ الهدف الفعلي (88 «مدرسة» لتيّار، 73 «مظلّة»، 23 «مدرسة» لعلاقة…) | 256 | يُستخرَج من `data.json` |
| عنوانٌ في `related` يخالف عنوانَ الهدف (تجميلي: أقواس/تشكيل) | 1,566 | — |
| عنوانٌ في `related` يخالف جوهرياً (**يُراجَع يدوياً — انظر 0.3**) | 371 | `related_stale_substantive.txt` |
| `type` في `related` يخالف نوعَ الهدف («حدث» بدل «حدث تاريخي»…) | 555 | — |
| `level` فارغ | 17 | `level_empty.txt` |
| `level: "أساسي"` (قيمةٌ خارج المعجم) | 3 | `level_odd.txt` |
| حقل `en` يحوي عربية | 14 | `en_field_arabic.txt` |
| `part: sociology` داخل أطلسٍ ثنائيّ | 3 | `sociology_part.txt` |
| مفتاحٌ مغلوط `crumbs:` بدل `crumb:` | 1 (`thk-mmithoefer`) | `odd_keys.txt` |
| بادئاتٌ خارج المعجم: `classification-icd-11`, `classification-dsm-5-tr`, `qst-mind-body-interaction-problem`؛ `con-dream-interpretation-technique` بنوع «تقنية» | 5 | `odd_prefix_or_type.txt` |
| جذرُ المسار منقسمٌ بين «مدرسة التحليل النفسي» (210) و«التحليل النفسي» (234)، وكذلك «مدرسة علم النفس الفردي»/«علم النفس الفردي» | 444 | `crumb_roots.txt` |
| جذرُ مسارٍ عامٌّ «الناس ← فلان» بلا مدرسة (123) و«نقد» (25) | 148 | `generic_crumb_root.txt` |
| بلا `active_start` (يغيب عن الخطّ الزمني)؛ 204 منها في حقل `dates` سنةٌ صريحةٌ قابلةٌ للاستخراج | 1,171 | — |
| عناوين `##` مكرَّرة داخل الملفّ («القيد» مرّتين…) | 13 | `dup_headings.txt` |
| قسمٌ فارغ (< 15 حرفاً) — 8 منها «الترجمات العربية» فارغ | 15 | `empty_sections.txt` |

### المستوى 3 — جودةُ المحتوى (لا يُصلَح بسكربت)

- **612 ملفاً قسمُ «المصادر» فيه عبارةُ تحوّط لا مصادر** (386 علم نفس، 198 فلسفة، 28 جسر): 151 مفهوماً، 107 مفكّراً، 81 تقنية، 51 تياراً، 50 متلازمة… — `sources_placeholder.txt`. الفحصُ الحاليُّ يعدّها «فيها قسمُ المصادر».
- **251 مفكّراً متنُه أقلُّ من 120 كلمة** (35 أقلُّ من 50): 31 ملفاً محجوراً بصيغةٍ قالبية («هذا الملف في الحجر» / «لا تتوفر معلومات موثَّقة»)، و283 مفكّراً حقلُ `dates` فيه «غير مؤكد». 3 ملفات عنوانُها «[مدخل غير محقَّق — منسوب إلى TEACCH]» (اثنان منها نسخةٌ من بعض). — `short_bodies.txt`, `unverified_titles.txt`. **قرارٌ تحريريٌّ مطلوب:** حذف / دمج / إبقاء بوسم.
- 129 ملفاً غيرَ مفكّرٍ متنُه أقلُّ من 120 كلمة (82 مفهوماً، 23 تياراً منها `br-gestalt-berlin` و`br-gestalt-therapy` و`br-psychosynthesis`).

### المستوى 4 — البنية والبناء

- **2,162 تغييراً غيرَ مُلتزَمٍ في git** (2,081 معدَّلاً + 80 جديداً بينها 21 مدرسةً جديدة و4 مفاهيم يونغية و7 تقارير). خطرُ ضياعٍ؛ يجب الالتزامُ أوّلاً.
- `content/en/`: 130 ملفاً كلُّها «[EN TRANSLATION NEEDED]»، و`index-en.html` مبنيٌّ منذ 19 أغسطس. إمّا يُحدَّث أو يُعلَن مجمَّداً.
- عنوانُ الصفحة في `shell_prefix.html` ما زال «… يضم حالياً: المدرسة الوجودية في العلاج» — بقيّةُ الإصدار 7.1.
- `.claude/launch.json` يشير إلى `/Users/minamoheb/Desktop` (مسارٌ غيرُ موجود).
- `audit_atlas.py` لا يفحص: أهدافَ `edges` غيرِ `belongs_to`، نوعَ هدف حروف النسب، التسلسلَ الزمني، التكرارَ عبر `en`، مطابقةَ `part` للأب، مطابقةَ slug للاسم، الإحالاتَ غيرَ الموسومة، جودةَ قسم المصادر.

---

## ج. خطّةُ الإصلاح — بالترتيب

> القاعدة: بعد كلِّ مرحلة `python3 scripts/build_atlas.py && python3 scripts/audit_atlas.py` ثم إعادةُ تشغيل سكربتات `scripts/audit_2026-09-09/` والتحقّقُ من انخفاض الأعداد، ثم **commit** مستقلّ.

### المرحلة 0 — تأمين (نصف ساعة)
1. `git add -A && git commit` لكلِّ التغييرات المعلَّقة (المدارس الـ21 الجديدة وتقاريرُ 09-07/08 غيرُ محفوظة).
2. إضافةُ `scripts/audit_2026-09-09/` إلى المستودع كمرجعٍ للأعداد الأساسية.

### المرحلة 1 — الأنسابُ والحروفُ الخاطئة (يومٌ واحد، سكربت + مراجعة)
1. **سكربت** `strip_stamped_syndrome_parents.py`: يحذف `belongs_to → sch-cognitive-behavioral` من كلِّ `syn-`/`dis-`/`evt-` لا يذكر متنُه CBT (171 ملفاً)، ويُبقي الـ6 التي تذكره للمراجعة. لا يُعوَّض بأبٍ آخر: المتلازمةُ كيانٌ تصنيفيٌّ لا عضوٌ في مدرسة. المسارُ (crumb) يبقى «الاضطرابات والمتلازمات ← …».
2. **مراجعةٌ يدوية** لـ 40 سياقاً (`ctx-`) ذات `belongs_to`: يُحذف النسبُ حيث لا يسنده المتن (الإصلاح الطباعي → الإنسانية، إلخ).
3. **مراجعةُ حروف النسب الـ657** في `school_edges.txt` بجدولٍ من ثلاثة أعمدة (إبقاء / حذف / عكس)؛ تبدأ بالـ30 حرفَ `superseded_by`/`absorbed_by` والـ46 `evolved_into` — الجدولُ في ب/0.2 يغطّي ~35 منها. القاعدة: لا يبقى حرفُ نسبٍ لا يذكره متنُ أحد الطرفين.
4. **سكربت** لتحويل الـ7 حروفِ نسبٍ ذات الهدف غير المدرسي إلى `relates_to`/`developed_by`، وتصحيح `sch-mbct → sch-cognitive-behavioral`، وحذف `sch-donghak → sch-donghak`.
5. **تصحيحُ تواريخ المدارس** الخمس: `sch-akbari` (1165→ مستمر)، `sch-hermeneutics` (1800)، `sch-zoroastrian-philosophy` (−1000 على الأقلّ)، `sch-samkhya` (−600)، `sch-phil-science` (1600 أو تُنقل غاليليو/نيوتن/بيكون إلى «الثورة العلمية»). ثم إعادةُ تشغيل فحص الأنكرونية؛ ما يبقى (دوستويفسكي، تروث، إريجينا…) يُنقل نسبُه إلى `relates_to`.

### المرحلة 2 — الأشخاصُ الخطأ والملفاتُ الناقصة (يومٌ واحد)
1. إنشاء `thk-zeno-citium` (زينون الرواقي) وتحويلُ رابط الرواقية إليه؛ إبقاءُ زينون الإيلي في الإيلية فقط.
2. تصحيح `rel-mbct-cbt`: `thk-segal → thk-zsegal`.
3. **مراجعةُ الـ371 سطراً** في `related_stale_substantive.txt`: لكلِّ سطرٍ إمّا تحديثُ العنوان (تشكيل/ترجمة) أو **تبديلُ الهدف** إن كان العنوانُ المكتوب يقصد شخصاً آخر (كحالة سيغال). يُفرَز أوّلاً بالبادئة `thk-` (الأخطرُ).
4. حلُّ الـ34 هدفاً نصّياً: 6 تُحوَّل إلى المدارس الجديدة (القائمة في ب/1.1)؛ إنشاءُ غوفمان وغاردنر وسبيرمان (أعمالُهم موجودةٌ بلا مؤلّف)؛ حذفُ `split_into` النصّية الخمسة من `sch-developmental` وتحويلُها إلى ذكرٍ في المتن ما لم تُنشأ ملفاتُها.
5. مراجعةُ الـ35 slug المذكورةِ بلا ملفّ في `gaps`/المتن: قرارٌ لكلٍّ (إنشاء / إعادة توجيه / حذف الذكر). السوسيولوجيّون (بورديو، توكفيل، غوفمان، غارفينكل) يُحسَمون مع سؤال النطاق.

### المرحلة 3 — الدمجُ وتصحيحُ الـslugs (يومان، الأخطرُ على الروابط)
1. لكلِّ مجموعةٍ في `dup_groups.txt` (51): اختيارُ الملفّ الأصلي (الأطولُ متناً والأصحُّ slug)، **دمجُ** المتن والـ`related` والـ`gaps` فيه، تحويلُ الآخر إلى إحالةٍ بحقل `redirect_to` **ووسمٍ في العنوان**، ثم إعادةُ توجيه كلِّ الروابط الواردة بسكربت (`fix_all_slug_references.py` موجود). التناقضاتُ (تريفي 2008/2011) تُحسَم بمصدرٍ وتُسجَّل في `gaps`.
2. الـ9 أزواجِ تقنياتٍ CBT/DBT/ACT: **قرارٌ تحريريٌّ** قبل أيِّ دمج — إن أُبقيت فيُضاف لكلٍّ سطرٌ يُميّز الصياغة ويربطها بشقيقتها.
3. الـ59 ملفاً ذات slug شخصٍ آخر: ما له مكرَّرٌ صحيحُ الـslug يُدمج فيه (المرحلة 3/1)؛ ما لا مكرَّرَ له يُعاد تسميتُه إلى slug صحيح مع إحالةٍ من القديم.
4. زرادشت: دمجُ `thk-zoroaster` في `thk-zarathushtra`.

### المرحلة 4 — الإحالاتُ والوصول (نصف يوم، سكربت)
1. سكربت يُضيف `redirect_to` لكلِّ الـ59 إحالة، ويوسم العنوانَ بـ«— إحالة» للـ12 غيرِ الموسومة، ويُعيد توجيهَ الـ45 رابطاً والـ5 حروفٍ الواردةَ إلى الأهداف النهائية.
2. تحديثُ `deprecated()` في `audit_atlas.py` ليقرأ `redirect_to`، واستثناءُ الإحالات من فهرس البحث في الواجهة.
3. سكربت تبادُلٍ: لكلِّ `belongs_to` يُضاف الابنُ إلى `related` الأب (3,167 إضافة — تُقسَّم دفعاتٍ حسب الأب، وتُوسم في `gaps` كما فُعل في 09-08). يُغلق معظمَ الـ118 مدرسةً الفارغة.
4. `data_extras.json`: `thk-buber → thk-mbuber`.

### المرحلة 5 — البياناتُ الوصفية (نصف يوم، سكربت واحد `normalize_metadata_2026-09-09.py`)
1. `part` = `part` الأب لكلِّ عقدةٍ غيرِ جسرٍ تحت أبٍ غيرِ جسر (533)؛ الـ53 المعكوسة تُراجَع يدوياً قبل التطبيق.
2. `target_type` في `edges` و`title`/`type` في `related` تُعاد كتابتُها من `data.json` (256 + 1,566 + 555) — بعد المرحلة 2/3 حتى لا تُثبَّت أهدافاً خاطئة.
3. `level`: تعبئةُ 17 فارغاً، تحويل «أساسي» → «مبتدئ».
4. `en`: نقلُ الحروف الصينية/اليابانية إلى المتن وإبقاءُ اللاتينية (14).
5. `active_start`: استخراجٌ من `dates` حيث توجد سنةٌ صريحة (204).
6. توحيدُ جذر المسار: «مدرسة التحليل النفسي» → «التحليل النفسي»، «مدرسة علم النفس الفردي» → «علم النفس الفردي» (444)؛ الـ123 «الناس ← فلان» تُعطى مدرسةَ الأب في الجزء الأوّل.
7. `crumbs:` → `crumb:`؛ إعادةُ تسمية `classification-*` إلى `dis-classification-*` و`qst-` → `que-` مع إحالة؛ `part: sociology` الثلاثة تُقرَّر (جسر أو حذف).
8. العناوينُ المكرَّرة (13) والأقسامُ الفارغة (15): حذفٌ أو تعبئة.

### المرحلة 6 — جودةُ المحتوى (مستمرّ، تحريري)
1. الـ612 ملفاً بلا مصادر: تُقسَّم حسب `part` والنوع؛ الأولوية للمدارس والتيارات (52) ثم المفاهيم (151). يُضاف فحصٌ إلى `audit_atlas.py` يعدّ قسمَ المصادر «فارغاً» إن قصر عن 40 حرفاً أو حوى عبارةَ تحوّط.
2. الـ31 ملفاً محجوراً و3 «غير محقَّق»: قرارُ رئيس التحرير (حذفٌ نهائيٌّ أو إبقاءٌ بوسم `status: quarantined` يُخفيها من البحث).
3. الـ23 تياراً القصيرَ (خاصةً الجشطالت برلين وعلاج الجشطالت والتركيب النفسي — عقدٌ مركزية): توسيعٌ إلى 400 كلمة على الأقلّ بمصادر.

### المرحلة 7 — تحصينُ الفحص والبناء (نصف يوم)
1. ضمُّ فحوص `deep_audit.py`–`deep5.py` إلى `audit_atlas.py` كفحوصٍ قاطعة: أهدافُ كلِّ `edges`، نوعُ هدف حروف النسب، عكسُ الزمن في النسب، التكرارُ عبر `en` المُطبَّع، `part` ≠ الأب، slug ≠ الاسم، الإحالاتُ بلا `redirect_to`، قسمُ مصادرٍ قالبي، slugs في `data_extras.json` بلا ملفّ.
2. تصحيحُ عنوان الصفحة في `shell_prefix.html` ومسار `launch.json`.
3. قرارٌ بشأن `content/en/` (تجميدٌ مُعلَن أو إعادةُ بناء).

---

## د. أرقامُ المرجع (قبل الإصلاح)

| المؤشّر | الآن | الهدف |
|---|---|---|
| أنسابٌ مختومةٌ (syn/dis/evt → CBT) | 183 | 0 |
| حروفُ نسبٍ معكوسةٌ زمنياً (آلي) | 15 | 0 |
| حروفُ نسبٍ هدفُها غيرُ مدرسة | 7 | 0 |
| حروفُ `edges` بهدفٍ نصّي | 34 | 0 |
| مجموعاتٌ مكرَّرةٌ كاملة | 51 | 0 (أو قرارٌ موثَّق للـ9 التقنيات) |
| slug مفكّرٍ لا يطابق اسمَه | 59 | 0 |
| `part` ≠ الأب | 533 | 0 |
| روابطُ/حروفٌ إلى إحالات | 50 | 0 |
| إحالاتٌ بلا `redirect_to` | 18 | 0 |
| ابنٌ لا يظهر في `related` أبيه | 3,167 | 0 |
| مدارسُ بلا أعضاءٍ وبأقلّ من 3 روابط | 118 | < 10 |
| `target_type`/`title`/`type` غيرُ مطابق | 256 / 1,937 / 555 | 0 |
| مصادرٌ قالبية | 612 | يُقاس شهرياً |
| مفكّرون < 120 كلمة | 251 | قرارٌ تحريري |
