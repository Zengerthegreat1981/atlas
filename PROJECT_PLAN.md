# خطة أطلس النفس البشرية والفلسفة — المسار للاكتمال

هذا الملف هو خطة العمل المرجعية للمشروع. الهدف: تجميع كل خطوات "الداتا" (المحتوى) المطلوبة
قبل الدخول في تنفيذ متواصل بدون توقف لإعادة التخطيط. بمجرد اعتماد هذا الملف، أي عمل لاحق هو
Execution على بنوده، لا تخطيط من جديد.

## 0. الرؤية النهائية (كما حددها رئيس التحرير)

الأطلس مشروع موسوعي، لا "أطلس مدرسة واحدة". الهدف النهائي: **مفيش حد مهم في تاريخ علم النفس
غير مكتوب، وكذلك مفيش حد مهم في تاريخ الفلسفة غير مكتوب**، مع تغطية الجسر بينهم.

المشروع مقسّم لثلاثة أقسام كبرى:

| # | القسم | الحجم | المحتوى |
|---|---|---|---|
| 1 | **علم النفس** | الأكبر | كل مدارس العلاج/علم النفس، وتحتها مفكرين، مؤلفات، جدالات، مفاهيم، اضطرابات، متلازمات، أدوات قياس، تقنيات، دراسات... كل أنواع الملفات العشرين |
| 2 | **الفلسفة** | كبير | كل فيلسوف مهم بمعنى الكلمة الواسع، بغض النظر عن صلته المباشرة بعلم النفس |
| 3 | **الجسر (فلسفة ↔ علم نفس)** | صغير | الكتب/المفكرين اللي كانوا نقطة وصل مباشرة أو مصدر إلهام موثّق بين الاثنين |

## 0.1 خطة قسم علم النفس التفصيلية (منفصلة، وأهم ملف تنفيذي فعلي)

هذا الملف (`PROJECT_PLAN.md`) بيحدد **المراحل والترتيب الزمني والقرارات المعمارية**. الجرد التفصيلي
الفعلي لكل حاجة مطلوب كتابتها في قسم علم النفس — بمدارسه ومفاهيمه واضطراباته ومتلازماته وأدواته
وتقنياته وجدالاته... إلخ، جدول لكل نوع محتوى — موجود في ملف منفصل:
**[PSYCHOLOGY_PART_PLAN.md](PSYCHOLOGY_PART_PLAN.md)**. ده الملف اللي فعلياً جاهز يتدّى لنموذج الكتابة
كـ backlog تنفيذي. لما قسم الفلسفة يبدأ (مرحلة 4) هيتعمله ملف مماثل `PHILOSOPHY_PART_PLAN.md`، وكذلك
الجسر.

## 1. قرار معماري لازم يتاخد قبل أي كتابة جديدة (Open Decision)

**المشكلة:** كل الملفات الحالية (388 ملف) مبنية على افتراض ضمني إن الأطلس = المدرسة الوجودية فقط.
دليل ذلك:
- كل الـ `thinkers/` (299 ملف) نوعهم `type: "مفكر"` موحّد، وكلهم مربوطين بـ `edges: belongs_to → "المدرسة الوجودية"`.
- مفيش حقل حالياً يميّز "فيلسوف بحت" عن "معالج/باحث نفسي" — حتى سارتر وهايدجر وكامو (فلاسفة بالكامل) مُدرجين تحت نفس مظلة "المدرسة الوجودية" لأنهم أثّروا فيها، مش لأن الفلسفة قسم مستقل.
- مجلد `schools/` فاضي تماماً (0 ملفات) — لسه معندناش تمثيل مستقل حتى لـ"المدرسة الوجودية" نفسها كعنصر.

**الأثر:** لو بدأنا نكتب مدرسة التحليل النفسي أو الفلسفة بنفس البنية الحالية (كل حاجة `belongs_to` مدرسة واحدة اسمها ثابت في edges)، هيبقى صعب بعدين نعرض الأطلس كموسوعة بثلاثة أقسام كبرى بدل "مدرسة واحدة وملحقاتها".

**الاقتراح** (يحتاج موافقتك قبل أي كتابة تانية):
1. إضافة مستوى تصنيف جديد فوق `edges.belongs_to` الحالي: حقل `part` في الـ frontmatter بقيمة من ثلاثة (`psychology` / `philosophy` / `bridge`)، أو ببساطة تفعيل مجلد `schools/` فعلياً وخلي كل عنصر يرتبط بمدرسته الحقيقية (وجودية / تحليل نفسي / سلوكية / إنسانية...) بدل نص ثابت.
2. أي مفكر فيلسوف بحت (مالوش تطبيق إكلينيكي مباشر) يتصنف تحت قسم "الفلسفة" حتى لو مذكور كمرجع تأثير في ملف وجودي — الربط بينهم يبقى عبر `related`، مش عبر `edges.belongs_to` وهمي.
3. `schools/` يتفعّل فعلياً: كل مدرسة (وجودية، تحليل نفسي، سلوكية-معرفية، إنسانية...) بقى ليها ملف واحد تمثيلي في `schools/` تتجمع عليه كل الروابط، بدل نص حر في `edges.target`.

هذا القرار لازم يتاخد ويتنفذ (حتى لو بأثر رجعي بسيط على الملفات الحالية) **قبل** ما نبدأ ندخل بجدية في مدرسة التحليل النفسي، عشان مانكررش نفس الشغل مرتين.

## 2. الجرد الحالي (Snapshot — 17 أغسطس 2026)

### علم النفس — المدرسة الوجودية (شبه مكتملة كبنية أساسية)
- `thinkers/`: 299 (المدرسة الوجودية بالكامل)
- `branches/`: 4 من أصل 4 تيارات معروفة (بريطاني، Daseinsanalysis، إنسانية، لوغوثيرابيا)
- `concepts/`: 22 · `debates/`: 11 · `works/`: 14 · `instruments/`: 5 · `techniques/`: 5 · `disorders/`: 5 · `contexts/`: 6 · `studies/`: 4 · `relations/`: 4
- مجلدات رقيقة جداً (تحتاج مراجعة هل هي فعلاً مكتملة أو لسه ناقصة): `axioms`:1، `critiques`:1، `dialogues`:1، `events`:1، `experiences`:1، `metaphors`:1، `questions`:1، `syndromes`:1، `terms`:1
- `schools/`: 0 (لسه ماتفعّلش، شوف القسم 1)
- فحص هيكلي (`/atlas-review`): **صفر أخطاء** (0 dangling، 0 duplicate، 0 orphan) اعتباراً من هذا التاريخ.

### علم النفس — مدرسة التحليل النفسي (بداية فقط، في drafts/)
- `content/ar/drafts/thinkers/`: **70 مسودة** جاهزة للمراجعة (وكيل 4) — لسة محدش منها نُقل لـ `content/ar/thinkers/`.
- باقي الأنواع (concepts, debates, works, disorders, instruments...) الخاصة بالتحليل النفسي: **لسه معمول عليها حاجة قليلة أو مفيش خالص** — المسودات حالياً مفكرين بس.
- `content/ar/drafts/thinkers/` كمان فيه **18 مسودة** لمفكرين وجوديين إضافيين لسه ماتراجعوش.

### الفلسفة (قسم مستقل)
- **لسه مفيش قسم مستقل.** الفلاسفة الموجودين حالياً (سارتر، هايدجر، كامو، بوبر، هوسرل، هيغل...) مذكورين بس كـ"مفكرين وجوديين" ضمن المدرسة الوجودية، مش كجزء من تغطية فلسفية شاملة (يعني نيتشه وماركس وفوكو وأدلر لسه مش موجودين أصلاً حتى كمراجع — طلعوا كفجوات في مراجعة agent-3 اليوم).

### الجسر (فلسفة ↔ علم نفس)
- لسه مفيش تصنيف مستقل له؛ حالياً بيتمثل بشكل مبعثر جوه `relations/` و`contexts/` و`works/` تبع الوجودية بس.

### الإنجليزية
- `content/en/` مرآة كاملة بنفس البنية، لسه placeholder بالكامل (`[EN TRANSLATION NEEDED]`) — مؤجل لحد ما المحتوى العربي يستقر.

## 3. خط الأنابيب لكل قطعة محتوى (ثابت، ينطبق على أي مدرسة/قسم جديد)

```
1. الكتابة    → نموذج أرخص يكتب مسودة في content/ar/drafts/<folder>/  (موجّه: draft-writer-brief.md)
2. المراجعة   → وكيل 4 (draft-review-agent) يفحص شكل + محتوى كل دفعة مسودات
3. الاعتماد   → موافقة رئيس التحرير على كل ملف (اعتماد / تعديل / رفض) — Human-in-the-Loop إلزامي
4. النقل      → mv من drafts/ للمكان النهائي بعد أي تصحيح
5. فحص الشبكة → /atlas-review (وكلاء 0-1-2-3) على المحتوى المعتمد الجديد بعد الدمج
6. الموافقة   → رئيس التحرير يوافق على أي روابط `related` جديدة مقترحة من الفحص
7. البناء     → python3 scripts/build_atlas.py ar (وأخيراً en لما يحين وقت الترجمة)
```
كل دفعة محتوى جديدة (سواء مدرسة كاملة أو ملف واحد) تعدّي بنفس الخطوات السبع دي، بالترتيب، بدون تخطي.

## 4. الترتيب المقترح للتنفيذ (Phasing)

> ملاحظة: هذا ترتيب مقترح بناءً على الوضع الحالي (تحليل نفسي أقرب للجاهزية لأن فيه 70 مسودة موجودة بالفعل). يحتاج تأكيدك أو تعديلك.

### المرحلة 0 — القرار المعماري (القسم 1 أعلاه)
تفعيل `part` (psychology/philosophy/bridge) و`schools/` الفعلي، وتطبيقه بأثر رجعي على الوجودية الحالية كأول مدرسة مسجلة رسمياً. هذه خطوة بنيوية لازم تسبق أي محتوى جديد.

### المرحلة 1 — إغلاق فجوات الوجودية الحالية
- مراجعة واعتماد الـ 18 مسودة الوجودية الإضافية الموجودة في `drafts/thinkers/`.
- تشغيل نتائج مراجعة اليوم (agent-2 + agent-3) والموافقة على البنود المهمة (زي thk-freud، thk-adler، thk-foucault، thk-marx اللي طلعوا "يحتاج إنشاء عنصر جديد" — لاحظ: دول هيبقى مكانهم الطبيعي "الفلسفة" أو "الجسر"، مش الوجودية، حسب القرار المعماري).
- تقييم المجلدات الرقيقة (axioms, critiques, dialogues, events, experiences, metaphors, questions, syndromes, terms) — هل ملف واحد لكل نوع كافٍ فعلاً وﻻ ده نقص محتوى محتاج توسيع؟

### المرحلة 2 — مدرسة التحليل النفسي (كاملة، مش مفكرين بس)
- مراجعة واعتماد 70 مسودة المفكرين الموجودة (وكيل 4).
- تكليف النموذج الأرخص بكتابة باقي الأنواع لنفس المدرسة: concepts (اللاوعي، الكبت، الأنا/الهو/الأنا الأعلى...)، debates (فرويد ضد يونج، فرويد ضد أدلر...)، works (تفسير الأحلام، ما وراء مبدأ اللذة...)، disorders، instruments، techniques، branches (فرويدية كلاسيكية، يونجية، لاكانية، العلاقات بالموضوع...).
- فحص شبكة كامل بعد الدمج.

### المرحلة 3 — باقي مدارس علم النفس (تُرتَّب لاحقاً)
مرشحين محتملين (يحتاج ترتيبك): السلوكية، المعرفية-السلوكية (CBT)، الإنسانية (لو معتبرة مستقلة عن الفرع الوجودي-الإنساني الموجود أصلاً في `branches/br-humanistic.md`)، الجشطالتية، النظامية/الأسرية، التطورية، البيولوجية/العصبية، الاجتماعية...

### المرحلة 4 — قسم الفلسفة المستقل
بناء تغطية فلسفية شاملة (ما قبل سقراط → معاصرين) بمعزل عن ربطها الإجباري بعلم النفس — يشمل استيعاب الفلاسفة المذكورين حالياً كـ"مفكرين وجوديين" بإعادة تصنيفهم تحت `part: philosophy` مع إبقاء روابطهم بالوجودية عبر `related`.

### المرحلة 5 — قسم الجسر
تحديد وتوثيق الكتب/الشخصيات اللي كانت نقطة وصل موثقة (زي وليام جيمس، فرويد كفيلسوف ضمناً، ميرلوبونتي، إلخ) — قسم صغير لكن يحتاج نفس الصرامة في التوثيق.

### المرحلة 6 — الترجمة الإنجليزية
تُبدأ بعد استقرار جزء كامل (مش قبل كده) — نفس القرار الحالي بالتأجيل.

## 5. القرارات (اعتمدت بتاريخ 17 أغسطس 2026)

### 5.1 القرار المعماري — ✅ معتمد
موافَق على `part` (psychology/philosophy/bridge) + تفعيل `schools/` فعلياً. يُنفَّذ في المرحلة 0 قبل أي محتوى جديد.

### 5.2 تصحيح مهم — فرويد وأدلر مش فلاسفة، هما علم نفس
رئيس التحرير صحّح نقطة كانت غلط في مسودة الخطة الأولى: **فرويد وأدلر وماركس مش فلاسفة**. التصحيح اتطبق كالتالي:
- **فرويد وأدلر**: ينتموا لـ `part: psychology`، تحديداً مدرسة التحليل النفسي (فرويد = العمود الفقري لها، أدلر = مؤسس علم النفس الفردي كأحد فروعها/امتداداتها المبكرة). إنشاؤهم يبقى **أول خطوة فعلية في المرحلة 2** (مدرسة التحليل النفسي)، مش مؤجل لمرحلة الفلسفة.
- **ماركس**: مش فيلسوف بمعنى الأطلس (هو مصدر إلهام لمفاهيم زي "الاغتراب" استُخدمت لاحقاً نفسياً/وجودياً) — يتصنف مبدئياً في `part: bridge` كمصدر تأثير فكري لا مدرسة علاجية، إلا لو ظهر لاحقاً إن وضعه الطبيعي فلسفة بحتة.
- باقي الأسماء اللي طلعت من مراجعة اليوم كـ"يحتاج إنشاء" وهما فلاسفة فعلاً (نيتشه، فوكو) → قاعدة القسم 5.3 تحتهم.

### 5.3 قاعدة "الفلاسفة في السكة" — ✅ معتمدة
الفلسفة كقسم كامل متكامل **مؤجلة بالكامل** لحد ما علم النفس (كل مدارسه) يخلص وتُبنى شبكته ويتراجع بالكامل (المرحلة 4 زي كانت). **لكن**: أي فيلسوف حقيقي بيظهر أثناء الشغل على مدرسة نفسية (زي نيتشه وهيغل وهوسرل وفوكو اللي طلعوا في مراجعة اليوم) **يُنشأ فوراً وقت ظهوره**، بحقل `part: philosophy` من أول يوم، بس بأقل قدر كافٍ (ملف مكتمل الحقول الأساسية + `related` بالمناسبة اللي جابته لظهوره)، مش بتوسّع كامل لشبكته الفلسفية الخاصة — التوسّع الكامل بتاعه يستنى المرحلة 4. الهدف: مانضيعش الرابط ولا نأجل الإنشاء لحد ما ننسى السياق، من غير ما نقلب المسار لشغل فلسفة مبكر.

### 5.4 ترتيب مدارس علم النفس (مرحلة 3) — مقترح مبني على القرابة التاريخية/المنهجية
بعد إغلاق الوجودية (مرحلة 1) والتحليل النفسي (مرحلة 2)، الترتيب المقترح:

| # | المدرسة | ملاحظة |
|---|---|---|
| 1 | **علم النفس التحليلي اليونغي** (Analytical/Jungian) | امتداد مباشر من انشقاق يونج عن فرويد — قرابة تاريخية قوية مع مرحلة 2، منطقي يجي فوراً بعدها |
| 2 | **علم النفس الفردي الأدلري** (لو مش كفى تغطيته كفرع داخل مرحلة 2) | نفس الدائرة الفيينية المبكرة |
| 3 | **السلوكية** (Behaviorism) | واطسون، سكينر، بافلوف — نقيض منهجي للتحليل النفسي، نقطة تحول تاريخية كبرى |
| 4 | **المعرفية والعلاج المعرفي السلوكي (CBT)** | امتداد مباشر من السلوكية (بيك، إليس) |
| 5 | **الجشطالتية** (كمدرسة علاج، مش بس فرع الوجودية الموجود) | فيرتهايمر/كوهلر كأصل، ثم بيرلز كتطبيق علاجي (بيرلز موجود جزئياً بالفعل ضمن الوجودية — يحتاج مراجعة تداخل) |
| 6 | **الإنسانية الكاملة** (روجرز، ماسلو — أوسع من فرع `br-humanistic` الوجودي الحالي) | تقاطع مباشر مع فجوات ظهرت في مراجعة اليوم (ماسلو/روجرز غير مربوطين بـ br-humanistic) |
| 7 | **النظامية/العلاج الأسري** (Systemic/Family) | باتيسون، مينوشين، بوين |
| 8 | **التطورية** (Developmental) | بياجيه، إريكسون، بولبي/التعلق |
| 9 | **الاجتماعية** (Social Psychology) | |
| 10 | **البيولوجية/العصبية** (Biological/Neuropsychology) | |
| 11 | **الإيجابية** (Positive Psychology) | سليجمان — حديثة نسبياً، منطقي تيجي متأخرة |
| 12 | **عبر الشخصية** (Transpersonal) | |

هذا الترتيب قابل للتعديل في أي وقت — مجرد نقطة بداية منطقية، مش قرار نهائي مقفول.

### 5.5 قسم الجسر — ✅ معتمد نبنيه
موافَق على عمل قائمة الجسر. تُبنى تدريجياً من نقطتين:
1. الأسماء اللي طلعت فعلاً من مراجعة اليوم كمرشحين للجسر (ماركس مبدئياً، وأي حالة مشابهة — مفكر أثّر في مدرسة نفسية لكنه مش معالج/باحث نفسي نفسه ولا فيلسوف نسقي بحت).
2. أي حالة تانية تظهر أثناء الشغل على مدارس علم النفس (مرحلة 2-3) — تُسجَّل هنا فور ظهورها بدل ما تتنسى.

قائمة أولية (تحتاج مراجعتك، مش نهائية): كارل ماركس (مصدر "الاغتراب").

---
*آخر تحديث: 17 أغسطس 2026 — القرارات في القسم 5 معتمدة. الخطوة التالية العملية: تنفيذ المرحلة 0 (القرار المعماري) ثم المرحلة 1 (إغلاق فجوات الوجودية، بما فيها اعتماد نتائج مراجعة اليوم).*
