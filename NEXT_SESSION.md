# ابدأ من هنا — تعليمات الجلسة التالية

> اقرأ هذا الملف أولاً، ثم `AUDIT_HANDOFF.md` للتفاصيل، ثم
> `scripts/audit_findings_log.md` لسجل ما وُجد ولماذا.

---

## 0. أوّل أمر تشغّله — قبل أي شيء

```bash
cd /Users/minamoheb/Desktop/Atlas && python3 scripts/reapply_thinkers_audit.py --apply
```

المستودع يُكتب فيه بالتوازي من جلسات أخرى، وتُستبدل الملفات أحياناً بنسخ أقدم.
هذا السكريبت **idempotent** ويعيد تطبيق كل التصحيحات المسجَّلة (2351 تصحيحاً في 795 ملفاً).
شغّله في **بداية** الجلسة و**بعد كل دفعة** عمل.

---

## 1. الوضع الحالي بالأرقام

| | |
|---|---|
| ملفات المفكّرين المعتمدة | **2292** |
| مُدقَّقة | **1389** (60%) |
| **متبقّية للتدقيق** | **903** — القائمة في `scripts/audit_unread_thinkers.txt` |
| تصحيحات مسجَّلة | 2351 في 795 ملفاً |
| ملفات قالبية بلا جسم | **0** (كُتب 205) |
| روابط معلَّقة في المحتوى المعتمد | **0** |
| بناء الأطلس | سليم |

---

## 2. المهمّة الأساسية: قراءة الـ903 ملفاً المتبقّية

**هذه هي المهمّة.** لا يكشفها الفحص الآلي — الأخطاء التي تُوجد بالقراءة فقط
هي: هويّة خاطئة لصاحب الملف، ومصطلح مقلوب المعنى، وعمل منسوب إلى غير صاحبه،
ومواقف نظرية معكوسة، وتواريخ مستحيلة.

**معدّل الأخطاء الحقيقية: 4–6 أخطاء لكل ملف.**

### طريقة العمل المجرَّبة

```bash
S=/tmp/claude-501/-Users-minamoheb-Desktop-claude/<session>/scratchpad   # أنشئ سكراتش خاصاً بك
# اقرأ الدفعة التالية (8 ملفات في المرّة مناسبة):
for f in $(head -8 scripts/audit_unread_thinkers.txt); do
  echo "════ $f"; sed -n '/^# /,$p' content/ar/thinkers/$f.md; done
```

ثم لكل تصحيح:
1. اكتب ملف JSON بالشكل `{"thk-slug.md": [["النص القديم","النص الجديد"], ...]}`
2. **تحقّق من التطابق الحرفي أوّلاً** (وإلا فالتصحيح لن يُطبَّق أبداً):
   ```bash
   python3 $S/mkfix.py $S/batch.json      # يفشل إن لم يتطابق نصّ واحد
   ```
   > ⚠️ انتبه للتشكيل والشدّة: «محرر» ≠ «محرّر». انسخ النصّ من الملف لا من ذاكرتك.
3. سجّل وطبّق:
   ```bash
   python3 scripts/add_audit_fix.py < $S/batch.json
   python3 scripts/reapply_thinkers_audit.py --apply
   python3 scripts/build_atlas.py ar
   ```
4. احذف الملفات المقروءة من `scripts/audit_unread_thinkers.txt`.

### الأخطاء المتكرّرة — ابحث عنها تحديداً

| النوع | مثال وُجد فعلاً |
|---|---|
| **الملف عن شخص آخر** | `thk-jgedo` كله عن «التنويم التحليلي» وجيدو لا صلة له بالتنويم |
| **موقف نظري معكوس** | `thk-jkihnstrom` نُسب إليه عكس مذهبه تماماً |
| **اسم أوّل خاطئ** | لوبورسكي «جوزيف» وهو **لِستر**؛ ماركويتز «جيمس» وهو **جون** |
| **جنسية/تواريخ** | ماك دوغال «أمريكية» وهي **نيوزيلندية**؛ فيرير مسجَّل ميتاً وهو حيّ |
| **مصطلح مقلوب** | `Behavioral Inhibition` ← «الانطواء» (وهما مختلفان) |
| **عمل لا وجود له** | كتب بعناوين مخترعة تماماً |
| **استحالة زمنية** | تتلمذ على أستاذ مات قبل مولده بعشرين سنة |
| **خلط اختصارات** | NLP (برمجة عصبية) ← NLP (معالجة لغة طبيعية) |

### قاعدة محرّرة مهمّة
كثير من الأسماء المتبقّية من **علم النفس الشعبي**. سجّل دائماً **الفجوة بين
الانتشار والسند**: فشل التكرار، والسحب، والنقد المنهجي — لا تكتفِ بالعرض.

---

## 3. قرار معلّق ينتظرك — الحجْر (264 ملفاً)

**هذا البند لا يتقدّم بلا تدخّلك.**

- 264 ملفاً غير قابلة للتحقّق (نص نائب ظاهر، أو قسم أعمال بلا عنوان كتاب واحد،
  أو الـslug يسمّي شخصاً و`en` يسمّي آخر).
- السكريبت جاهز ومُختبَر: `scripts/quarantine_unverified_thinkers.py --apply`
  (ينقلها إلى `drafts/` ويحوّل كل رابط وارد إلى ملاحظة `gaps` صريحة — صفر رابط مكسور).
- **نُفِّذ مرّتين وأُعيد مرّتين** خلال دقائق من جلسة متوازية. المانيفست في
  `scripts/quarantined_thinkers.json`، وما زالت الـ264 كلها في المجلد المعتمد.

**الخيارات:**
1. **أوقف الجلسات المتوازية** (Spark / MiniMax / Atlas Agent) ثم شغّل السكريبت — يستغرق ثوانيَ.
2. انقلها إلى عزلٍ على غرار `content/ar/_merged/` — **نجا** من كل الاسترجاعات
   لأن الجلسات الأخرى لا تعرفه، بخلاف `drafts/` الذي تكتب فيه.
3. اتركها وأضف تنبيهاً ظاهراً في كل ملف.

> ملاحظة: `[DRAFT-UNKNOWN]` ظاهر الآن في **221** ملفاً معتمداً — عادت مع الاسترجاع.

---

## 4. بنود أصغر جاهزة للإنجاز

- **3 ملفات بلا أي قسم**: `thk-isap`, `thk-jmertz`, `thk-ebosnak` — وهي ملفات
  **جمعيات لا أشخاص** (عناوينها «[مؤسِّسو كذا]»). تحتاج قراراً: تُكتب بوصفها
  مؤسسات، أم تُنقل، أم تُحذف من نوع «مفكر».
- **10 ملفات خارج `thinkers/`** فيها `[DRAFT-UNKNOWN]` (في `concepts/`
  و`techniques/`) — خارج نطاق تدقيق المفكّرين، لكنها تُعرض على الموقع.
- **مرشّحو تكرار جدد**: شغّل فحص التكرار بعد كل نموّ في المجلد:
  ```bash
  python3 scripts/merge_thinker_duplicates.py      # فحص فقط
  ```
  (دُمج 48 حتى الآن، محفوظة في `content/ar/_merged/` ولم تُحذف.)

---

## 5. أدوات جاهزة — لا تُعِد بناءها

| الأداة | الوظيفة |
|---|---|
| `scripts/reapply_thinkers_audit.py` | يعيد تطبيق كل التصحيحات — **idempotent ومُتحقَّق منه** |
| `scripts/add_audit_fix.py` | يسجّل تصحيحات جديدة (تراكمي، يتجنّب التكرار) |
| `scripts/merge_thinker_duplicates.py` | دمج مكرّرات — قابل للعكس (أرشيف لا حذف) |
| `scripts/quarantine_unverified_thinkers.py` | حجْر غير المتحقَّق منه + تحويل الروابط إلى gaps |
| `scripts/write_shell_bodies.py` | يستبدل جسماً قالبياً — **يرفض** أي ملف ليس قالبياً |
| `scripts/write_headless_bodies.py` | يضيف أقساماً لملف بلا عناوين — **يرفض** ما فيه `## ` |
| `scripts/fix_vocalized_thinkers.py` | ينزع التشكيل ويصلح الماركداون المكسور |
| `scripts/build_atlas.py ar` | يبني `data.json` و`index.html` |

**تحذيران من أخطاء وقعت فعلاً:**
- `write_shell_bodies.py` كان يكتفي بوجود العنوان فمحا 224 كلمة حقيقية في
  `thk-suarez` — شُدِّد الآن ليشترط وجود نصّ قالبي فعلي. **لا تخفّف الشرط.**
- `reapply_thinkers_audit.py` كان غير idempotent: زوجٌ نصُّه القديم **جزء من**
  بديله (`«ما أعطت»` → `«ما أعطته»`) كان يضيف حرفاً كل تشغيلة. أُصلح.
  **أي قياس اعتمد على تشغيله المتكرّر قبل 2026-08-23 يحتاج إعادة.**

---

## 6. أرقام لا تثق بها بلا إعادة قياس

عدّة بنود في التقارير القديمة كانت **مضخَّمة** أو **إيجابيات كاذبة**:

| ما قيل | الحقيقة |
|---|---|
| 8063 سطر frontmatter مكسور | **صفر** — كان أثر خلل السكريبت يقيس نفسه |
| 406 حقلاً بلا علامات اقتباس | **صفر** |
| 272 `crumb` لا ينتهي بالعنوان | **22** حقيقي (الباقي اسم مختصر مشروع) |
| 234 عنوان H1 يخالف `title` | **16** حقيقي (الباقي يضيف الاسم الإنجليزي) |
| 10 روابط معلَّقة | **صفر** |
| إزاحة `related` تُسقط عناصر | **لا تُسقط شيئاً** — تحقّقتُ من `data.json` مباشرةً |
| `edges.target` روابط مكسورة | **نصّ حرّ لا مرجع** — 360 منها لا تطابق أي مدرسة، وهذا طبيعي |

**القاعدة:** قِس بنفسك قبل أن تبني عملاً على رقم موروث.

---

## 7. ما لا تفعله

- لا تعيد تسمية أي `slug` — يكسر الروابط عبر المجلد كله. سجّل التعارض في `gaps`.
- لا تحذف ملفاً — انقله إلى أرشيف (`_merged/`) مع مانيفست.
- لا تلتزم (commit) بلا طلب: شجرة العمل فيها آلاف التغييرات من جلسات متوازية.
- لا تعتمد على `[غير مؤكد]` أو `[DRAFT-UNKNOWN]` بوصفها معلومة — هي علامة نقص.
