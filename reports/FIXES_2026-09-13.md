# تنفيذُ المراجعة — ما أُصلح فعلاً

**التاريخ:** 2026-09-13 · **المرجع:** `AUDIT_INDEPENDENT_2026-09-13.md`
**الفحوص بعد العمل:** `audit_atlas.py` **صفرُ فشلٍ قاطع** · `check_content_integrity.py`
**سليمٌ على 6,674 ملفاً** · البناءُ يُعاد إنتاجُه · لا خطأ console في الصفحة.

نُفِّذت بنودُ جدول العمل الأحد عشر بالترتيب، ومعها ما ظهر أثناء التنفيذ.

---

## 1. السكربتات — 76 مساراً مثبَّتاً و3 لا تُصرَّف

- **3 سكربتاتٍ كانت لا تُصرَّف أصلاً** صُلِّحت: `analyze_phantoms.py` (سطورٌ داخل سلسلة)،
  `densify_reciprocal_links.py` (ثلاثةُ أخطاءٍ: اقتباساتٌ متداخلة، وسطرٌ داخل regex،
  ومرجعٌ خلفيٌّ كُتب محرفَ تحكّمٍ `\x01`)، و`generate_all_expansion_batches.py`
  (شرطةٌ خلفيةٌ في f-string + خمسةُ سطورٍ داخل سلاسل). **209 سكربتاً تُصرَّف الآن كلُّها.**
- **76 سكربتاً** كان يثبّت `/Users/minamoheb/Desktop/Atlas` وهو غيرُ موجود. استُبدل بجذرٍ
  مشتقٍّ من موضع الملفّ. الأهمُّ أنّ **43 منها كان يُخفق بصمت**: مُجرَّبٌ قبلُ وبعدُ —
  `check_all_queries.py` كان يقول إنّ `seligman` و`piaget` و`milgram` غيرُ موجودين،
  وصار يجدهم (`thk-mseligman`, `thk-piaget`, `thk-stanley-milgram`).
- `agents_specs/write_recheck.py` كذلك، و11 وثيقةً فيها أوامرُ `cd` بمسارٍ ميّت.

## 2. الروابط الخارجية — 3,082 رابطاً فُحصت كلُّها

**تصحيحٌ لتقرير المراجعة:** ما ذُكر أنّه «42 رابطاً مبتوراً بقوسٍ غير مغلق» كان
في أكثره **خطأً في أداة الاستخراج** لا في الملفات: الروابطُ في الملفّ سليمةٌ
بأقواسها، والأداةُ هي التي قطعتها عند `)`. أُعيد الاستخراجُ بموازنةِ الأقواس وبقراءةِ
صيغة `<url>`، ثم أُعيد فحصُ كلِّ ما لم يثبت سلامتُه.

| | |
|---|---|
| ميتٌ عولج بلقطةِ أرشيفٍ متحقَّقةٍ من Wayback | **60 رابطاً في 58 ملفاً** |
| ميتٌ بلا بديلٍ يمكن التحقّقُ منه — حُذف وسُجِّل في `gaps` | **57 رابطاً في 54 ملفاً** |
| بلاغاتٌ كاذبةٌ من أداة الاستخراج (أقواسٌ و`<>`) | 45 |
| تعذّر التحقّقُ منه من هنا (حجبُ روبوتات) — تُرك | 25 |

لم يُخترَع بديلٌ قط: كلُّ بديلٍ فُحص بطلبٍ فعليٍّ قبل قبوله. ومداخلُ SEP السبعةُ
(`entries/thales/`, `entries/condorcet/`, `entries/cynicism/`, `entries/dussel/`,
`entries/extended-mind/`, `entries/gestalt-psychology/`, `entries/islamic-theology/`)
**لا وجودَ لها في الموسوعة أصلاً** — كانت دعاوى مصدرٍ لا مصدراً، فحُذفت ونصُّها مُثبتٌ
في `gaps` كي لا يضيع الأثر.

## 3. سبعةُ ملفاتٍ بترويسةٍ مكسورة

حُذف الفاصلُ `---` الزائد. الفجواتُ التي كان البناءُ لا يقرؤها صارت تُقرأ:
`thk-amy-morgan` 0←4، `thk-deepak-ramsubeik` 0←5، `thk-jlubar` 0←4، `thk-rhooton` 2←6.
وثلاثةُ ملفات متلازماتٍ لم يعد متنُها يبدأ بسطر `---`.
وظهر عند الإصلاح مفتاحُ `gaps` مكرَّرٌ في `thk-rhooton` كان مختبئاً خارجَ الترويسة — دُمج.

## 4. الفحصُ الأخضرُ الكاذب

`audit_atlas.py` كان يبحث عن السلسلة `"## المصادر"` في كامل النصّ، فتُرضيه جملةٌ داخل
`gaps` تقول «لا يوجد قسم `## المصادر`». صار `re.search(r'^##\s*المصادر', …, re.M)`،
فظهرت **35 ملفاً بلا القسم** كانت مخفيّة.
وكذلك `deprecated()` كان يميّز المهجورَ بالعنوان وحدَه فيفوته المحجورُ الموسومُ
بـ`status: quarantined`؛ صار يقرأ الوسمَ و`redirect_to`.

## 5. بقايا وأخطاءٌ في المتن

- `</content>` — بقايا توليدٍ في **13 ملفاً** (6 حيّة) حُذفت.
- جدولُ `axm-holism-gestalt`: أربعةُ صفوفٍ بخانتين مقابل ثلاث — أُكملت بالأمثلة
  القياسية لقوانين الجشطالت (**محتوًى كتبتُه أنا: يستحقّ مراجعةَ محرِّر**).
- حرفٌ مكرَّرٌ في `br-logotherapy` (`relates_to → thk-langle` مرّتين) — حُذف.
- `thk-mbalint`: قسمُ «القيد» كان محتواه «توفي 1970، عمر 74» — جملةٌ سِيَريّةٌ لا قيداً،
  والتاريخُ مثبتٌ أصلاً في `dates`. أُزيلت وسُجِّل السببُ في `gaps`.
- **44 ملفاً** فيه `«` و`»` غيرُ متوازنة — أُغلقت كلُّها: 17 آلياً بنمطٍ واحد
  (`**«عنوان (English, 1995)**`)، و**45 موضعاً مقروءاً واحداً واحداً**.

## 6. مُحوِّلُ الماركداون — أكبرُ تغييرٍ يراه القارئ

القالبُ كان يحقن متنَ الملفات في الصفحة نصّاً خاماً بلا أيِّ تحويل، فتظهر النجومُ
والأقواسُ حرفيّةً. أُضيف `md()` في `shell_suffix.html` يعالج:

`**عريض**` · `*مائل*` · `***كلاهما***` · `**عريضٌ فيه *مائل***` · `` `شفرة` `` ·
قوائمَ `- ` و`1.` · عناوينَ `#### ` · اقتباساتِ `> ` · كتلَ ` ``` ` (رسومٌ نصيةٌ تُعرض كما هي) ·
`[نص](slug)` → رابطٌ داخليّ · `[نص](http…)` و`<http…>` والروابطَ العاريةَ → روابطُ خارجيةٌ تُنقَر.

قواعدُ السلامة: وسومُ `<b>` و`<br>` المكتوبةُ في المتن تمرّ كما هي؛ و`wrk-<عمل>-<مؤلِّف>`
لا تُعامَل وسماً؛ ورابطٌ هدفُه slug غيرُ موجودٍ يسقط قوساه ويبقى نصُّه.

**التحقّق:** `md()` شُغِّلت على **61,232 حقلَ نصٍّ** في الأطلس — **صفرُ بقايا ماركداون
وصفرُ استثناء**. و**24 رابطاً** كانت مكتوبةً `<https://…>` كان المتصفّحُ يبتلعها وسماً
مجهولاً فتختفي من الصفحة كلَّها — صارت ظاهرةً قابلةً للنقر.

## 7. البياناتُ المكتوبةُ ولا تُعرض

أُضيفت **بطاقةُ «بياناتُ العنصر»** تعرض نحوَ ثلاثين حقلاً كانت مكتوبةً ولا يقرؤها
القالبُ إطلاقاً — تظهر اليوم في **2,060 عقدة**: رموزُ DSM-5-TR وICD-11، وسيكومتريةُ
الأدوات (عددُ البنود، الثبات، الصدق، الفئةُ المستهدَفة)، وتصميمُ الدراسة وحجمُ عيّنتها
ونتيجتُها وحالُ تكرارها، ومؤلِّفُ العمل وسنةُ نشره ولغةُ أصله، ودرجةُ الدليل والسِّجلّ
والأصلُ الثقافيّ. والقيمُ المعجميةُ الإنجليزيةُ تُترجَم عند العرض فقط
(`well-established` ← «راسخة») — الملفُّ لا يُمسّ، فتبقى الفحوصُ تقرأ الرمز.
واسمُ المؤلِّف يصير رابطاً إلى ملفّه حين يوجد `author_slug` يُحلّ.

وأُضيف **صندوقُ الإحالة**: `redirect_to` كان مكتوباً في **86 عقدة** ولا تقرؤه الواجهةُ
أصلاً، فيقف الزائرُ على صفحةٍ مسدودة. صار يعرض المدخلَ القانونيَّ وزرَّ انتقالٍ إليه.

## 8. التكرارُ عبرَ البادئات — دُمج

فحصُ التكرار كان يجمع بمفتاحٍ أوّلُه بادئةُ الـslug فلا يقارن بادئتين مختلفتين أبداً.
صار يقارن العنوانَ عابراً للبادئات، بثلاثِ درجات: تكرارٌ **داخلَ** البادئة يُفشِل كما كان،
وتكرارٌ **عبرَ** البادئات تنبيهٌ (قرارُ الدمج تحريريّ)، والاسمُ الإنجليزيُّ مرشَّحُ مراجعة.

ثمّ **دُمجت المجموعاتُ كلُّها** بأداةٍ مخصَّصة (`scripts/merge_duplicate_pair.py`):
**59 ملفاً** قُرئ متنُه ونُقل ما انفرد به إلى الملفّ القانونيِّ ثمّ صار **إحالةً دائمة**
بـ`redirect_to`، و**58 ملفاً قانونياً** استقبل المتنَ المدموج. ونسخةُ كلِّ ملفٍّ قبل الدمج
محفوظةٌ في `content/ar/_merged/`، والمعرِّفاتُ (`id`) لم تُمسّ، وكلُّ إشارةٍ واردةٍ أُعيد
توجيهُها آلياً. والحصيلةُ اليوم: **لا عنوانَ عربياً ولا اسمَ إنجليزياً مكرَّراً بين عقدتين حيّتين.**

وستُّ مجموعاتٍ **لم تُدمج لأنّ الدمجَ فيها إتلاف**: كتابٌ ومفهومٌ يحمل اسمَه (أمبيدكار، كوفي،
واطسون، يالوم)، وعقدتا **علاقةٍ** (`rel-psychoanalysis`, `rel-humanistic`) تصفان علاقةَ
الوجودية بمدرستين لا المدرستين نفسَيهما. مُيِّزت عناوينُها وأسماؤها الإنجليزيةُ بدل ذلك.
وكذلك **«العلاج النفسي الإيجابي»**: تجانسٌ لفظيٌّ بين علاج **بسشكيان** الألمانيِّ (1968)
وبروتوكول **راشد وسليغمان** الأمريكيِّ (2006) — لا صلةَ بينهما، فصار كلُّ عنوانٍ يحمل صاحبَه.

التفصيلُ كلُّه في **`DUPLICATES_CROSS_PREFIX_2026-09-13.md`**.

## 9. مرشِّحُ «المدرسة» في الخطّ الزمني

كان يُشتقّ من أوّل جزءٍ في `crumb` — نصٌّ حرٌّ فيه 1,493 قيمةً، 982 منها تَرِد مرّةً
واحدة، فبلغت القائمةُ **1,044 خياراً**. صار يُشتقّ من سلسلة `belongs_to` صعوداً إلى
جذرها: **372 قيمةً**، والعنصرُ الذي لا ينتهي نسبُه إلى مدرسةٍ يبقى بلا مدرسةٍ مسجَّلةٍ
بدل اختلاق واحدة. مُجرَّبٌ حيّاً: اختيارُ «التحليل النفسي» يعطي **429 عنصراً** من شجرته
كلِّها (بروير، فرويد، بلويلر، أندرياس-سالومي) لا مَن صادف أن يبدأ مسارُه بالاسم.

## 10. بطاقاتُ اقتباسٍ لنصوصٍ ليست اقتباسات  ⬛ لم يكن في التقرير

ظهر أثناء العمل وهو أثقلُ ممّا سُجِّل: `renderQuoteCards` تصبّ أيَّ نصٍّ في قسم
«اقتباسات مختارة» في بطاقةٍ بعلامتَي تنصيصٍ وإسنادٍ وزرَّي «نسخ النص» و«مشاركة كصورة».
و**396 قسماً** منها نثرٌ تحريريّ، أكثرُه جملةُ **«لا يوجد اقتباس مباشر منقول حرفياً»**
نفسُها — فكانت تُعرض **بطاقةَ اقتباسٍ منسوبةً إلى صاحب الملفّ**، أي نقيضَ ما تقوله،
وقابلةً للمشاركة صورةً. الشرطُ الآن أن يبدأ النصُّ بعلامة اقتباسٍ فعلية.
البطاقاتُ في الأطلس: **277 بطاقةً حقيقية** بدل 515 قسماً أكثرُه زائف.
وصارت الاقتباساتُ المفصولةُ بسطرٍ فارغٍ تنقسم بطاقاتٍ بدل أن تندمج في واحدة.

## 11. المصادر

- **301 ملفاً** كان قسمُ «المصادر» فيها يسرد **عناوينَ مداخلَ أخرى من الأطلس نفسِه**
  (93% من بنودها عناوينُ عقدٍ داخلية) — أي موسوعةٌ تستشهد بنفسها. غُيِّر عنوانُ القسم
  إلى **«مداخلُ الأطلس المتّصلة»** ونصُّه التمهيديُّ يقول صراحةً إنها إحالاتٌ داخليةٌ
  لا مصادرُ خارجية، وسُجِّل في `gaps` أنّ الملفَّ ما زال بلا مصادر.
- أُضيف **وسمٌ في ترويسة كلِّ مدخل**: «⚠ بلا مصادرَ موثَّقة» أو «✓ له قسمُ مصادر» —
  فيعرف القارئُ حالَ التوثيق من أوّل نظرةٍ لا بعد النزول إلى آخر الصفحة.
  **الرقمُ الصادق: 4,222 مدخلاً موثَّقاً (63.3%) مقابل 2,452 بلا مصدرٍ واحد.**
- ما تبقّى — كتابةُ مصادرَ لـ2,452 مدخلاً — **عملٌ بحثيٌّ لكلِّ ملفٍّ على حدة، لم أقم به
  ولا يصحُّ أتمتتُه**: توليدُ استشهاداتٍ آلياً هو اختلاقُها. الوسمُ يجعل النقصَ مرئياً
  ومقيساً بدل أن يكون مطموراً.

---

## ما ظهر أثناء التنفيذ وعولج

| | |
|---|---|
| روابطُ متنٍ بصيغة slug هدفُها ملفٌّ تحت اسمٍ آخر | **18 صُوِّبت** بعد التحقّق من الاسم الإنجليزي (`thk-mheidegger`←`thk-heidegger`، `thk-buber`←`thk-mbuber`، `thk-jbowlby`←`thk-bowlby`…) |
| روابطُ متنٍ بمسارٍ مطلق `](/concepts/con-x)` | **9 حُوِّلت** إلى slugs فصارت روابطَ حيّة |
| روابطُ ملفاتٍ مكسورةٌ في `agents_specs/` | 9 مساراتٍ جذريةٍ داخل مجلّدٍ فرعيّ + **37 رابط `file:///Users/minamoheb/…`** |
| `qst-mind-body-interaction-problem` | بادئةٌ شاذّةٌ ومعرِّفُها `QUE-0129` — أُعيدت تسميتُها `que-…` وحُدِّث 9 ملفات |
| `classification-icd-11` و`classification-dsm-5-tr` | كان نوعُهما «اضطراب/حالة إكلينيكية» فيظهران في فهرس الاضطرابات — صارا «نظام تصنيف تشخيصي»، وحُدِّثت إشاراتُهما في 21 ملفاً |
| `con-dream-interpretation-technique` | نوعٌ «تقنية» يتيمٌ في المعجم — وُحِّد على «تقنية/تدخل علاجي» |
| `part: "sociology"` و`classification-` | أُضيفا إلى معجم الفحص بتعليلٍ مكتوب (لا بتغيير الملفات) |
| **10 روابطَ `related`** من ملفاتٍ حيّةٍ إلى ملفاتٍ **محجورة** | حُذفت وسُجِّل سببُها — كانت تقود القارئَ إلى طريقٍ مسدود |
| `data_extras.json` | مدخلٌ محجورٌ (`thk-rcabrera`) كان معروضاً في قائمة الأعلام وفي المخطّط الزمني — أُزيل من الأولى ونُزع رابطُه من الثاني |
| **36 مسودةً لها نظيرٌ حيّ** | وُسمت `superseded_by` وبنداءٍ ظاهرٍ في صدرها: «مسودةٌ مُتجاوَزة — لا تُحرَّر ولا تُبنى» |
| `.claude/launch.json` | كان مرفوضاً من أداة المعاينة (`url` بمسار) ويثبّت مسارَ المستودع — صُحِّحا |
| ملفاتٌ دخيلةٌ متعقَّبة | 3 ملفات `xlsx` لتوزيع غرفٍ، و12 `thinker_batch_*`، و`names_to_check.txt`، و`app.js` (لوحةُ بياناتٍ مجمَّدةٌ عند 2026-08-25 لا صفحةَ تشير إليها) — **رُفعت من التعقّب وبقيت على القرص** وأُضيفت إلى `.gitignore` |
| لا `git hooks` | أُضيف `.githooks/pre-commit` يُشغّل فحصَ السلامة على الملفات المرحَّلة. للتفعيل: `git config core.hooksPath .githooks` (مُفعَّلٌ في هذه النسخة) |
| `NEXT_SESSION.md` | «ابدأ من هنا» عمرُه ثلاثةُ أسابيع وأمرُه الأوّلُ بمسارٍ ميّت — صُدِّر ببيانِ تقادمٍ وجدولِ الوثائق السارية، وصُحِّح الأمر |

---

## ما لم يُنفَّذ — ولماذا

1. **كتابةُ مصادرَ لـ2,452 مدخلاً.** بحثٌ لكلِّ ملفٍّ على حدة؛ أتمتتُه اختلاق.
2. **290 عقدةً بلا أيِّ رابط** و**272 عقدةً متنُها دون 600 حرف**: عملٌ تحريريٌّ لا خللٌ بنيويّ.
3. **`content/en`**: مجمَّدةٌ بقرارٍ معلَنٍ في `content/en/README.md`. لم تُمسّ.
4. **`wrk-two-dogmas` بنوع «عمل / مقالة»**: وحدَه بين 608، لكنّه **وصفٌ صحيح** — «عقيدتان
   من عقائد التجريبية» مقالةٌ لا كتاب. تركُه على صوابه أولى من توحيدِه على خطأ.
