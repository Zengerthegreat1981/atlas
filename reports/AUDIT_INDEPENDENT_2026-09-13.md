# مراجعةٌ مستقلّةٌ للمستودع كلِّه — 2026-09-13

**النطاق:** كلُّ ما في المستودع لا المحتوى وحده: `content/ar` (6,674 ملفاً) و`content/en`
و`scripts/` (209 سكربتاً) و`scripts/template/` (القالب الذي يرسم الواجهة) و`agents_specs/`
ووثائقُ الجذر و`data_extras.json` و3,055 رابطاً خارجياً.

**نقطةُ الانطلاق:** فحوصُ المشروع نفسِه تخرج كلُّها خضراء —
`audit_atlas.py` (19 مجموعة) و`check_content_integrity.py` (6,674 ملفاً) لا تُبلغ عن خللٍ قاطع.
وإعادةُ البناء من الصفر تُنتج `data.json` **مطابقاً بايتاً ببايت** لِما على القرص، فالبناءُ سليمٌ ومُعاد الإنتاج.
كلُّ ما دونه وُجد **خارج** ما تفحصه تلك الفحوص، وأكثرُه معاينٌ في الصفحة الحيّة لا مستنتَجٌ من الملفات.

---

## أ. أعطالٌ يراها القارئُ في الصفحة

### 1) القالبُ لا يحوِّل الماركداون — النجومُ والأقواسُ تظهر حرفيّةً  ⬛ الأثقل
`shell_suffix.html` يحقن نصَّ الأقسام في `<p>` كما هو (`autoLink` تربط الأسماءَ فقط، ولا
مُحوِّلَ ماركداون في أيٍّ من ملفَّي القالب). فالنتيجة على الشاشة:

- **`**عريض**`** تظهر بنجومها في **3,960 عقدة** من 6,674.
- **`[نص](slug)`** تظهر كما كُتبت: **616 رابطاً في 137 ملفاً**، منها **53** هدفُها slug غير موجود أصلاً.
- الشرطة المائلة المائلة `*مائل*` والعلاماتُ الخلفية `` ` `` كذلك.

معاينٌ حيّاً على `#axm-tabula-rasa` و`#con-martyr-complex`.
القرارُ مطلوب: إمّا مُحوِّلُ ماركداون صغير في القالب، وإمّا تنظيفُ المتون من الماركداون.

### 2) سبعةُ ملفات بترويسةٍ مكسورة — `---` زائد
البناءُ يقطع الترويسةَ عند **أوّل** `---`، وهذه الملفاتُ فيها ثلاثة:

| الملفّ | الأثر |
|---|---|
| `thk-amy-morgan`, `thk-deepak-ramsubeik`, `thk-jlubar` | كتلةُ `gaps:` الخامُّ تُطبع في صدر الصفحة، و**الفجواتُ تُفقد كلُّها** (0 فجوة) |
| `thk-rhooton` | نفسُ الطبع، وفجوتان من أربعٍ تصل |
| `syn-emotional-numbing`, `syn-hypervigilance`, `syn-insomnia` | المتنُ يبدأ بسطر `---` ظاهر |

معاينٌ حيّاً على `#thk-amy-morgan`. وفحصُ المشروع «كلُّ بنود gaps يقرأها البناء» لا يرى هذا
لأنه يقارن ما داخل الترويسة، وهذه الفجواتُ خارجَها أصلاً.

### 3) `redirect_to` لا تقرأه الواجهة — 86 صفحةَ إحالةٍ مسدودة
86 عقدةً تحمل `redirect_to` صالحاً إلى الملفّ القانوني، والواجهةُ لا تستعمل الحقلَ إطلاقاً
(لا ذكرَ له في القالب)، فيقف الزائرُ على صفحةٍ عنوانُها «(حجر — انظر …)» بلا تحويلٍ ولا زرّ.

### 4) بقايا توليدٍ داخل المتن: `</content>`
ستةُ ملفاتٍ حيّة فيها وسمٌ خامّ يُحقن في HTML الصفحة:
`axm-tabula-rasa`, `dbt-substance-vs-process-ontology`, `sch-philosophy-of-mind`,
`sch-epistemology`, `sch-aesthetics`, `sch-philosophy-of-religion` (و7 في المسودات).

### 5) مرشِّحُ «المدرسة» في الخطّ الزمني: 1,044 خياراً
الواجهةُ تشتقّ «المدرسة» من **أوّل جزءٍ في `crumb`**. وفي الملفات **1,494 جذراً مختلفاً،
983 منها يَرِد مرّةً واحدة فقط** — فالقائمةُ ليست تصنيفاً بل فهرسٌ ثانٍ للعناصر.
أسوأُها في الأنواع الصغيرة: الاستعارات 130 قيمةً لـ144 عنصراً، الأسئلة 117/129،
المصطلحات 116/126، الخبرات 115/124، السياقات 112/118.
وفيها مترادفاتٌ تنقسم بلا داعٍ («الجسر المعرفي» 62 مقابل «الجسر (فلسفة ↔ علم نفس)» 47،
«الاضطرابات والمتلازمات» 178 مقابل «الاضطرابات والحالات الإكلينيكية» 104).

### 6) جدولٌ مكسور
`axm-holism-gestalt`: أربعةُ صفوفٍ من ستّةٍ فيها خانتان مقابل ثلاثِ خاناتٍ في الترويسة
(«المثال» ناقص) — الجدولُ يُرسم مشوَّهاً.

### 7) اقتباساتٌ تُعرض ككتلةٍ واحدة
`renderQuoteCards` تفصل البطاقاتِ على `<br>`. و**17 ملفاً** قسمُ اقتباساتِها أطولُ من 300 حرفٍ
وبلا `<br>` واحد (`rel-plato-chariot-freud-ego-id`, `thk-diogenes-sinope`,
`crt-lyotard-critique-of-grand-narratives` وغيرها) فتُعرض بطاقةً واحدةً مكدّسة.

---

## ب. بياناتٌ تُكتب ولا تُعرض أبداً

نحو **ثلاثين حقلاً** في الترويسات لا يقرؤها القالبُ إطلاقاً. أبرزُها بالأرقام:

| الحقل | عدد الملفات | ما يضيع |
|---|---|---|
| `author` / `publication_year` / `original_language` | 588–593 | مؤلّفُ العمل وسنةُ نشره |
| `evidence_level` | 554 | درجةُ الدليل |
| `cultural_origin` / `register` | 414 / 378 | الأصلُ الثقافي والسِّجلّ |
| `design` / `main_finding` / `replication_status` / `sample_size` | 121 / 121 / 121 / 112 | كلُّ ما يُهمّ في الدراسة |
| `dsm5tr_code` / `icd11_code` | 110 / 110 | رموزُ التشخيص |
| `items_count` / `reliability` / `validity_note` / `target_population` | 102 | سيكومتريةُ الأداة |
| `event_date` / `event_place` | 149 | تاريخُ الحدث ومكانُه |
| `redirect_to` | 86 | انظر (أ-3) |

هذا عملٌ تحريريٌّ مبذولٌ فعلاً وغيرُ ظاهرٍ لأحد. القرارُ إمّا عرضُه أو إسقاطُه.

---

## ج. تكرارٌ لا يراه فحصُ التكرار

`audit_atlas.py` يجمع المرشَّحين بمفتاح `(البادئة، اللغة، العنوان)` — فلا يقارن **عبر**
البادئات أبداً. وخارجَ حدوده:

- **16 زوجاً بالعنوان العربيّ نفسِه** عبر بادئتين، أوضحُها ازدواجٌ فعليٌّ في المتن:
  `con-family-sandplay` / `br-family-sandplay` (نفسُ العنوان والاسم الإنجليزيّ، وتاريخان
  متضاربان 1990 و1980)، وكذلك `con-abstinence-vs-harm-reduction`/`br-…`،
  `con-critical-liberation-therapy`/`br-…`، `con-intersectional-feminism`/`br-intersectional-feminist`،
  `con-restorative-justice`/`br-…`، `con-queer-theory`/`sch-…`، `con-deliberative-democracy`/`sch-…`،
  `br-fft`/`tec-functional-family-therapy`، `br-mst`/`tec-multisystemic-therapy`، `br-ddp`/`tec-ddp`.
- **43 مجموعةً بالاسم الإنجليزيّ نفسِه** عبر البادئات (بعضُها مشروع: مدرسةٌ وتقنيةٌ باسمها،
  أو عملٌ ومفهومٌ باسمه — لكنّ أزواجَ `br-` مع `con-` تحتاج حسماً).
- **`br-logotherapy`**: الحرفُ `relates_to → thk-langle` مكتوبٌ مرّتين.

(لا يوجد أيُّ ملفَّين متنُهما متطابقٌ حرفياً — فُحص بالبصمة.)

---

## د. فحصٌ أخضرُ كاذب

`audit_atlas.py` يطبع **«✅ كلُّ ملفٍّ فيه قسمُ المصادر»**، والواقعُ **35 ملفاً بلا القسم**.
السببُ أنّ الفحص يبحث عن السلسلة `"## المصادر"` في **كامل نصّ الملفّ**، فتُرضيه جملةٌ داخل
`gaps` تقول حرفياً «لا يوجد قسم `## المصادر`» — الاعترافُ بالنقص هو ما يُخفيه.
(أمثلة: `thk-arascovsky`, `con-narrative-identity`, `tec-cbt-cog-socratic-questioning`.)

**الإصلاح:** `re.search(r'^##\s*المصادر', t, re.M)` بدل `in`.

---

## هـ. المصادر — الرقمُ الحقيقيّ

| | |
|---|---|
| قسمُ مصادرَ بمراجعَ فعلية | 4,224 (63.3%) |
| **قسمُ مصادرَ جملتُه قالبيةٌ بلا مرجعٍ واحد** | **2,413 (36.2%)** |
| بلا قسمٍ أصلاً | 37 |

أربعُ جملٍ قالبيةٍ تتناوب (535 + 515 + 511 + 489 مرّة)، وزيادةً عليها **301 ملفّ** قسمُ مصادرِها
هو «مصادر أساسية مرتبطة بهذا المدخل (وفق حقل `related`)» — أي مصادرُ مولَّدةٌ من البيانات الوصفية
لا من مراجع. فحصُ المشروع يقيسها بحدّ 40 حرفاً فيرى 1,101 فقط؛ الجملُ القالبيةُ أطولُ من ذلك.

---

## و. السكربتات — 209 ملفاً

### 1) ثلاثةٌ لا تُصرَّف أصلاً (SyntaxError)
- `scripts/analyze_phantoms.py:64` — سلسلةٌ غيرُ مغلقة
- `scripts/densify_reciprocal_links.py:9` — اقتباساتٌ متداخلةٌ في regex
- `scripts/generate_all_expansion_batches.py:34` — شرطةٌ خلفيةٌ داخل f-string (ممنوعةٌ في 3.9؛ المحلّيّ 3.9.6)

### 2) 76 سكربتاً تثبّت مساراً غيرَ موجود  ⬛ الأخطر عملياً
`<جذر المستودع>` — لا وجود له على هذا الجهاز (`/Users/mina/…`). والانقسام:

- **33 سكربتاً ينهار** بخطأٍ صريح — مزعجٌ لكنّه أمين.
- **43 سكربتاً يُخفق بصمت**: يمسح مجلداً غيرَ موجودٍ فيعود بصفر نتائج **ويطبعها كأنها نتيجة**.
  مُجرَّب: `python3 scripts/check_all_queries.py` يقول إنّ `seligman` و`piaget` و`milgram`
  **غيرُ موجودين** — وملفاتُهم الثلاثةُ في `content/ar/thinkers/` فعلاً. أيُّ قرارٍ بُني على
  مخرجات واحدٍ من هذه الـ43 (إنشاءُ ملفٍّ موجود، حذفُ «يتيم» غير يتيم) هو قرارٌ على معطًى كاذب.

كلُّها تُصلَح بسطرٍ واحد: `ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))`
— وهو ما يفعله `build_atlas.py` و`audit_atlas.py` و`reapply_thinkers_audit.py` بالفعل.

### 3) ستةُ سكربتاتٍ مذكورةٍ في `ACTION_PLAN_2026-08-24.md` وغيرُ موجودة
`connection_density_report.py`, `bidirectional_related_audit.py`, `check_active_end_dead.py`,
`coverage_matrix.py`, `check_empty_related.py`, `network_centrality.py`.

---

## ز. الروابط

### 1) خارجية — فُحص كلُّ رابطٍ في المتون

> **تصحيحٌ لاحقٌ (بعد التنفيذ):** ما كُتب هنا أوّلاً من أنّ «42 رابطاً مبتورٌ بقوسٍ
> غير مغلق» كان **خطأً في أداة الاستخراج لا في الملفات**: الروابطُ في الملفّ سليمةٌ
> بأقواسها، والأداةُ هي التي قطعتها عند `)` — وكذلك قطعت صيغةَ `<url>` عند `>`.
> أُعيد الاستخراجُ بموازنةِ الأقواس وقراءةِ الصيغتين، وأُعيد الفحص. الأرقامُ أدناه هي
> الصحيحة، وتفصيلُ ما عُمل في `FIXES_2026-09-13.md`.

**3,082 رابطاً فريداً في المتون.** بعد إعادة الفحص:

- **60 رابطاً ميتاً** له لقطةُ أرشيفٍ متحقَّقةٌ في Wayback — استُبدل بها (58 ملفاً).
- **57 رابطاً ميتاً بلا بديلٍ يمكن التحقّقُ منه** — حُذف وسُجِّل نصُّه في `gaps`.
  ومنها **سبعةُ مداخلَ في `plato.stanford.edu` لا وجودَ لها في الموسوعة أصلاً**
  (`entries/thales/`, `entries/condorcet/`, `entries/cynicism/`, `entries/dussel/`,
  `entries/extended-mind/`, `entries/gestalt-psychology/`, `entries/islamic-theology/`)
  — أي دعاوى مصدرٍ لا مصادر.
- **45 بلاغاً كاذباً** من أداة الاستخراج (أقواسٌ و`<>`).
- **25 رابطاً تعذّر التحقّقُ منه من هنا**: حجبُ روبوتاتٍ (واشنطن بوست، Poetry
  Foundation، genome.gov…) لا موتٌ مثبت — تُركت كما هي.
- **476 رابطاً بحالة `403`** استُبعدت من العدّ: حجبُ روبوتاتٍ لا كسرُ رابط.

**واكتُشف أثناء الإصلاح:** **24 رابطاً** مكتوبةً بصيغة `<https://…>` كان المتصفّحُ
يبتلعها وسماً مجهولاً **فتختفي من الصفحة كلَّها** — عطلٌ أشدُّ من الرابط الميّت.

القائمةُ الكاملةُ في `scripts/audit_2026-09-13_dead_links.md` — كلُّ رابطٍ مع ملفاتِه وما آل إليه.


### 2) داخلية
- **617 رابط ماركداون في المتن هدفُه slug** — انظر (أ-1)؛ **53** منها هدفُه غيرُ موجود
  (`thk-mheidegger`, `con-polyvagal`, `thk-nwiener`, `thk-jbowlby` …).
- **35 رابطاً مكسوراً في وثائق `agents_specs/`**: 9 كُتبت بمسارٍ من جذر المستودع داخل مجلّدٍ
  فرعيّ (تُكسر عند العرض)، و**37 رابط `file:///Users/minamoheb/…`** لمسار مستخدمٍ غيرِ موجود.
- **6 ملفاتٍ من المسودات المذكورة في `agents_specs/historical-dsm-scope-list.md` محذوفة.**

### 3) ترقيمٌ مكسور
**44 ملفاً فيه `«` و`»` غيرُ متوازنة** — نمطٌ متكرّر: `**«عنوان (English, 1995)**` بلا `»`
(`thk-sherry-turkle` 14/10، `thk-tom-regan` 20/16، `thk-zmoreno` 34/31، `thk-mary-wollstonecraft` 38/36 …).
(فحصُ الأقواس العادية `()` استُبعد: أغلبُه ترقيمُ قوائم «1)» «ج)».)

---

## ح. المسودات والنظافة

- **36 مسودةً لها نظيرٌ حيٌّ منشور** (`thk-zeno-citium`، و21 مدرسةً في `drafts/minimax/schools`…)
  — أي أنّ الملفَّ رُقِّي ونسختُه القديمةُ بقيت.
- **19 slug مكرّراً بين مسارَي المسودات** spark و minimax (`thk-pargament`, `thk-okasha`, `thk-vico` …).
- **290 عقدةً بلا أيِّ رابط** (لا `related` ولا `edges`) — تظهر في الفهارس ولا تقود إلى شيءٍ ولا يقود إليها شيء.
- **272 عقدةً متنُها أقلُّ من 600 حرف** (الوسيط 1,862).
- `thk-mbalint`: قسمُ «القيد» محتواه «توفي 1970، عمر 74» — لا قيدَ فيه.
- انحرافا نوع: `con-dream-interpretation-technique` نوعُه «تقنية» وهو في `concepts/`،
  و`wrk-two-dogmas` نوعُه «عمل / مقالة» وحدَه بين 608.
- `part: "sociology"` في ثلاثة ملفاتٍ فقط بينما `SOCIOLOGY_PART_PLAN.md` (48 ك.ب) يخطّط لقسمٍ كامل.
  (الحقلُ لا تقرؤه الواجهةُ أصلاً، فالأثرُ على الأدوات لا على القارئ.)

## ط. المستودعُ وغيرُ المحتوى

- **ملفاتٌ دخيلةٌ متعقَّبةٌ في git**: ثلاثةُ ملفات `xlsx` لتوزيع غرفٍ واشتراكات (لا صلةَ لها بالأطلس)،
  و12 ملفَّ `thinker_batch_*` مؤقتاً، و`names_to_check.txt`.
- **`app.js`**: لوحةُ بياناتٍ مجمَّدةٌ عند 2026-08-25 (تقول 6,618 ملفاً، والواقع 6,674؛
  وتقول «ملفات فيها قسم المصادر: 1») **ولا صفحةَ HTML في المستودع تشير إليها** —
  `atlas_dashboard.py` يكتب إلى `~/Desktop/atlas-dashboard.html` خارجَ المستودع.
- **`NEXT_SESSION.md`** يقول «اقرأ هذا أولاً» وهو من 2026-08-25 (قبل ثلاثِ جولاتِ مراجعة)،
  وأوّلُ أمرٍ فيه `cd "$(git rev-parse --show-toplevel)"` — مسارٌ لا يعمل. و27 وثيقةَ خطةٍ وتقريرٍ
  في الجذر بلا فهرسٍ يقول أيُّها ساري.
- **`.claude/launch.json`**: حقلُ `url` فيه مسارٌ (`http://localhost:8778/index.html`) فترفضه
  أداةُ المعاينة؛ الصحيحُ `http://localhost:8778` والتنقّلُ بعد الفتح.
- **لا `git hooks`** — كلُّ الفحوص يدويةٌ بالكامل، فلا شيءَ يمنع دخولَ خللٍ في دفعةٍ قادمة.
- **`content/en`**: 130 ملفاً كلُّها `[EN TRANSLATION NEEDED]` — **حالةٌ مجمّدةٌ بقرارٍ معلَنٍ
  وموثَّقٍ في `content/en/README.md`**، لا إهمال. لا إجراءَ مطلوباً.

---

## ي. ترتيبُ العمل المقترح

| # | العمل | الكلفة | الأثر |
|---|---|---|---|
| 1 | إصلاحُ 76 مساراً مثبَّتاً في السكربتات (سطرٌ واحدٌ لكلٍّ) | دقائق | يوقف 43 إخفاقاً صامتاً |
| 2 | إغلاقُ 42 قوساً في الروابط المبتورة | دقائق | 42 رابطاً يعود |
| 3 | إصلاحُ ترويسات الملفات السبعة | دقائق | يزيل أوضحَ عطلٍ بصريّ |
| 4 | `re.search` بدل `in` في فحص المصادر | دقيقة | يُنهي خضرةً كاذبة |
| 5 | حذفُ `</content>` ×6 وإصلاحُ جدول `axm-holism-gestalt` والحرفِ المكرَّر | دقائق | — |
| 6 | **قرار:** مُحوِّلُ ماركداون في القالب أم تنظيفُ المتون | ساعات | يمسّ 3,960 عقدة |
| 7 | **قرار:** الحقولُ الثلاثون — تُعرض أم تُسقَط | ساعات | يمسّ عملاً تحريرياً كاملاً |
| 8 | حسمُ 16 زوجاً مكرَّراً عبر البادئات، وتوسيعُ فحص التكرار ليعبر البادئات | ساعات | — |
| 9 | تجميعُ 1,494 جذرَ `crumb` في تصنيفٍ معدود | أيام | يُحيي مرشِّح المدرسة |
| 10 | 170 رابطاً خارجياً ميتاً (128 بعد إصلاح الأقواس) | أيام | — |
| 11 | 2,413 قسمَ مصادرَ قالبياً | مشروعٌ قائمٌ بذاته | — |

**ملاحظةُ بناء:** أُعيد بناءُ `data.json` و`index.html` أثناء المراجعة للتحقّق؛ الناتجُ مطابقٌ
تماماً لِما كان، والملفّان مستثنيان في `.gitignore` فلا أثرَ لهما على الالتزام.
ولم يُعدَّل أيُّ ملفِّ محتوًى أو سكربت — هذا التقريرُ وحدَه هو التغيير.
