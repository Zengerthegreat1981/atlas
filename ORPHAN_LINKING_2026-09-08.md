# ربطُ العقد المعزولة — تقرير

**التاريخ:** 2026-09-08 · **الأطلس:** 6,649 عقدة

## النتيجة

| | قبل | بعد |
|---|---|---|
| عقدٌ بلا رابطٍ وارد | **1,089** | **125** |
| منها مهجورةٌ بقصد (محجورة/إحالة) | 18 | 18 |
| **معزولةٌ فعلاً** | **1,071** | **107** |
| نسبةُ العقد التي يُشار إليها | 83.62٪ | **98.12٪** |
| بنودُ `related` في الأطلس | 25,690 | 26,688 |
| وسيطُ طول `related` · p90 | 3 · 7 | **3 · 7** (لم يتغيّر) |

**أُضيف 998 رابطاً، ولم يزدد وسيطُ الطول ولا p90** — أي أنّ الروابط وُزِّعت على
655 ملفاً ولم تتكدّس. وبغير التوزيع كانت `sch-psychoanalysis` وحدها ستأخذ 109
روابط فتصير جدارَ شرائح.

## أوّلاً: التحقّق من أنّ الخلل حقيقي

قبل أيِّ تعديل فحصتُ الواجهة (`scripts/template/shell_suffix.html`)، لأنّ الدعوى
كلَّها تسقط لو كانت الواجهةُ تحسب الروابطَ الواردةَ وقتَ العرض:

- التنقّلُ يُبنى من `related` و`edges` **الصادرة** فقط (شرائحُ الروابط في سطر
  1030، و`readNextFor`، و«العناصر المجاورة»).
- ثمّة دالّةٌ تحسب الواردَ — `relCountFor` — لكنها تُستعمل في **شارةِ عددٍ**
  (سطر 955) لا في تنقّل. ولا يوجد في الواجهة أيُّ ربطٍ عكسيٍّ آخر.

**فالنتيجة: العقدةُ المعزولةُ مكتوبةٌ ولا يبلغها القارئُ إلا ببحثٍ مباشرٍ باسمها.**
وكان ذلك حالَ سُدسِ الأطلس، ومنه 377 مفكِّراً.

## ثانياً: القاعدة — لا يُخترَع رابط

كلُّ رابطٍ أُضيف هو **عكسُ علاقةٍ يؤكّدها المستودعُ أصلاً**، بأحد خمسة تسويغات:

| التسويغ | العدد | معناه |
|---|---|---|
| `belongs_to` | 382 | أبٌ هرميٌّ يُدرِج منتسبَه (مدرسةٌ تُدرِج تيّاراً) |
| تبادل | 358 | المعزولةُ تُشير إليه، فجُعلت العلاقةُ ذاتَ اتجاهين |
| تأليف | 130 | `author_slug` أو حرفُ تأليفٍ في `edges` |
| حرفٌ آخر في `edges` | 33 | علاقةٌ مُصرَّحٌ بها من نوعٍ آخر |
| عَلَمٌ في الـslug | 25 | الـslug والعنوانُ كلاهما يسمّيان الشخص |
| **خريطةٌ يدويةٌ محقَّقة** | **36** | حكمٌ تحريريٌّ مني، مقصورٌ على العلاقات البيّنة |

وهذا الاحترازُ مقصود: الأطلسُ فيه خللُ **إقحام الروابط** عُولج مرّتين من قبل
(`remove_spurious_related_links.py` و`remove_filler_authenticity_links.py`)،
فزيادةُ ألفِ رابطٍ بلا تسويغٍ كانت ستُعيد الخللَ نفسَه بحجم أكبر.

**والمهجورُ لم يُربَط:** 18 ملفاً عنوانُها «حجر — انظر …» أو «إحالة» تُركت معزولةً
عن قصد — هي مهجورةٌ تُشير إلى بديلها المعتمد، وربطُها يسوق القارئَ إلى طريقٍ مسدود.

## ثالثاً: خللان أكبرُ اكتُشفا في الطريق

### 1) مساراتُ تنقّلٍ خاطئة: 132 ملفاً نُسبت إلى «المدرسة الوجودية»

`crumb` هو المسارُ الظاهرُ للقارئ فوق كلِّ صفحة. ووجدتُ **629 ملفاً** يبدأ مسارُها
بـ«المدرسة الوجودية»، ومنها **253 لا يذكر متنُها الوجوديةَ إطلاقاً**. أي أنّ
القارئ كان يقرأ:

> الأطلس ← **المدرسة الوجودية** ← أدوات القياس ← مقياس بيك للاكتئاب
> الأطلس ← **المدرسة الوجودية** ← الدراسات والأبحاث ← دراسة بروكا للمريض «تان»

ودراسةُ بروكا في توطين وظيفة الكلام سنة 1861 لا صلةَ لها بالوجودية، ولا مقياسُ
هاملتون، ولا فحصُ دنفر للفرز التطوري. **وهي معلومةٌ خاطئةٌ تُعرَض للقارئ**، لا
مجرَّدَ نقصٍ — فهي أخطرُ من مسألة العزلة نفسِها.

صُحِّح **132 ملفاً** من نوعَي «أداة قياس» و«دراسة وبحث»، لأنّ نسبةَ أداةِ قياسٍ
أو تجربةٍ معمليةٍ إلى مدرسةٍ فلسفيةٍ خطأٌ قاطعٌ لا يحتاج حكماً. **ولم تُلمس
المفكِّرون ولا الأعمال** (121 ملفاً) — فيهم مَن انتسابُه الوجوديُّ صحيح
(`wrk-being-nothingness` لسارتر، `thk-wong`)، والتمييزُ فيهم يحتاج مراجعةً بشرية.
و**21 ملفاً أُوقِف** لأنّ متنَه أو روابطَه وجودية — ومنها ما هو وجوديٌّ بحقّ
(`ins-death-attitude-profile`، `stu-terror-management-theory`).

**وجُرِّب تصنيفٌ موضوعيٌّ للمجال ثم أُسقِط:** أسند «دراسة تريبليت للتيسير
الاجتماعي» إلى «علم النفس العصبي»، و«النمذجة الاجتماعية» إلى «علم النفس النمائي».
فاستُعملت قيمٌ **صادقةٌ دائماً** ولو كانت أعمّ — «القياس النفسي والتشخيص» لأدوات
القياس، و«علم النفس» للدراسات — وسُجِّل في `gaps` أنّ المجالَ الأدقَّ لم يُحدَّد.
**أعمُّ وصادقٌ خيرٌ من أخصَّ وخاطئ.**

### 2) حشوٌ في الروابط: 96 ملفاً تُشير إلى «الأصالة» بلا سبب

نفسُ الدفعة القالبية حشَت `con-authenticity` في `related` لملء الحقل الفارغ.
فكان القارئُ يجد في «تجربة آش للامتثال» و«مقياس وكسلر للذاكرة» و«منحنى إبنغهاوس
للنسيان» رابطاً وحيداً اسمه **«الأصالة»**. ومن 153 ملفاً تحمل الرابط، **96 لا
يُذكَر فيها المفهومُ في المتن ولو مرّة** — و27 منها كان الرابطَ الوحيد فيها.

حُذفت الـ96، وأُبقيت الـ57 التي يسوّغها متنُها. والمعيارُ هو نفسُه الذي استُعمل
في دفعة `br-ml-personalized-therapy` من قبل.

ثم رُبطت الملفاتُ التي أُفرِغت بخريطةٍ يدويةٍ محقَّقة — مقياسُ وكسلر للذاكرة إلى
عائلة وكسلر، وستروب وربطُ المسارات إلى بطارية D-KEFS التي تضمُّهما، وبيريندا
وكوستانزو إلى تجربة آش.

## رابعاً: خللٌ في أدواتي، صُحِّح قبل التنفيذ

**قاعدةُ «العَلَم في الـslug» كانت ستُحدث أخطاءً فاحشةً لو نُفِّذت بصياغتها الأولى:**

| كانت ستُسند | إلى | السبب |
|---|---|---|
| أطروحات **لوثر** 1517 | **مارتن لوثر كينغ** | مطابقةُ luther |
| **الأليثيا** (الحقيقة عند هايدغر) | **سوجورنر تروث** | مطابقةُ truth |
| «خذ فاقرأ» عند **أوغسطين** | **إيكهارت تول** | مطابقةُ tolle |
| الآبائيةُ **اليونانية** | **كريستيان فولف** | مطابقةُ christian |
| فلسفةُ **اللغة** العادية بأوكسفورد | **بهارتريهاري** | وصفٌ بين قوسين في `en` |
| إدانات **باريس** 1277 | **جينيت باريس** | اسمُ مدينةٍ يُقرأ لقباً |
| سقوطُ جدار **برلين** | **فريد برلين** | اسمُ مدينةٍ يُقرأ لقباً |
| **أبو البركات** البغدادي | **حليم بركات** | تشابهٌ في العربية |

فأُضيفت خمسةُ شروطٍ لازمةٍ مجتمعةً: اللقبُ آخِرُ كلمةٍ في `en` بعد إسقاط ما بين
قوسين؛ ومطابقتُه وحيدةٌ في الأطلس؛ **وتأكيدٌ عبر الخطَّين** (ظهورُ الاسم العربي
في العنوان العربي أيضاً)؛ واللقبُ أوّلُ كلمةٍ في الـslug؛ وألّا تكون المعزولةُ
مفكِّراً. فهبطت القاعدةُ من 122 رابطاً إلى **25 رابطاً راجعتُها واحداً واحداً**.

**وخللٌ آخر:** التوزيعُ بالحمولة وحدَها كان يُسند «ميشينباوم» إلى زميلٍ مغمور
و«أصل الأنواع» إلى «معيار يوروبسي 2001» — تسويغٌ صحيحٌ ومنفعةٌ رديئة. فصار
اختيارُ المستضيف في رتبة التبادل **بالمركزية** لا بقلّة الحمولة، فانتقل
ميشينباوم إلى **بيك**، وراضو إلى **فرويد**.

**وبندٌ واحدٌ كسره البناءُ بصمت:** عنوانٌ يحمل `\"` داخله يقطع نمطَ القراءة
فيُهمَل البندُ كلُّه. رصده `check_content_integrity.py` فوراً، وأُضيف تطهيرٌ
للعنوان في السكريبت يمنع تكرارَه.

## خامساً: ما بقي معزولاً — 107 عقدة

**كلُّها `related` و`edges` فارغان معاً** — أي ملفاتٌ أُنشئت بلا أيِّ بيانات
علائقية. ولا يبلغها أيُّ تسويغٍ آليٍّ، ولم أُلفِّق لها روابط.

| النوع | العدد |
|---|---|
| مفكر | 72 |
| حدث تاريخي | 25 |
| أنواعٌ أخرى | 10 |

- **المفكِّرون (72):** وسيطُ متونهم منخفض، و39 من الـ107 أقلُّ من مئة كلمة — أي
  بذورٌ لم تُكتب، وفيها ما هو مُعلَّقٌ لعدم التحقّق من هوية صاحبه. وأثقلُ ما فيها
  غياباً ما متنُه معتبَرٌ ولم يُربَط بعد.
- **الأحداثُ (25):** متونُها معتبرة (200–500 كلمة) — مجمعُ نيقية، تدميرُ مكتبة
  الإسكندرية، ثورةُ التعلّم العميق 2012 — ومع ذلك بلا رابطٍ واحد. رُبط منها 11 حدثاً
  بالخريطة اليدوية، وبقي الباقي.

### أثقلُ ما بقي (الأعلى عددَ كلماتٍ أولى بالربط)

| الملفّ | كلمات |
|---|---|
| `thk-patricia-churchland` — باتريشيا تشيرشلاند | 445 |
| `thk-idmarshall` — توماس هانا | 373 |
| `thk-larrynims` — لاري نيمز | 346 |
| `thk-michael-yapko` — مايكل يابكو | 270 |
| `thk-bala-jaison` — بالا جايسون | 229 |
| `thk-jwpark` — جي-وو بارك | 218 |
| `thk-peta-stapleton` — بيتا ستاپلتون | 193 |
| `thk-icheolhong` — تشو هي-سو | 176 |
| `evt-destruction-of-alexandria-library` — حرق وتدمير مكتبة الإسكندرية القديمة | 519 |
| `evt-founding-of-university-of-paris-1150` — تأسيس جامعة باريس (السوربون) وازدهار السكولا | 335 |
| `evt-universal-declaration-human-rights-1948` — صدور الإعلان العالمي لحقوق الإنسان في باريس  | 326 |
| `evt-ai-breakthrough-deep-learning-2012` — ثورة التعلم العميق والذكاء الاصطناعي 2012 وم | 320 |
| `evt-fourth-world-conference-women-beijing-1995` — مؤتمر بيجين للمرأة 1995 وترسيخ حقوق النساء ك | 318 |
| `evt-launch-of-internet-www-1991` — إطلاق شبكة الويب العالمية 1991 وفلسفة الفضاء | 308 |
| `evt-frege-russell-paradox-letter-1902` — رسالة راسل إلى فريجه حول مفارقة المجموعات (1 | 287 |

## سادساً: سكريبتات أُضيفت

| السكريبت | ما يفعله |
|---|---|
| `deorphan_links.py` | الربطُ الآليُّ بالتسويغ الخمسيّ، بسقفٍ وتوزيعٍ ومركزية |
| `link_curated_orphans.py` | خريطةٌ يدويةٌ محقَّقة لما لا تسويغَ آليَّ له (36 عقدة) |
| `fix_wrong_existential_crumbs.py` | تصحيحُ مسار التنقّل الخاطئ (نطاقٌ مضيَّقٌ عن قصد) |
| `remove_filler_authenticity_links.py` | حذفُ حشو «الأصالة» بمعيار المتن |

وكلُّها بوضع فحصٍ افتراضيٍّ و`--apply`، ويحمل كلٌّ منها في رأسه شرحَ الخلل وسببَ
كلِّ احترازٍ فيه — وبخاصّةٍ الأخطاءُ التي وقعت أثناء التطوير، كي لا تُعاد.

## سابعاً: ما لا يقوله هذا التقرير

- **الربطُ لا يُصحِّح الوجهة.** ضمنتُ أنّ كلَّ عقدةٍ يُشار إليها، لا أنّ الإشارةَ
  هي الأنسب. ولكلِّ معزولةٍ رابطٌ **واحدٌ** غالباً — وهو حدُّ الوصول لا حدُّ الجودة.
- **الـ376 ملفاً الباقية بمسار «المدرسة الوجودية»** لم تُفحَص واحداً واحداً؛ منها
  الصحيحُ ومنها الخاطئ، والتمييزُ يحتاج مراجعةً بشرية. **فالخللُ لم يُستوفَ.**
- **خريطةُ الـ36 حكمٌ مني** لا مُشتقّةٌ من المستودع، وإن اقتُصرت على البيّن.
- **لم أُصلح `related` الصادرَ للمعزولات** — «أصل الأنواع» ما يزال يُشير إلى
  «معيار يوروبسي 2001» و«مدرسة كيوتو»، وهما إشارتان لا معنى لهما. صار الملفُّ
  مبلوغاً، وروابطُه الصادرةُ ما تزال رديئة. وهذا بابٌ ثالثٌ لم أفتحه.
