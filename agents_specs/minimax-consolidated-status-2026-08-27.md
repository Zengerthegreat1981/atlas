# MiniMax — الحالة الحقيقية الموحّدة (آخر تحديث: 2026-08-31 لايف من task.py status + state.json)

هذا الملف بديل عن كل الرسايل المتفرقة اللي اتبعتت لعشر شاتات مختلفة. مصدر الحقيقة الوحيد
من هنا فصاعداً. النسخة دي محدّثة بفحص فعلي لـ`task.py status minimax` + قراءة مباشرة لـ
`agents_specs/reports/minimax/state.json` (claimed) + عدّ تقارير `reports/minimax/T*/`.

## ⚠️ فخّ خطير لازم تعرفه قبل أي حاجة تانية

**`task.py status` بيقول "0 دفعة" لـTask 3 وTask 13-17 رغم إن شغل حقيقي خلص فيهم فعلاً.**
السبب: Task 3 اتعمل بسويب آلي شامل (`preflight_check.py` على كل الـ1006 ملف) مش عن طريق
`task.py get` — فمحدّش سجّل claim. Tasks 13-17 (مرحلة الإنشاء) بردو بتتكتب في `drafts/minimax/`
من غير `get` أصلاً (مفيش قايمة ملفات تتاخد). **لا تعتمد على `task.py status` وحده — دايماً
راجع `agents_specs/reports/minimax/INDEX.md` كمان قبل ما تقرر إن تاسك "لسه ماتلمسش".**

## الحالة الحقيقية لكل تاسك

| Task | المجلد | الحجم الكلي | المُنجَز | الحالة |
|---|---|---|---|---|
| 1 | thinkers m-z (تراث) | ~1013 (مسبح مشترك 1-4، claimed=1013) | جزء من 1013 | جزء من مسبح 1-4 |
| 2 | thinkers m-z (مشكوك) | (نفس المسبح) | جزء من 1013 | جزء من مسبح 1-4 |
| 3 | thinkers m-z (تدقيق قرائي) | 1006 | **1006** | ✅ **مُنهَك فعلياً** (سويب كامل، صفر مخالفات — مش عبر task.py) |
| 4 | التعميق والمصادر | (نفس مسبح 1-4) | 900/1006 فيه `## المصادر` فعلياً | ✅ **مكتمل فعلياً (تحقّق 2026-09-01)** — الفجوة كانت وهمية: التعميق حصل كأثر جانبي لـTask 1+2، والباقي (~106) يطابق حجم `quarantine-minimax.md` (88 اسم محجور لا يستحق مصادر أصلاً). راجع `T4/closeout.md` |
| 5 | schools+branches+relations (belongs_to) | 730 (schools=370، branches=234، relations=126 مجموع claimed) | 730 | ✅ **مُنهَك بالكامل تقريباً** |
| 6 | branches (تعميق) | (نفس مسبح 5) | — | ✅ مُنهَك |
| 7 | relations (تعميق) | (نفس مسبح 5) | — | ✅ مُنهَك |
| 8 | schools (مصادر+cultural_origin) | (نفس مسبح 5) | 370/370 | ✅ **مكتمل فعلياً (تحقّق 2026-09-01)** — `cultural_origin` في 370/370، `## المصادر` في 369/370 (الاستثناء الوحيد إحالة متعمَّدة `sch-cbt.md`). راجع `T8/8.1`–`8.10` + `T8/closeout.md` |
| 9 | concepts (إعادة ربط) | 908 (مسبح مشترك 9-10) | **420** (كان 300 في آخر تحديث — تقدّم فعلي) | 🔴 **488 ملف باقي** |
| 10 | concepts (كسر قالب) | (نفس مسبح 9) | — | 🔴 نفس الـ488 الباقيين |
| 11 | debates+critiques | 276 | **0** (claimed=0، تقارير=0) | 🔴 **لسه ما اتلمسش خالص، مؤكد** |
| 12 | dialogues+questions+terms+axioms | 354 | **0** (claimed=0، تقارير=0) | 🔴 **لسه ما اتلمسش خالص، مؤكد** |
| 13-17 | مرحلة الإنشاء (drafts/minimax/) | **مكتمل بالكامل** | ✅✅✅✅ | 🟢 **آخر تحديث 2026-09-02 (الجلسة الثالثة — إغلاق نهائي شامل)**: كل التاسكات 9-17 مكتملة. **كل القضايا الأربع المتروكة من الإغلاق السابق حُسمت بالكامل هذه الجلسة**: (1) فجوة Task 13 العددية محلولة (149+9 مدرسة، تجاوز الهدف ~147)، (2) قائمة orphan-scan منخفضة الأولوية (159 حالة) محلولة بالكامل عبر 6 دفعات + إصلاحات شخصية، (3) خلل تصادم الترقيم حُسم بنياً (نطاقات أرقام محجوزة مسبقاً لكل دفعة، مُختبَر بنجاح على 29 ملف مفكر بصفر تصادم)، (4) كل طلبات slug المعلَّقة (29 مفكراً) أُنشئت. راجع `agents_specs/reports/minimax/ISSUE-RESOLUTION-2026-09-02.md` و`MINIMAX-FINAL-COMPLETION-2026-09-02.md` للتفصيل الكامل. **القيد الوحيد المتبقي المُوثَّق (لم يُطلب حسمه): 22 تعارض slug عبر مساري Spark/MiniMax يحتاج تنسيقاً بين المسارين.** |

## قرارات اتخدت بالفعل (مش محتاجة تكرار)
- 40 ملف bug جنس نحوي — **اتصلحوا بالكامل**.
- ~965 مخالفة عبر 269 ملف من سويب Task 3 — **اتصلحوا بالكامل**، التحقق النهائي: 1006/1006 صفر مخالفات.
- `thk-jgantt` — اتحجر نهائي (الشخص غير موجود)، الـslug بقى فاضي لـSpark (Susan P. Gantt).
- `thk-rames`، `thk-tfalola` — اتنشأوا بسير حقيقية (طلبات R-002/R-006/R-007).
- 41 ملف `.bak` طارئ — اتنضّفوا.

## من أين تبدأ (الترتيب الصحيح)

1. **تحقق أولاً من Task 4 وTask 8** — كل واحد فيهم مسجّل رسمياً بملف/اتنين بس، لكن فيه فخ
   موثّق إن ملفات فعلية اتعدّلت من غيره تسجيل (زي Task 3). قارن الملفات المُعدَّلة فعلياً في
   `content/ar/` (git diff / تاريخ التعديل) بقايمة الـclaimed، واقفل أي فجوة تسجيل قبل ما
   تكمل.
2. **Task 9+10 — الـ488 ملف الباقيين من concepts/** (908 - 420 claimed) — أكبر شغل حقيقي متبقي
   في المشروع كله.
3. ✅ **Task 11 — debates+critiques كامل (276 ملف) — مكتمل بالكامل بتاريخ 2026-09-01** (10 دفعات
   11.1-11.10، راجع `agents_specs/reports/minimax/T11/` وINDEX.md). أكثر من 25 استشهاد ملفَّق
   مؤكَّد اكتُشف وصُحح عبر الدفعات — نمط منهجي موصى بتدقيق منفصل لرئيس التحرير.
4. ✅ **Task 12 — dialogues/questions/terms/axioms كامل (354 ملف) — مكتمل بالكامل بتاريخ 2026-09-01**
   (12 دفعة 12.1-12.12، راجع `agents_specs/reports/minimax/T12/` وINDEX.md). عدة دمجات فعلية
   مطبَّقة (قاعدة 6)، اكتشاف نمط نسبة ترجمات عربية مختلَقة مرتين (Sein und Zeit). ← **Tasks 13-17 التالي**.
5. **كمّل مرحلة الإنشاء 13-17** — راجع `agents_specs/reports/minimax/T13/` لحد `T17/` الأول
   عشان متكررش شغل موجود (خصوصاً Task 13: 127 مدرسة لسه باقية من أصل 147، وTask 15: عشرات
   الفلاسفة لسه باقيين).

## ⚠️ فخاخ لازم تتجنبها
- **متعملش `task.py get` "للتجربة"** — بيحجز ملفات فعلياً حتى بحجم 1. لو حصل بالغلط، رجّعه
  يدوياً من `state.json` (احذف من `claimed`، رجّع `next_sub`/`since_checkpoint`) قبل ما تكمل.
- **الوقفة كل 5 دفعات مشتركة على مستوى المسار كله** — `since_checkpoint` الحالي = 1، يعني
  فاضلك 4 دفعات قبل ما `get` يرفض. لو ظهرتلك "قف واستنى مراجعة"، شغّل
  `python3 scripts/task.py checkpoint minimax` بنفسك وكمّل.
- **`preflight_check.py` بقى فيه فحص قائمة سوداء حرفي دلوقتي** (كان ناقص) — أي دفعة قديمة
  اتعملت قبل اليوم يستاهل إعادة فحص لو لسه ما دقّقتهاش.
- **أي شخص جديد تكتبه في Tasks 13-17 لازم يتحقق وجوده فعلياً ببحث خارجي** قبل سيرة واثقة —
  ده أخطر نمط ظهر في التدقيق الأصلي.

---

## 📋 النص اللي تبدأ بيه الشات الوحيد بتاع MiniMax (انسخه كامل)

```
إنت الشات الوحيد المسؤول عن مسار MiniMax كامل في مشروع الأطلس (/Users/minamoheb/Desktop/Atlas).

أول حاجة: شغّل نفسك على /loop (self-paced، من غير فترة محددة) من دلوقتي — عشان تفضل شغالة
مستمرة في الخلفية من غير ما تستنى تدخّل مني بعد كل دفعة. استخدم ScheduleWakeup / dynamic /loop
عشان تكمّل الدفعة اللي بعدها لوحدك، وما توقفش إلا لما (أ) تخلص كل الـtasks المتاحة، (ب) توصل
لحد الـ5 دفعات وتحتاج checkpoint (شغّله بنفسك وكمّل — ماتستناش مني)، أو (ج) تقابل تعارض حقيقي
أو قرار لا رجعة فيه على مستوى المشروع كله محتاج قرار مني. غير كده، كمّل لوحدك من غير ما تسألني.

اقرا agents_specs/MINIMAX.md كامل، وبعدين اقرا
agents_specs/minimax-consolidated-status-2026-08-27.md كامل — ده مصدر الحقيقة الحالي (محدّث
لايف من task.py status + state.json بتاريخ 2026-08-31)، فيه حالة كل تاسك فعلياً (مش المفروض
من task.py status وحده — فيه فخ موضّح في الملف عن Task 3 وTasks 4/8/13-17 اللي بتظهر أرقام
غير دقيقة رغم شغل حقيقي خلص أو بدأ فيهم).

ابدأ بالترتيب ده بالظبط:
1. تحقق من Task 4 وTask 8 (فخّ تسجيل موثّق — قارن الملفات المعدَّلة فعلياً بالـclaimed، اقفل
   أي فجوة تسجيل بسرعة)
2. Task 9+10 — 488 ملف باقيين من concepts/ (أكبر شغل متبقي، 908 الكل - 420 claimed)
3. Task 11 — debates+critiques كامل (276 ملف، claimed=0 مؤكد لايف، متلمسش خالص)
4. Task 12 — dialogues/questions/terms/axioms كامل (354 ملف، claimed=0 مؤكد لايف، متلمسش خالص)
5. كمّل مرحلة الإنشاء 13-17 — راجع agents_specs/reports/minimax/T13/ لحد T17/ الأول عشان
   متكررش شغل موجود فعلاً (65 ملف اتكتبوا، والتارجت الأصلي أكبر بكتير خصوصاً Task 13 وTask 15)

المنهج لكل دفعة: 10 subagents متوازيين → preflight_check.py فوري → verify → report → INDEX.
since_checkpoint الحالي = 1 من 5 — فاضلك 4 دفعات قبل ما get يرفض. لو وصلت لحد الـ5 دفعات، شغّل
checkpoint بنفسك وكمّل تلقائياً بالـ/loop من غير ما تستنى رد مني. لا تسألني إلا لو فيه تعارض
حقيقي أو قرار لا رجعة فيه على مستوى المشروع كله.
```
