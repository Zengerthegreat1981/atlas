# خطة الترقية الشاملة والنشر الحي للأطلس (Production Promotion & Release Plan)

**التاريخ:** 22 أغسطس 2026  
**الهدف:** نقل وتسكين جميع المسودات المنجزة (**1,473 مسودة**) من مجلد `content/ar/drafts/` إلى المجلدات المعتمدة `content/ar/`، وتعيين الأرقام التعريفية المعيارية (`ID: PREF-####`)، وفحص شبكة العلاقات، وبناء النسخة الحية للموقع ليرتفع الأطلس من 3,998 إلى **5,500+ عنصراً حياً متصلاً**.

---

## المرحلة 1: الفحص والتدقيق القبلي للمسودات (Pre-Promotion Integrity Audit)
- [ ] **1.1 تدقيق البنية الحقلية للـ Frontmatter:** التحقق من وجود كافة الحقول الإلزامية (`slug`, `type`, `part`, `level`, `title`, `en`, `crumb`, `active_start`, `active_end`, `edges`, `related`, `gaps`).
- [ ] **1.2 تدقيق صرامة الأسطر الفردية:** التأكد من كتابة عناصر `related` و`edges` بصيغة السطر الفردي المفصول بفواصل لمنع تعطل المحلل البرمجي (Parser).
- [ ] **1.3 تدقيق أهداف الانتماء المدارسي (`belongs_to`):** مطابقة أسماء ومعرفات المدارس المستهدفة في حقل `edges` مع المدارس المسجلة فعلياً في `content/ar/schools/`.
- [ ] **1.4 تدقيق خلو المسودات من الروابط الوهمية (Zero Phantom Slugs):** التأكد من أن كل معرف مذكور في `related` يطابق عنصراً حقيقياً في الفهرس الشامل.

---

## المرحلة 2: توليد وتسكين المعرفات المعيارية (Sequential ID Generation)
- [ ] **2.1 حصر أعلى الأرقام التعريفية في المجلدات المعتمدة لكل نوع:**
  - `thinkers` (`THK-####` تبدأ من `THK-2221`)
  - `concepts` (`CON-####` تبدأ من `CON-2167`)
  - `works` (`WRK-####` تبدأ من `WRK-0496`)
  - `branches` (`BRN-####` تبدأ من `BRN-0229`)
  - `debates` (`DBT-####` تبدأ من `DBT-0353`)
  - `relations` (`REL-####` تبدأ من `REL-0154`)
  - `axioms` (`AXM-####` تبدأ من `AXM-0004`)
  - `critiques` (`CRT-####` تبدأ من `CRT-0023`)
  - `dialogues` (`DIA-####` تبدأ من `DIA-0004`)
  - `events` (`EVT-####` تبدأ من `EVT-0073`)
- [ ] **2.2 استبدال وسم `[DRAFT-UNKNOWN]` بالمعرف الدائم:** تشغيل سكريبت ترقيم آمن يحدث حقل `id` في كل ملف مسودة برقم تسلسلي فريد دون المساس بأي حقول أخرى.

---

## المرحلة 3: النقل الهيكلي والترقية المادية (Physical Promotion & Migration)
- [ ] **3.1 إنشاء سكريبت النقل والدمج الآمن:** نقل الملفات من `content/ar/drafts/<folder>/<slug>.md` إلى `content/ar/<folder>/<slug>.md`.
- [ ] **3.2 معالجة التعارضات والتطابق:** التأكد من عدم وجود تضارب أسماء ملفات والاحتفاظ بالنسخة الأكثر اكتمالاً وتفصيلاً.
- [ ] **3.3 تنظيف المسارات المؤقتة:** إفراغ مجلدات المسودات مع الإبقاء على مجلد `drafts/` جاهزاً لأي إضافات مستقبلية.

---

## المرحلة 4: إعادة تدقيق الشبكة وحل الأيتام (Graph Cross-Linking & Zero Orphan Audit)
- [ ] **4.1 فحص العقد المعزولة (Orphan Nodes):** التأكد من أن كل عنصر من الـ 5,500+ عنصر يرتبط بمدرسته عبر `belongs_to` ويمتلك رابطين ذوي صلة على الأقل في `related`.
- [ ] **4.2 تحديث الفهرس الشامل:** تشغيل `scripts/build_slug_index.py` لإعادة بناء `EXISTING_SLUGS.md` والتأكد من تسجيل 5,500+ عنصراً معتمداً بصفر تعارض.
- [ ] **4.3 تحديث مصفوفات التغطية العامة والفلسفية:** تشغيل `scripts/build_philosophy_matrix.py` و`scripts/build_coverage_matrix.py`.

---

## المرحلة 5: البناء النهائي والتحقق من الإصدار الحي (Production Build & Live Verification)
- [ ] **5.1 بناء قاعدة البيانات الحية والواجهة:** تشغيل `python3 scripts/build_atlas.py ar` لتوليد `data.json` و`index.html` المحدثين بـ 5,500+ عنصراً مدمجاً بالكامل.
- [ ] **5.2 فحص سلامة الواجهة البرمجية وحجم الحزمة:** التأكد من سلامة ملف `index.html` وخلوه من أخطاء الـ JSON أو الـ JS Syntax.
- [ ] **5.3 توثيق محضر الترقية والإصدار:** تسجيل تقرير الترقية في `agents_specs/pipeline-progress-log.md` وتحديث التوثيق الإداري.
