# تقرير إنجاز المرحلة صفر — 2026-08-25

جميع خطوات §3 (0.1 → 0.12) في `ATLAS_EXECUTION_PLAN.md` مكتملة ومُتحقَّقة.
كل خطوة لها لقطة git منفصلة (12 commit)، وكل واحدة اتفحصت بـ`atlas_parse.py --scan`
و`atlas_health.py` قبل وبعدها.

## الأرقام: قبل المرحلة صفر ← بعدها

| المؤشر | قبل | بعد | الفرق |
|---|---|---|---|
| إجمالي الملفات | 6,608 (10 فشل تحليل) | 6,618 | +10 (كلهم بقوا قابلين للقراءة) |
| ملفات صالحة كـ YAML (محلّل الأطلس) | 0 | **6,618 (100%)** | ✅ |
| فشل تحليل تام | 10 | 0 | ✅ |
| الروابط المطبوعة آلياً | 1,267 | **4** | −1,263 |
| روابط الحشو | 1,613 | **15** | −1,598 |
| أقسام سقّالة ظاهرة | 531 | 61 (كلها معمارية موثَّقة، مش سقّالة) | −470 |
| `belongs_to` → slug | 2,718 | 4,025 | +1,307 |
| `belongs_to` → اسم عربي حر | 2,809 | 1,513 | −1,296 |
| مدارس غائبة بلا مطابقة تامة | 309 | 237 | −72 |
| ملفات < 3 روابط `related` | 1,259 | 3,142 | +1,883 (متوقَّع — الحشو كان بيخفي الرقم الحقيقي) |
| حجم `sch-psychoanalysis` (frontmatter) | 205 كيلوبايت | 15 كيلوبايت | — |
| حجم `index.html` الكامل | ~25 ميغا | 15.2 ميغا | — |

## ما اتعمل بالضبط

1. **0.1** إيقاف النماذج + لقطة `e57b5ad` + نسخة zip خارجية.
2. **0.2** `atlas_parse.py` — محلّل/مُصدِّر موحّد. اتأكد بـround-trip 6,561/6,575.
3. **0.3** `atlas_health.py` — كل مؤشرات §1/§8 بأمر واحد، `--save`/`--diff`.
4. **0.4** `task.py` — دورة get/verify/report/checkpoint، بوابة وقوف كل 5 دفعات.
5. **0.5** 32 ملف بعطل ميكانيكي (اقتباسات ناقصة، مفاتيح ملتصقة/مكررة، عنوان مفقود).
   كشف تلوّث أعمق في 7 ملفات critiques (مفتاح `gaps:` مكرر) — **موثَّق لا مُصلَح**،
   قرار تحريري لصاحب Task 11.
6. **0.6** حذف 8,089 رابطاً مطبوعاً/حشو من 2,529 ملفاً.
7. **0.7** حصاد 501 قسم سقّالة إلى `pending-links-bank.md` ثم حذفها. الـ30 ملف
   «ملاحظة معمارية» (قرارات دمج حقيقية، بعضها معلَّق) **لم تُلمس** — موثَّقة في
   `phase0-architectural-notes-flagged.md` لصاحب `thinkers/`.
8. **0.8** تحويل 1,305 رابط انتماء من اسم عربي حر لـslug مطابق تماماً (250 اسماً
   فريداً). الباقي (496 اسماً) في `missing-schools-registry.md`.
9. **0.9** توحيد `country` (362 ملفاً، 3 مرادفات لا لبس فيها فقط) · مطابقة H1
   مع `title` (852 ملفاً) · `## قيد`→`## القيد` (1,035 ملفاً). **لم يُلمس عمداً:**
   بادئات `axm-`/`qst-` (كسر روابط)، وصيغة «أهم أعماله/الأعمال» (قرار تحريري لكل ملف).
10. **0.10** تقليم الملفات الثلاثة المتضخمة لـ≤25 `related`/≤10 `gaps`. العضوية
    الحقيقية (472/384/378 عضواً) محفوظة كاملة في `mega-schools-full-roster.md`.
11. **0.11** إنشاء `content/ar/drafts/spark/` و`drafts/minimax/` فعلياً على القرص.
12. **0.12** الداشبورد — مُحدَّث ومُعاد توليده.

## قرارات موثَّقة بلا تنفيذ (تحتاج DR أو قرار تحريري لاحق)

- بادئات `axm-`/`qst-` غير موحَّدة — تحتاج DR + سكربت تحديث مراجع.
- 7 ملفات critiques فيها التباس بين `related` و`gaps` (مفتاح مكرر) — Task 11.
- `CLS-`/`NOD-` في حقل `id:` الداخلي — غير مؤثر وظيفياً، موثَّق فقط.
- 496 اسم مدرسة حر لسه بلا ملف — `missing-schools-registry.md`، Task 13 (MiniMax).

## الخطوة التالية

المرحلة 1 (المفكرون، ~2,295 ملفاً) جاهزة للبدء على المسارين — `task.py get spark 1`
و`task.py get minimax 1`.
