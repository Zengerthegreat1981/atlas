# Spark — الحالة الحقيقية الموحّدة (آخر تحديث: **2026-09-02، بعد مراجعة شاملة مستقلة**)

> ## ⛔ تصحيح جوهري (2026-09-02)
> خضع هذا الملف لمراجعة مستقلة أعادت قياس كل رقم من القرص. **ادّعاء «Tasks 1–17 كلها مُنهَكة
> بالكامل» المكتوب أدناه غير صحيح** وتُرك كما هو للسجل التاريخي فقط.
> **الحالة الفعلية: 2,829 مخالفة آلية باقية في 1,199 ملفاً معتمداً داخل أرض Spark، وTask 4 وTask 6
> لم تُصدَر لهما دفعة واحدة قط.**
> **اقرأ `agents_specs/reports/spark/REVISION/revision-2026-09-02.md` قبل أي شغل جديد.**
> ملخص ما صحّ وما لم يصح:
> - ✅ صحيح: التصحيحات الواقعية على المعتمد (17/17 فُحصت ومطبَّقة)، الـ12 فصل هوية، دمج drafts
>   (صفر فقد محتوى)، تعارضات slug = 22، وTasks 7/8/11/13(ناقص ملف واحد)/14/15/16/17.
> - ❌ غير صحيح: Tasks 1–4 و6 و9 و10 و12 «مُنهَكة». أبرز عيب: **954 ملفاً في `thinkers/` a–l
>   تحمل جملة القائمة السوداء** المرفوضة صراحةً (نطاق MiniMax m–z: صفر)، و`## المصادر` عند **4%**
>   في thinkers a–l مقابل **89%** عند MiniMax.
> - ⚠️ خطر: **صفر commit منذ `73d92c96`** (7,364 ملفاً معدَّلاً)، والمسودات المحذوفة كانت غير
>   متتبَّعة في git فلا يمكن استرجاعها.

هذا الملف بديل عن كل الرسايل المتفرقة. مصدر الحقيقة الوحيد من هنا فصاعداً.

## ⚠️ [مُبطَل — انظر التصحيح أعلاه] Tasks 1-17 كلها مُنهَكة بالكامل

| Task | الحالة |
|---|---|
| 1-11 | ✅ مُنهَك بالكامل |
| 12 | ✅ مُنهَك بالكامل (386/386) |
| 13 | ✅ مُنهَك بالكامل (دمج drafts/thinkers القديم + صفر فجوة متبقية) |
| 14 | ✅ مُنهَك بالكامل (11 ملف جديد: 6 دراسات + 5 أدوات) |
| 15 | ✅ مُنهَك بالكامل (7 ملفات جديدة: تحليل وظيفي + 6 اضطرابات) |
| 16 | ✅ مُنهَك بالكامل (16/16 دواء، كان شبه مكتمل، أُكِّد فقط) |
| 17 | ✅ مُنهَك بالكامل (2 ملف جديد: مهلوسات + صحة نفسية رقمية) |

**لا يوجد حالياً أي Task من القائمة الأصلية في SPARK.md له عمل معروف متبقٍ.**

## ✅ قرار مسار drafts — نُفِّذ بالكامل

`content/ar/drafts/thinkers/` (56 ملف قديم) دُمج بالكامل (نقل مباشر، صفر تعارض) في
`content/ar/drafts/spark/thinkers/` (المسار الرسمي الوحيد الآن). المسار القديم أُزيل نهائياً.
تفاصيل: `agents_specs/reports/spark/T13/task-13-drafts-merge.md`.

## ملخص أعمال 2026-09-01 (هذه الجلسة)

1. **دمج drafts**: 56 ملف منقول، صفر دمج محتوى مطلوب فعلياً (كل الأسماء كانت أشخاصاً جدداً
   حقيقيين لا تكراراً). تقرير: `T13/task-13-drafts-merge.md`.
2. **Task 13**: تحليل فجوة كامل بعد الدمج → صفر فجوة. كل اسم في قائمة SPARK.md له ملف حقيقي.
   تقرير: `T13/task-13-gap-analysis-final.md`.
3. **Task 16**: تأكيد سريع → 16/16 دواء مطابق، preflight نظيف. تقرير: `T16/task-16-final-confirmation.md`.
4. **Task 17**: فجوة (مهلوسات + صحة نفسية رقمية) حُدِّدت وأُغلقت في نفس الدفعة.
   تقارير: `T17/task-17-gap-analysis.md` + `T17/task-17.2-gap-closure.md`.
5. **Task 14**: فجوة (Many Labs 1/3/4 + فشل استنزاف الأنا + فشل وضعية القوة + بارك الجرذان + PSS +
   استبيان ACE + WHODAS 2.0 + CORE-OM + RAADS-R = 11 ملف) حُدِّدت وأُغلقت.
   تقارير: `T14/task-14-gap-analysis.md` + `T14/task-14.2-gap-closure.md`.
6. **Task 15**: فجوة (تحليل وظيفي ABA + 6 اضطرابات) حُدِّدت وأُغلقت — **مع اكتشاف وتصحيح فوري**:
   بند "مكوّنات CBT-I" كان مكتوباً بالفعل في المسار المعتمد (`content/ar/techniques/tec-cbt-insomnia.md`)
   ولم يظهر في الفحص الأولي لأنه بحث في drafts/spark فقط لا في المعتمد؛ حُذف الملف المكرر فور
   اكتشافه عبر `build_slug_index.py`. تقارير: `T15/task-15-gap-analysis.md` + `T15/task-15.2-gap-closure.md`.

## ⚠️ درس مكتسب لأي عمل مستقبلي على Tasks 13-17

عند فحص "هل هذا البند مكتوب بالفعل؟" **لازم البحث في `content/ar/` المعتمد كاملاً، مش بس
`content/ar/drafts/spark/`** — ظهرت حالة واحدة فعلية (CBT-I) كانت مكتوبة معتمدة ولم تظهر في فحص
drafts فقط. الطريقة الموثوقة: `find content/ar -iname "<slug>.md"` بلا استثناء مسار، ثم فحص
`build_slug_index.py` فوراً بعد كل كتابة جديدة (يكشف التكرار مع المعتمد تلقائياً حتى لو فات الفحص
اليدوي).

## ⚙️ ملاحظة عن "الخلفية" — حدود حقيقية

مفيش `git remote` لفولدر Atlas حالياً، يعني `/schedule` (agent سحابي) **مش متاح فعلياً**. القرار
المتخد: العمل يستمر بـ`/loop` self-paced محلي — **لازم الـwindow يفضل مفتوح على الجهاز** طول مدة
الشغل. لو اتقفل، الـloop بيوقف تمامًا ومحتاج فتحه يدوياً تاني.

## من أين يبدأ أي شات جديد الآن

**[مُبطَل 2026-09-02]** ~~كل الـTasks المعروفة في SPARK.md (1-17) مغلقة. لا يوجد عمل فوري إلزامي
متبقٍ على مستوى الملف.~~

**الحقيقة بعد المراجعة:** الأولويات مرتَّبة بالمردود في §11 من
`agents_specs/reports/spark/REVISION/revision-2026-09-02.md`. أولها: (1) commit للحالة الحالية،
(2) سويب القائمة السوداء على 954 ملفاً في `thinkers/` a–l بقالب التخصيص المُثبَت (§9.c) لا بالحذف،
(3) 237 تضارب `title`/`id`، (4) إطلاق Task 4 وTask 6 فعلياً، (5) إكمال Task 5 وcontexts.
الخطوات المحتملة التالية (تقديرية، تحتاج توجيهاً بشرياً صريحاً قبل البدء لأنها ليست جزءاً من قائمة
SPARK.md الأصلية):
- تدقيق قرائي (Task 3 pattern) على الملفات الـ~26 المكتوبة حديثاً اليوم (Tasks 13-17) قبل ترقيتها
  من مسودة لمعتمد — لم تخضع بعد لمراجعة بشرية فعلية، فقط لفحص preflight الآلي وبناء فهرس الـslugs.
- مراجعة التكرارات الثنائية المكتشَفة عرضاً أثناء هذه الجلسة بين `content/ar/thinkers/` المعتمد
  و`content/ar/drafts/spark/thinkers/` لأسماء مثل `thk-amy-cuddy` و`thk-jaustin` (نفس الـslug في
  المسارين) — لم تُعالَج لأنها خارج نطاق التاسك المطلوب في هذه الجلسة، لكنها تستحق فحصاً منفصلاً.
