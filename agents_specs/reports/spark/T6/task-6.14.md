# Task 6.14 — تعميق 30 ملف works/

## الأرقام

| مقياس | قبل الدفعة | بعد الدفعة |
|---|---|---|
| works/ بمصادر | 388/609 (64%) | **418/609 (69%)** |
| الوسيط (متن بعد الفرونتماتر) | 3,516 حرف | 2,691 حرف* |
| صفر انتهاكات preflight | ✅ | ✅ (30/30) |
| فوق سقف 6,000 حرف | — | 0 |
| `## المصادر` موجود | — | 30/30 |
| تعارضات slug (الفهرس) | 22 | 22 (بلا تغيير) |
| إجمالي الكوميتات | 83 | **84** |

*الوسيط انخفض عن الدفعات السابقة لأن أغلب ملفات هذه الدفعة كانت قصيرة جداً قبل التعميق (729–3,710 حرفاً)، فحتى بعد التعميق بقيت أقرب لحد الإيجاز الفلسفي/الأدبي من ملفات علم نفس أطول.

## أبرز ما حدث هذه الدفعة

### تصحيح بنيوي حقيقي: `wrk-wandlungen-symbole-libido`
الملف لم يحمل `author`/`author_slug` إطلاقاً. تحقّق الوكيل أن الكتاب هو *تحوّلات ورموز الليبيدو* ليونغ (1912)، وتحقّق من وجود `content/ar/thinkers/thk-jung.md` فعلياً قبل الإضافة، ثم أضاف `author: "كارل غوستاف يونغ"` و`author_slug: "thk-jung"` و`edges: authored_by`. هذا تصحيح لحقل فارغ لا تجاوز على حقل موجود، فهو داخل نطاق التفويض.

### فجوتان كاذبتان أُزيلتا (اختراع سابق، لا تحقق فعلي)
- **`wrk-embodied-mind`**: الفجوة الموروثة زعمت أن فاريلا وروش وطومسون (مؤلفو الكتاب الثلاثة) لا يملكون ملفات مفكر (`thk-`) بعد. تحقّق الوكيل: الثلاثة موجودون فعلاً. حُذفت الفجوة.
- **`wrk-nervosen-charakter`**: فجوة موروثة زعمت عدم وجود `thk-adler` رغم أن `author_slug` في نفس الملف يشير إليه. الوكيل المكلَّف بالشِّق (`aa_g`) سجّل التناقض دون حسمه؛ **أنا تحقّقت مباشرة من وجود `content/ar/thinkers/thk-adler.md` على القرص وحذفت السطر الكاذب بنفسي** (تعديل مباشر، لا عبر وكيل فرعي) — هذا هو التدخل اليدوي الوحيد في هذه الدفعة.

### تصحيح ترجمة/نقحرة فعلي: `wrk-gongsunlongzi`
النص الصيني الكلاسيكي 白馬論 (مقالة "الحصان الأبيض") كان مترجَماً خطأً كـ"النقطة البيضاء"، و堅白 (الصلابة والبياض) كان مخلوطاً بكلمة صينية أخرى متجانسة النطق لا علاقة لها بالمعنى. صُحِّحا. كما أُزيلت فجوة "لا يوجد ملف بعد لـ Gongsun Long" الكاذبة إذ `thk-gongsun-long.md` موجود الآن.

### تصحيحات `active_end` (القاعدة §10: سنة النشر، لا سنة الاستقبال)
- `wrk-gilbert-compassionate-mind`: كانت `"مستمر"` (ممنوعة أصلاً بالقاعدة) → صُحِّحت إلى 2009.
- `wrk-horney-inner-conflicts`: كانت 2010 (سنة استقبال لاحق) → صُحِّحت إلى 1945 (سنة النشر الفعلية).

### تلفيق مُزال: `wrk-horney-inner-conflicts`
الوكيل المكلَّف اكتشف ادّعاءً غير مسنَد بوجود "نظرية صراع حديثة لإبراهيم وبرنر 2010s" ومعيارية زعومة على MMPI-2 داخل مسوَّدة سابقة — لم يتحقّق منه فحذفه بدل تمريره. كذلك خفَّف تفصيل "الصورة المثالية للذات" الذي ينتمي فعلياً لكتاب هورني الآخر (*العصاب والنمو الإنساني*، 1950) لا لهذا الكتاب، فأُبقي كعنصر أولي فقط.

### مسألة معلَّقة، سُجِّلت لا حُسمت (خارج النطاق): `wrk-question-concerning-technology`
`author_slug` يشير إلى `thk-heidegger-technology` — وهو ملف "شخصية مشقوقة" منفصل عن `thk-heidegger` الحقيقي، سبق تسجيله في بند 6 من قائمة قرارات المستخدم المعلَّقة. الوكيل كُتب له صراحة عدم إصلاح الـ slug بنفسه؛ اكتفى بتسجيل الملاحظة في `gaps` وكتابة المتن استناداً إلى `thk-heidegger` الحقيقي مع `related`/`edges: authored_by` صحيحة.

### الترجمات العربية — تحقّق فعلي عبر بحث ويب لثلاثة كتب
- *Ecce Homo* (نيتشه): لا ترجمة عربية منشورة مثبَتة (الفرع الثاني).
- *السؤال حول التقنية* (هايدغر): توجد ترجمة موثَّقة (سبيلا ومفتاح، المركز الثقافي العربي، 1995) لكنها لمختارات أوسع تضمّ هذه المحاضرة لا لها منفردة — ذُكرت بهذا التحفّظ.
- *صراعاتنا الداخلية* (هورني): ترجمة متداولة تُنسب لمترجمة لكن الناشر/السنة غير مؤكَّدين — الفرع الرابع (لا تُسمَّى كاملة).

## ما نجح بسبب الامتناع لا الإضافة
- ملف *سِفر الأمثال* (`wrk-book-of-proverbs`): تُرك سؤال المؤلف/التأريخ مفتوحاً وممنهجاً نقدياً بدل حسمه اعتباطاً — نص تراثي بلا مؤلف مفرد متفق عليه.
- لا شِق من الشِّق العاشر (`aa_j`) نسب كتاباً لغير مؤلفه رغم ثلاثة كتب فلسفية معقدة (نيتشه، هايدغر، هورني) — صفر أخطاء نسبة في هذه الدفعة، والثالثة على التوالي بعد تفعيل قاعدة التحقق من `author_slug` قبل الكتابة في 6.13.

## معلَّق للمحرِّر (لا شيء جديد يتجاوز قائمة الـ13 القائمة)
- بند 6 (هايدغر المشقوق) يتأكد مجدداً بحالة استخدام جديدة.
- الازدواج `wrk-fasl-al-maqal-averroes` كُتب بنسخة مستقلة (غير منسوخة عن الطرف الآخر) طبقاً للتوجيه القائم؛ لم يُدمَج.

## تسليم الشِّق
جميع الشِّقات العشرة (`aa_a` .. `aa_j`) عادت نظيفة عند preflight، صفر مخالفات على 30 ملفاً.
