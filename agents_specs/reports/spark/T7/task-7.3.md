# Task 7.3
الحالة: مكتمل
العملية: studies: إضافة study_year/design/sample_size/main_finding/replication_status + سد فجوات المتن | الملفات: 30

## الأرقام
study_year موجود: قبل 0 → بعد 30/30
preflight_check: 30/30 → صفر مخالفات (كل subagent شغّل preflight_check على دفعته بنفسه وصحّح فوراً — تحسين في العملية عن الدفعات السابقة)

## أمر التحقق
python3 scripts/task.py verify spark 7.3 → study_year 30/30، قائمة سوداء 0/30

## قرارات اتخذتها
- تعميم العملية: كل subagent شغّل preflight_check.py على ملفاته الخاصة وصحّح المخالفات مباشرة قبل التسليم، بدل تجميع الإصلاحات في نهاية الدفعة — قلّل الوقت والتكرار.
- sample_size تُرك فارغاً + gap محدد في ~12 ملفاً حيث الدراسة سلسلة تجارب/مراجعة نظرية بلا رقم عيّنة موحّد (ميلر، ليلي، ميننج-ميك، بياجيه×3، بارك، إلخ).
- replication_status "contested" لـ: لانجر-كوبي مشين، ليوين، ليبت، لوتش-روس، ميلتزوف-مور (فشل تكرار واسع لاحقاً)، بيترسون-بيترسون (تفسير Decay قُلب لـInterference)، بياجيه-الحفظ، بياجيه-الجبال الثلاثة، ميلر، ميشيل-مارشميلو.
- "untested": لانجر-رودين (لا تكرار مباشر لنتيجة الوفيات لأسباب أخلاقية)، بارك.

## متوقف عنده (لرئيس التحرير)
- لا طلبات جديدة.
- نمط القائمة السوداء استمر في عدد أقل من الملفات هذه الدفعة (تحسّن تدريجي، لكن ما زال يستحق فحصاً شاملاً منفصلاً لبقية الأطلس).

## الملفات
30 ملفاً في content/ar/studies/ (stu-langer-mindlessness-copy-machine → stu-ringelmann-social-loafing، ترتيب أبجدي)
