# Task 7.2
الحالة: مكتمل
العملية: studies: إضافة study_year/design/sample_size/main_finding/replication_status + سد فجوات المتن | الملفات: 30

## الأرقام
study_year موجود: قبل 0 → بعد 30/30
preflight_check: 30/30 → صفر مخالفات (بعد إصلاح YAML مكسور في ملف واحد، حذف قائمة سوداء من 8 ملفات، تصحيح 5 تعارضات title/id، رابط edges حر واحد، و9 مشاكل سنة/active_end)

## أمر التحقق
python3 scripts/task.py verify spark 7.2 → study_year 30/30، قائمة سوداء 0/30

## قرارات اتخذتها
- replication_status صُنّف بدقة: "contested" لهوثورن، إكمان (التفسير لا النتيجة)، غازانيغا، هيرتو-سيليجمان، هوفلينغ، جونز-هاريس-كاسترو (الحجم لا الاتجاه)، كولبرغ، كلاوس-كينل، كوهلر، آيينغار-ليبر (حجم الأثر يقترب من الصفر في التحليل التلوي)، داتون-آرون (تحيز اختيار ذاتي).
- sample_size تُرك فارغاً + gap محدد في ~10 ملفات حيث لا رقم موحّد موثوق (بدل الاختراع).
- إصلاح حرج: wrk-dutton-aron-suspension-bridge.md كان بفرونتماتر YAML مكسور تماماً (سطر `edges:` متبوع بـ`[]` في سطر منفصل مُزاح، ما كسر باقي التحليل) — تم إصلاحه لصيغة `edges: []` في سطر واحد، واكتُشف لاحقاً أن تعديل سابق حذف الفاصل الختامي `---` سهواً فأُعيد.

## متوقف عنده (لرئيس التحرير)
- لا طلبات جديدة (Task 7 لا يتعامل مع thinkers).
- التنبيه العام مستمر: القائمة السوداء لا تزال موجودة بكثافة في ملفات لم تُفحص بعد — 8 ملفات إضافية هذه الدفعة.

## الملفات
30 ملفاً في content/ar/studies/ (stu-dutton-aron-suspension-bridge → stu-kohler-insight-chimpanzees، ترتيب أبجدي)
