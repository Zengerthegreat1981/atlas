# Task 16.1
الحالة: مكتمل جزئياً
العملية: النوع الجديد `drg-` (أدوية) — تصميم schema أول ملف من هذا النوع في الأطلس + كتابة 5 مداخل (Lithium + 4 subagent متوازي) | الملفات: 5

## الأرقام
مسودات الأطلس الكلية: 194 → 208 (+14 صافي — دفعات من تاسكات تانية اتضافت في نفس النافذة)
نوع عقدة جديد كلياً: `drg-` (0 → 5 مداخل)

## أمر التحقق
`python3 scripts/preflight_check.py` على كل الـ5 ملفات → صفر مخالفات آلية (بعد تصحيح تعارض id/title واحد في related لـdrg-lithium أثناء الكتابة الأولى، وتصحيح مشابه في drg-methadone صُحِّح ذاتياً من الـsubagent)
`python3 scripts/build_slug_index.py` → 6824 عنصر (6616 معتمد + 208 مسودة)

## قرارات اتخذتها
- بما إن `drg-` نوع جديد كلياً بلا سابقة في الأطلس، صممت الـfrontmatter بنفسي أولاً في `drg-lithium.md` (type: "دواء"، بالإضافة لحقول Task 16 المطلوبة بالضبط: `discovery_year`, `drug_class`, `mechanism`, `main_indications`, `key_adverse_effects`, `evidence_level`) وبنية متن ثابتة (## الاكتشاف والتاريخ / ## الآلية والاستطباب / ## القيد والمخاطر / ## المصادر)، ثم استخدمته كقالب حرفي لباقي الدفعة عبر subagents متوازية.
- غطّت الدفعة: الليثيوم، الكلوزابين، فئة SSRI كاملة (لا دواء واحد — قرار متعمد لتغطية الفئة الأشمل)، الميثادون، فئة البنزوديازيبينات كاملة.
- ربطت الملفات بمتلازمات الأدوية الموجودة بالفعل بلا مؤلف موصوف (syn-neuroleptic-malignant، syn-ssri-discontinuation، syn-serotonin) — بالضبط السبب اللي Task 16 بيذكره كفجوة أصلاً.
- تجنبت اختراع thk- لمكتشفي الأدوية غير الموجودين (جون كيد، ليو ستيرنباخ) وسجلتهم في gaps لكل ملف.

## متوقف عنده (لرئيس التحرير)
- باقي 20-25 مدخل مطلوب من Task 16 لسه غير مكتوب: الهالوبيريدول، ثلاثية الحلقات، مثبطات MAO، الميثيلفينيديت، النالتريكسون، البوبرينورفين، الإسكيتامين، TMS، تحفيز العصب الحائر، البلاسيبو والنوسيبو، تعدد الأدوية، الإيقاف التدريجي — محتاجة دفعات إضافية بنفس القالب الجاهز الآن.
- الآن بعد ما schema النوع الجديد اتصمم واتثبّت بـ5 أمثلة ناجحة، الدفعات التالية أسرع لأنها بس تحتاج تتبع القالب بدل تصميم بنية جديدة.

## الملفات
content/ar/drafts/spark/drg/drg-lithium.md
content/ar/drafts/spark/drg/drg-clozapine.md
content/ar/drafts/spark/drg/drg-ssri-class.md
content/ar/drafts/spark/drg/drg-methadone.md
content/ar/drafts/spark/drg/drg-benzodiazepines.md
