# Task 8.2
الحالة: مكتمل
العملية: instruments: إضافة items_count/target_population/reliability/validity_note/evidence_level + سد فجوات المتن | الملفات: 30

## الأرقام
evidence_level موجود: قبل 0 → بعد 30/30
preflight_check: 30/30 → صفر مخالفات (كل subagent شغّل preflight_check بنفسه وصحّح فوراً)

## أمر التحقق
python3 scripts/task.py verify spark 8.2 → evidence_level 30/30، قائمة سوداء 0/30

## قرارات اتخذتها
- ins-ham-a.md وins-hamilton-hama.md: ملفان مستقلان لنفس المقياس (هاملتون للقلق) — عُومِلا بشكل مستقل بنفس المحتوى الحقيقي، الدمج خارج نطاق Task 8.
- تقنين عربي موثّق فعلياً وجُد فقط لعدد قليل من الأدوات (EPQ عبر أحمد عبد الخالق، MoCA نسخة رسمية معتمدة) — أغلبية الأدوات المفحوصة بلا تقنين عربي موحّد موثّق، وثِّق ذلك صراحة بدل الاختراع.
- ins-neo-pi-r.md: طلب slug جديد لكوستا وماكري (مصمّمي المقياس) سُجّل في requests-spark.md.

## متوقف عنده (لرئيس التحرير)
- طلب slug جديد: بول كوستا وروبرت ماكري (مصمّما NEO-PI-R).
- نمط متكرر أُصلح تلقائياً في preflight عبر كل الدفعة: روابط related بعناوين غير مطابقة، وجمل قائمة سوداء متبقية — يستمر الاكتشاف التدريجي عبر دفعات مختلفة، ما يرجّح فحصاً شاملاً منفصلاً يستحق الأولوية.

## الملفات
30 ملفاً في content/ar/instruments/ (ins-eq-empathy-quotient → ins-oci-r، ترتيب أبجدي)
