# Task 8.1
الحالة: مكتمل
العملية: instruments: إضافة items_count/target_population/reliability/validity_note/evidence_level + سد فجوات المتن | الملفات: 30

## الأرقام
evidence_level موجود: قبل 0 → بعد 30/30
preflight_check: 30/30 → صفر مخالفات (بعد حذف قائمة سوداء من 6 ملفات، تصحيح 4 تعارضات title/id)

## أمر التحقق
python3 scripts/task.py verify spark 8.1 → evidence_level 30/30، قائمة سوداء 0/30

## قرارات اتخذتها
- لكل أداة: مصمّمها وسنة الإصدار، عيّنة التقنين الأصلية، حالة التقنين العربي (موجود جزئياً/غير موحّد/غير موثّق)، ونقد منهجي — مبني على معرفة حقيقية لا اختراع.
- عدة أدوات (CPT-3، إلخ) بلا items_count تقليدي — وُصف الحقل نصياً بدل رقم مخترع.
- تعطلت 9 من 10 subagents في المحاولة الأولى بسبب حد جلسة API؛ أعيد تشغيل الملفات الناقصة فقط (تحقق مسبق عبر grep) في 3 دفعات صغيرة، فاكتملت الـ30 بنجاح.

## متوقف عنده (لرئيس التحرير)
- لا طلبات جديدة (Task 8 لا يتعامل مع thinkers).
- تقنين عربي مؤكد نادر جداً عبر الأدوات المفحوصة — يستحق ملاحظة تحريرية عامة إن أغلب أدوات القياس النفسي في الأطلس بلا تقنين عربي موحّد موثّق.

## الملفات
30 ملفاً في content/ar/instruments/ (ins-aai → ins-epds-edinburgh-postnatal، ترتيب أبجدي)
