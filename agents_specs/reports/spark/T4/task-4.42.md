# Task 4.42 — تعميق 26 ملف مفكر (thk-a..l) + كشف كيان جماعي مختلَق وتلفيقات ببليوغرافية

## الأرقام
- 30 ملفاً في الدفعة؛ 26 عُدِّل، 3 لم تُعدَّل (2 محجوران مسبقاً بإعادة
  تأكيد + ملف تحويل صحيح thk-jkabat). ملف مجمَّد واحد (`thk-alasdair-macleod`)
  لم يُمس إطلاقاً.
- 9 مخالفات preflight بعد جمع الشِّقات، أُصلحت جميعها ما عدا مخالفة واحدة
  موجودة سلفاً في الملف المجمَّد نفسه (تُركت دون إصلاح احتراماً للتجميد).
  صفر مخالفات في كل الملفات غير المجمَّدة.
- إجمالي الكوميتات: 264.

## احترام الملف المجمَّد
**`thk-alasdair-macleod`**: قُرئ فقط للتحقق دون أي تعديل. يحمل مخالفة
preflight موجودة أصلاً (عنوان `related` خاطئ لـ"ستيف دي شيزر") لم تُصلح
احتراماً للتجميد — مسجَّلة هنا للعلم فقط.

## فساد frontmatter (نمط متكرر سابع)
**`thk-jramiro`**: نفس عطل "الحدّ الختامي الغائب" بعد قائمة `gaps` (السابع
من نفس النمط بعد `thk-kstinshoff`، `thk-goldberg`، `thk-cchan`،
`thk-lkuhn`، `thk-bacal`، `thk-alan-fruzzetti`) — أُصلح بإدراج `---`
الناقصة.

## اكتشاف كيان جماعي مختلَق (مشكلة بنيوية جديدة)
**`thk-knoblauch`**: "مؤسسو DGAP" كيان جماعي لا شخص فرد، وكان مصنَّفاً
رسمياً LIKELY_FABRICATED في وثيقة داخلية سابقة
(`missing-thinkers-final-resolution.md`). التحقق كشف أن DGAP (الفرع
الألماني لـIAAP) تأسست فعلياً عام **1963** في شتوتغارت برئاسة **هيلموت
إشنباخ** — لا في السبعينيات بالأسماء والمدن المذكورة سابقاً (W. Bitter،
J. Jacobi، D. Baumann، L. Klages، M. Brack) التي لا سند مستقل لها؛
أُزيلت هذه الأسماء المختلَقة وصُحِّح تاريخ التأسيس. يبقى مرشحاً قوياً
للحجر كونه كياناً جماعياً لا فرداً — نفس نمط `thk-kmurah`/`thk-edsch`/
`thk-isap` المرصود سابقاً.

## تلفيقات ببليوغرافية مُصححة (ثلاث حالات)
- **`thk-larrynims`**: قسم "ما أعطاه" بأكمله كان ملفَّقاً (وُصف كممارس
  EFT متخصص في الأداء الرياضي، مع كتاب *Tapping Into Your Best Self*
  غير موجود) — أُعيدت كتابته بالكامل بناءً على الحقيقة الموثَّقة: هو
  فعلياً مبتكر تقنية **BSFF** (Be Set Free Fast، منفصلة تماماً عن EFT)،
  ومؤلف *Be Set Free Fast!* مع Joan Sotkin.
- **`thk-ldavidson`**: كتابان مختلَقان بالكامل بأسماء مؤلفين مشاركين
  وهميين ("N. Killion"، "P. Rudof") استُبدلا بالأعمال الحقيقية الموثَّقة.
- **`thk-aboller`**: كتابان مختلَقان استُبدلا بالسيرة الحقيقية (أرماند
  فولكاس، معالج بالدراما ومسرح Playback، لا "شعر علاجي" كما ورد سابقاً).

## مرشح حجر جديد
**`thk-jramiro`**: بحث مكثف بعدة صياغات لم يُظهر أي أثر مستقل لـ"جان-لوك
رامارو" أو كتابه المزعوم *Ho'oponopono en Polynésie française* (2008)؛
الأسماء الفعلية الموثَّقة في هذا المجال (Luc Bodin، معهد Ho'oponopono
تاهيتي) مختلفة تماماً.

## إعادة تأكيد حجر سابق / تحويل صحيح
**`thk-james-berman`**: محجور مسبقاً، أُعيد تأكيده دون تغيير.
**`thk-jkabat`**: ملف تحويل صحيح لـ`thk-jkabat-zinn` (نفس الشخص، جون
كابات-زن) بقرار تحريري سابق سليم — لم يُلمس لأنه يعمل كما هو مصمَّم.

## تحقّق دقيق بلا أخطاء لشخصيات محورية
`thk-ibn-sina` (مع تصحيح نسبة ترجمة لاتينية خاطئة إلى جيرار الكريموني)،
`thk-erasmus`، `thk-gobind-singh`، `thk-gnaranjo`، `thk-lequier` — جميعها
تحقّقت بدقة عالية دون تصحيحات جوهرية.

## تصحيحات preflight بعد جمع الشِّقات
فساد frontmatter (`thk-jramiro`)، 5 مطابقات عنوان `related` حرفية
(`thk-cooper`، `thk-gold` ×2، `thk-knoblauch`، `thk-kspence`)، تصحيح
`active_end` لشخص حيّ (`thk-cooper`: 2015 خاطئ → "مستمر")، وإعادة صياغة
سطر `gaps` في `thk-ldavidson` لتفادي محفّز "تأكيد حقيقة بدل تسمية فجوة"
الآلي.

## معلَّق للمحرِّر
- إعادة تصنيف/حجر `thk-knoblauch` (كيان جماعي لا فرد).
- حسم قرار الحجر لـ`thk-jramiro`.
- إصلاح مخالفة `related` في `thk-alasdair-macleod` عند رفع التجميد.
