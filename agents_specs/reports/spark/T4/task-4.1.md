# Task 4.1
الحالة: مكتمل
العملية: التعميق والمصادر لملفات تحت المعيار | الملفات: 30

## الأرقام
وسيط الطول: 1,986 → **4,131** حرفاً
ملفات تحت 1,800 حرف: 9 → **3** (وكلها متروكة قصيرة عن عمد)
ملفات فوق سقف 6,000: 1 → **1** (`thk-bvanfraassen` عند 6,158 — انظر القرارات)
فيها `## المصادر` فعلياً: **1 → 23** من 30 (و7 بلا مصادر عن عمد)
مخالفات preflight: **صفر** · تعارضات slug: 22 (بلا تغيير)

## أمر التحقق
`cat b1.txt | xargs python3 scripts/preflight_check.py` → ✅ 30 ملف — صفر مخالفات
`python3 scripts/task.py verify spark 4.1` → القائمة السوداء 0 · السقّالة 0

## 🔎 الاكتشاف الأهم: هذا التاسك كاشف تلفيق، لا مجرد إضافة مصادر
نصف الملفات تقريباً كانت تحمل **مادة مختلَقة أو غير قابلة للتحقق** أُزيلت الآن:

| الملف | ما أُزيل |
|---|---|
| `thk-krochmal` | عنوان كتاب عبري **غير مفهوم لغوياً**، طبعة 1894 وهمية (الحقيقية Rawidowicz 1924)، ادّعاء أنه من أتباع نحمان مبراتسلاف، وأنه انتقل إلى **فيلنا** (عاش في غاليتسيا)، واقتباس بمترجم مختلَق «شاؤول كوهن» |
| `thk-jbissel` | **الادّعاء المركزي كاذب**: *Jung Journal* أسّسها **جون بيبي 1979** لا صاحب الملف 2007 (2007 سنة إعادة التسمية فقط)؛ وجولة الكتاب الأحمر مع شمداساني، والإشراف في PAJA. **سُجِّل في `quarantine-spark.md`** |
| `thk-bodunrin` | ثلاثة أخطاء إسناد: عنوان ودورية مقالته الأشهر (الصحيح *Philosophy* 56(216)، 1981)، مقال حُوِّل إلى «كتاب 1988»، ونسبة تحرير مشترك لكتاب حرّره **موزلي وحده**، و«مصدر» هو في الحقيقة محاضرة **أولو-وولابي** الافتتاحية |
| `thk-bbundy` | أربعة عناوين وهمية + دليل **آيبرغ** منسوب إليها |
| `thk-dtacey` | ثلاثة اقتباسات مُدّعاة، أستاذية غير مؤكَّدة، وكتاب لا يوجد |
| `thk-arascovsky` | الهوية نفسها مشكوك فيها (المؤسِّس الموثَّق **أرنالدو** لا «أرماندو»)؛ قُلِّص 9,058 → 4,080 وأُزيل ~96 من ~100 رابط لا صلة له |
| `thk-fahrenberg` | كتاب *Psychophysiologie* (1983) لا وجود له |
| `thk-erickson-erl` | «نموذج 3-2-1» وكتاب 2014 — كلاهما غير قابل للتحقق |
| `thk-hstein` | كتاب منسوب إليه لا وجود له + رابط `thk-kets` وهو **مَنفرِد كِتس دي فريس**، شخص آخر تماماً عن **صوفيا دي فريس** معلّمته |
| `thk-ehutchins` | مبدأ «الجسد في حقل الجاذبية» منسوب إليه وهو **لإيدا رولف نفسها** |
| `thk-deborah-kremer` | أربعة «أعمال» بلا عنوان ولا سنة ولا ناشر |

**وأخطاء واقعية صُحِّحت:** `thk-bhartrhari` ادّعى أن مذهبه **سابق** على أفلوطين وفيلون
(وكلاهما يسبق شخصية من القرن الخامس) · `thk-daniel-gilbert` نُسبت إليه دراسة الفائزين
باليانصيب وهي لـ**بريكمان وكوتس وجانوف-بولمان 1978** · `thk-gschwartz` **أغفل إسهامه
الرئيسي الوحيد** (تعريف الطب السلوكي المعدَّل، شوارتز وفايس 1978) ووصفه بالبارابسيكولوجي فقط.

## قرارات اتخذتها
- **7 ملفات بلا `## المصادر` عن عمد** (`thk-arascovsky` `thk-grose` `thk-heydt`
  `thk-deborah-kremer` `thk-jbissel` `thk-ehutchins` `thk-hstein`): لم يتوفر **مرجعان**
  يستوفيان القاعدة 5 (بقلم الشخص أو عنه تحديداً؛ مرجع عن مدرسته أو معلّمه لا يُحتسب).
  هذا **السلوك المطلوب** لا تقصير، والسبب مكتوب في `gaps` بكل ملف.
- **3 ملفات تُركت قصيرة عن عمد** لأن أصحابها غير مؤكَّدي الوجود.
- `thk-bvanfraassen` بقي **158 حرفاً فوق السقف** (6,158): كل محتواه موثَّق، وتقليصه أكثر
  كان سيحذف مادة حقيقية — والسقف مؤشر لا يُشترى بحذف معلومة.
- **⚠️ تباين مقصود التسجيل**: دفعة واحدة **غيّرت** تصنيف `thk-goldmann` من
  `sch-existential-therapy` إلى `sch-western-marxism`، بينما تركت دفعات أخرى نفس الخطأ في
  `thk-bloch` و`thk-fahrenberg` و`thk-anzieu` و`thk-aron` بوصفه خارج نطاق Task 4.
  **أُبقي التغيير** لأنه صحيح واقعياً، ولأن `sch-existential-therapy` **مدرَج نصاً في قائمة
  الروابط الممنوعة في SPARK.md** («ممنوع في `related` إلا لو المتن يشرح الصلة بالاسم والتاريخ»).
  فهو نموذج عملي لقرار الـ269 المعلَّق.

## ملاحظة على أداة التحقق
`task.py verify` يقول «فيها ## المصادر: 28/30» والعدد الحقيقي **23** — لأنه يطابق النص
كسلسلة فرعية فيَعُدّ الملفات التي تذكر `## المصادر` داخل سطر `gaps` يشرح **سبب غيابه**.
العدد الصحيح يُقاس بمطابقة ترويسة القسم `^## المصادر$`.

## متوقف عنده (لرئيس التحرير)
- `thk-jbissel`: الأرجح **سحب** لا كتابة — سُجِّل في الحجر.
- `thk-arascovsky` و`thk-grose` و`thk-deborah-kremer`: حالات **Task 2** (مشكوك في وجود
  أصحابها) لا Task 4.
- `thk-ieyberg` عنوانه «شيرلي إِيبِرغ» و`en:` عنده **Sheila** M. Eyberg — اسم أول خطأ في ملف
  معتمد، رصدته دفعتان مستقلتان.
- `part: "philosophy"` لأطباء نفسيين إكلينيكيين: `thk-cwebster-stratton` `thk-bbundy`
  `thk-gschwartz`.

## الملفات
الثلاثون في `scratchpad/t4/b1.txt`.
