# Task 4.12 — تعميق 27 ملف مفكر (thk-a..l)، أعلى كثافة شخصيات كبرى حتى الآن

## الأرقام
- 27 ملفاً عُدِّل، ملف جديد (`thk-halim-barakat`) أُدرج لأول مرة في git.
- ثلاثة ملفات تُركت بلا لمس عمداً (`thk-bobbeck` مجمَّد، `thk-ecolle`/`thk-jlubar` محجوران بالفعل).
- صفر مخالفات preflight بعد إصلاح فساد حقيقي في ملف وتسعة تصحيحات أخرى.
- إجمالي الكوميتات: 194.

## احترام قرار مجمَّد
`thk-bobbeck` ضمن قائمة الـ20 تعارض slug المجمَّدة نهائياً بقرار رئيس التحرير (2026-09-02): "لا حذف ولا تعديل لأي من الملفين". فُتح للتحقق فقط، ولم يُلمَس إطلاقاً — تحقّق git status يؤكد ذلك.

## فساد ملف حقيقي اكتُشف وأُصلح
`thk-kstinshoff.md`: preflight رفض تفسير الـfrontmatter بالكامل ("YAML مكسور") — التحقيق كشف أن **حد الفرونتماتر الختامي (`---`) كان غائباً بالكامل من الملف**، مما جعل قسم `gaps` يمتزج بمتن المقال دون حد فاصل. هذا ليس خطأً تجميلياً بل فساداً بنيوياً حقيقياً كان سيُفسِد أي بناء آلي للأطلس من هذا الملف. أُصلح بإضافة السطر المفقود.

## تلفيقات وأخطاء حقيقية اكتُشفت وأُزيلت
- **`thk-jframo`**: سنة وفاة كاذبة (2013) — الصحيح موثَّق في نعيَي APA وJMFT: **2001**.
- **`thk-lachmann`**: سنة وفاة كاذبة بالكامل (2019) وسنة ميلاد خاطئة — الشخص **حيّ فعلياً** حتى إعلان تقاعده الرسمي في يونيو 2024 على الأقل.
- **`thk-ibieber`**: عنوانان مذكوران كأنهما كتابان منفصلان هما في الحقيقة **كتاب واحد**، وهو عمل جماعي بتسعة مؤلفين لا عمل بيبر المنفرد.
- **`thk-guggenbuhl`**: مبالغة في وصفه كـ"أحد المؤسسين الثلاثة" لعلم النفس الأركيتايبالي بالتساوي مع هيلمان — الصحيح: هيلمان هو المؤسِّس الرئيسي المتفق عليه، وغوغنبوهل-كرايغ زميل مساهم لا مؤسِّس مشارك رسمي.
- **`thk-frankl`**: تاريخ عنوان *Man's Search for Meaning* صُحِّح — الترجمة الإنجليزية الأولى (1959) كانت بعنوان مختلف تماماً، وتغيَّر العنوان الحالي فقط في طبعة 1962.

## تطبيق الاتفاقية السابقة على أرقام التوزيع
`thk-louise-hay`: رقم "50 مليون نسخة" — بدل حذفه أو تثبيته كحقيقة مستقلة (كما حدث سابقاً مع ألبوم وبايرن في `FINDING-distribution-figures.md`)، طُبِّقت الاتفاقية المقترحة سابقاً: نُسب الرقم صراحة لدار هاي هاوس (الناشر) لا كواقعة عن الكتاب نفسه.

## فخّ تُجنِّب بنجاح
`thk-klew`: بدا اسماً مفرط الاختصار يستدعي شكاً (نمط عناقيد التلفيق)، لكن التحقق كشف أنه **كورت لوين** الحقيقي (klew = k+lewin) — لا حجر، محتوى دقيق، بما فيه تمييز صريح بين ما قاله لوين فعلاً وما نُسب له خطأً لاحقاً (نموذج الإذابة-الحركة-التجميد الثلاثي).

## §3 مطبَّق بصرامة على نصوص/شخصيات كلاسيكية
`thk-ibn-rushd` (طبعة نقدية، سلسلة نقل عربي-عبري-لاتيني)، `thk-democritus` (معرفة غير مباشرة عبر شذرات، ترقيم ديلز-كرانتس)، `thk-laozi` (خلاف أكاديمي جوهري حول الوجود التاريخي نفسه، لا سيرة فردية مؤكَّدة).

## تصحيحات ما بعد التسليم (preflight)
عشر مخالفات ظهرت بعد جمع الشِّقات:
- **فساد بنيوي حقيقي** في `thk-kstinshoff` (انظر أعلاه).
- ثلاثة عناوين `related` صُحِّحت حرفياً في `thk-halim-barakat`.
- **ثلاث إيجابيات كاذبة إضافية** في فحص الجنس النحوي (`thk-emaroda`، `thk-lfinlay`، `thk-louise-hay` — الثلاث نساء موثَّقات، والسبب ضمائر/أسماء محايدة عامة "المعالج"/"العالم" لا خطأ تذكير فعلي) — نفس النمط المكتشف في 4.11، يستحق الآن اعتباره ثغرة معروفة في أداة preflight تستحق تحسيناً دائماً بدل إصلاح متكرر يدوي.
- سطر `gaps` واحد في `thk-emaroda` أُعيدت صياغته لتفادي نمط "تأكيد حقيقة".

## معلَّق للمحرِّر
- **توصية تقنية**: فحص preflight للجنس النحوي (`check_gender_headers`) ينتج إيجابيات كاذبة متكررة بسبب عدم استبعاد الأسماء/الضمائر العامة المحايدة ("المعالج"، "العالم"، "هو" بمعنى "ذلك"). حدث هذا 5 مرات عبر دفعتين متتاليتين (4.11، 4.12) — يستحق تحسين المصفوفة regex نفسها مركزياً بدل إعادة الصياغة اليدوية في كل ملف.
- مراجعة الفساد المكتشف في `thk-kstinshoff` كنموذج: يستحق فحصاً استباقياً شاملاً لكل ملفات thk- بحثاً عن حدود frontmatter مفقودة مشابهة (preflight الحالي يكتشفها فقط عند التشغيل المباشر على الملف).
