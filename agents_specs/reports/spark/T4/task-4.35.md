# Task 4.35 — تعميق 28 ملف مفكر (thk-a..l) + تصحيحات وفاة وتلفيقات ببليوغرافية

## الأرقام
- 30 ملفاً في الدفعة؛ 28 عُدِّل، 2 محجورة مسبقاً بلا تعديل إضافي (إعادة
  تأكيد). لا ملفات مجمَّدة.
- 7 مخالفات preflight بعد جمع الشِّقات (4 مطابقة عناوين related + سنة وفاة
  أقدم من `active_end` + سنة نشر بعد وفاة سيئة الموضع)، أُصلحت جميعها. صفر
  مخالفات نهائياً.
- إجمالي الكوميتات: 242.

## تصحيحات وفاة (ثلاث حالات غير مسجَّلة)
- **`thk-gcraig`** (غاري كريغ، مؤسس EFT): توفي فعلياً يناير 2026 — `dates`
  وactive_end صُححا من "مستمر".
- **`thk-keen`** (جون إرنست كين): تاريخ وفاة خاطئ 2019 → **20 مايو 2010**
  الصحيح (حسب تأبين *The Humanistic Psychologist* 2011)؛ `active_end`
  صُحِّح ليطابق (كان 2001، خطأً أقدم من الوفاة الفعلية).

## تصحيحات ببليوغرافية/واقعية بارزة
- **`thk-dmiller`**: عنوان كتاب مختلَق بالكامل ("الأشباح والشياطين
  المسيحية") استُبدل بثلاثيته الحقيقية (*Christs* 1981، *Three Faces of
  God* 1986، إلى جانب *Hells and Holy Ghosts* 1989 الصحيح أصلاً).
- **`thk-bvdkolk`** (فان دير كولك): تصحيح سنة تأسيس مركز الصدمة
  (1984→1982) وعنوان كتاب محرَّر غير مؤكَّد استُبدل بعملين حقيقيين
  موثَّقين (*Psychological Trauma* 1987، *Traumatic Stress* 1996).
- **`thk-csluzki`**: كتاب مختلَق بالكامل ("Network: A Framework for the
  Description of Human Interaction") لا وجود له في قائمة مؤلفاته الموثقة
  على sluzki.com — استُبدل بعملين حقيقيين.
- **`thk-keen`**: عنوان كتاب 2001 خاطئ ("الفينومينولوجيا وعلم النفس")
  استُبدل بالعمل الحقيقي (*A History of Ideas in American Psychology*).
- **`thk-dbaucom`**: سنة ميلاد خاطئة 1953 → **1949** الصحيحة.
- **`thk-baker`**: كتاب كان مُصنَّفاً خطأً كـ"غير مؤكَّد ومحذوف" في مراجعة
  سابقة، أُعيد إدراجه بعنوانه الصحيح بعد تحقّق مستقل (*My Eleven Years
  with Reich*).

## مرشح حجر جديد
**`thk-andrew-johnson`**: لا أثر مستقل موثَّق يربط الاسم بـ"التنويم
الرياضي" المزعوم أو بأسلوب إلمان؛ الادعاءات غير المؤكدة وُسِمت صراحة في
المتن بعبارات "غير مؤكد/يُدَّعى" بدل عرضها كحقائق.

## إعادة تأكيد حجر سابق
**`thk-jenniferschwab`**، **`thk-lars-johansson`**: كلاهما محجوران مسبقاً
(قرارات موثَّقة من جلسات سابقة)؛ أُعيد تأكيدهما بعد بحث مستقل دون أي
تغيير.

## ملاحظة تناقض بين ملفات (خارج نطاق هذه الدفعة)
**`thk-lopezpedraza`** يتناقض مع **`thk-capriles`** (من دفعة 4.30) حول
تاريخ/دور تأسيس جمعية SVAJ الفنزويلية اليونغية — مصادر مستقلة توثّق
التأسيس فعلياً عام 1998 بدور تأثير لا تأسيس مباشر من لوبيز-بيدراثا، بينما
ملف كابريليس ينسب التأسيس إليه مباشرة في الثمانينيات. مسجَّل في `gaps`
لمراجعة لاحقة على ملف capriles، لم يُلمس ذلك الملف في هذه الدفعة.

## تحقّق دقيق لشخصيات محورية (بلا أخطاء تُذكر)
`thk-freud`، `thk-aquinas`، `thk-buddha`، `thk-fanon`، `thk-anaximenes` —
جميعها من أهم الشخصيات في الموسوعة، تحقّقت بدقة عالية عبر مصادر مستقلة
متعددة دون تصحيحات جوهرية (فقط تفاصيل صياغة/توضيح طفيفة).

## تصحيحات preflight بعد جمع الشِّقات
4 مطابقات عنوان `related` حرفية (`thk-ddinste`، `thk-jflax`، `thk-fanon`
×2، `thk-freud`)، وسنة وفاة كانت أقدم من `active_end` (`thk-keen`)، وإعادة
صياغة جملة في `thk-falexander` لجعل عبارة "بعد وفاته" تسبق السنة المذكورة
(2008) كما يتطلب الفحص الآلي (كانت العبارة موجودة لكن بعد السنة لا قبلها).

## معلَّق للمحرِّر
- حسم قرار الحجر لـ`thk-andrew-johnson`.
- مراجعة التناقض بين `thk-lopezpedraza` و`thk-capriles` حول تأسيس SVAJ.
