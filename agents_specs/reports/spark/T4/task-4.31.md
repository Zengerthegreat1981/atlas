# Task 4.31 — تعميق 28 ملف مفكر (thk-a..l) + تصحيح وفيات وتلفيقات

## الأرقام
- 30 ملفاً في الدفعة؛ 28 عُدِّل، 2 محجوران مسبقاً بلا تعديل إضافي (لا ملفات
  مجمَّدة بقرار الـ20 حالة في هذه الدفعة).
- 10 مخالفات preflight بعد جمع الشِّقات العشر (9 مطابقة عناوين related +
  فساد frontmatter رابع)، أُصلحت جميعها. صفر مخالفات نهائياً.
- إجمالي الكوميتات: 232.

## فساد frontmatter (نمط متكرر رابع)
**`thk-lkuhn`**: نفس عطل "الحدّ الختامي الغائب" بعد قائمة `gaps` (بعد
`thk-kstinshoff` من دفعة 4.12، `thk-goldberg` من دفعة 4.19، `thk-cchan` من
دفعة 4.28) — أُصلح بإدراج `---` الناقصة. الحالة الرابعة من نفس النمط،
معزولة كسابقاتها.

## تصحيحات وفاة كبرى (ثلاث وفيات حديثة غير مسجَّلة)
- **`thk-elaszlo`** (إرفين لازلو): توفي فعلياً **29 يونيو 2026** في تشيتشينا،
  إيطاليا، عن 94 عاماً — كان الملف يعامله كحيّ ("مستمر").
- **`thk-jhouston`** (جين هيوستن): توفيت فعلياً **16 مايو 2026** عن 89 عاماً
  — نفس المشكلة.
- كلاهما: `dates` و`active_end` صُحِّحا من "مستمر" إلى تاريخ الوفاة الفعلي.

## اكتشافات تلفيق واقعي مباشر
**`thk-diclemente`**: كتاب مختلَق بالكامل ("الانتكاس والاستمرار في علاج
الإدمان"، 1990) ومؤلفة مشاركة مختلَقة ("جودي لوفلو" لكتاب 2003 الذي هو في
الواقع من تأليف ديكليمنتي منفرداً) — كلاهما أُزيل واستُبدل بالأعمال
الحقيقية الموثَّقة (Guilford Press).

## مرشحو حجر جدد
- **`thk-jgould`**: لا أثر أكاديمي مستقل لـ"جيمس ليبشيتز" كمنظّر لـMST
  (العلاج متعدد الأنظمة)؛ المؤسسون الفعليون (Henggeler، Borduin،
  Schoenwald) موجودون بالفعل في الملف كـ`related`.
- **`thk-lkuhn`**: بحث مكثف لم يُظهر أي أثر مستقل لـ"ليندا كون" كمطوّرة لـ
  Circle of Security؛ المؤسسون الموثَّقون هم كوبر وهوفمان وباول.

## إعادة تأكيد حجر سابق
**`thk-david-reeve`** و**`thk-deepak-ramsubeik`**: كلاهما محجوران مسبقاً
(قرارات موثَّقة سابقاً)؛ لم يُعثر على أي دليل يناقض قراري الحجر الأصليين،
فأُبقيا كما هما دون تعديل.

## تصحيحات preflight بعد جمع الشِّقات
- 9 مخالفات مطابقة عناوين `related`/`edges` الحرفية (`thk-diclemente`،
  `thk-ereichelt`، `thk-greenwald`، `thk-gshepherd`، `thk-hammarskjold`،
  `thk-hherzog`، `thk-jgould`) — النمط الأكثر تكراراً كالمعتاد.
- مخالفة جندر جديدة النوع في `thk-jennifer-cheavens`: الملف يخص امرأة
  (جينيفر تشيڤنز)، لكن جملة "بعد سنايدر (توفي 2006)" فعّلت فحص المؤشر
  المذكر الآلي زوراً لأن كلمة "توفي" (تخص أستاذها الراحل سنايدر، لا هي)
  عُدَّت كمؤشر جندري للملف نفسه — أُعيدت الصياغة إلى "بعد وفاة سنايدر عام
  2006" لتفادي المحفّز دون تغيير المعنى. **ملاحظة للمحرِّر**: هذا نمط خطأ
  إيجابي كاذب (false positive) في `check_gender_headers` يستحق تحسيناً في
  السكربت نفسه — أي إشارة لوفاة شخص آخر مذكور في نص السيرة قد تُحرِّف عدّ
  المؤشرات الجندرية لصاحب الملف نفسه.

## تصحيحات أخرى بارزة
- `thk-david-berceli`: سنة ميلاد غير موثَّقة أُزيلت (لا مصدر أولي)، تصحيح
  `active_start` (1995→2003 بناءً على التأصيل الأكاديمي الفعلي)، إزالة
  ادّعاء غير موثَّق عن العمل مع ضحايا الإبادة في رواندا.
- `thk-bohanlon`: تصحيح شريكة التطوير الفعلية للعلاج الموجَّه نحو الحل
  (Michele Weiner-Davis)، إزالة رابط معلَّق لملف محجور مسبقاً (`thk-ecolle`).
- `thk-lsaari` (ليفي-برول): إزالة اسمين مختلَقين/مشوَّهين بالكامل من قائمة
  النقاد ("ريتشارد سميث"، "كوشي موانجوي") واستبدالهما بمرجعية موثَّقة فعلياً
  (كواسي ويريدو) وصياغة عامة موثَّقة بدل ادّعاء منسوب لشخص غير مؤكَّد.
- `thk-hherzog`: علامة استفهام جزئية حول تأكيد الهوية (الاسم الأصلي في
  السجل كان "F. Herzog" واستُنتج "ماكس هرتسوغ" بالتخمين المجالي فقط) —
  مسجَّلة في `gaps`، غير مُصعَّدة لحجر كامل لأن العمل نفسه (كتاب 1991
  المشترك) موثَّق بشكل مستقل.

## معلَّق للمحرِّر
- حسم قرار الحجر لـ`thk-jgould` و`thk-lkuhn`.
- توضيح هوية `thk-hherzog` (تأكيد كونه نفس الشخص في السجل الأصلي أم لا).
- تحسين `check_gender_headers` في `scripts/preflight_check.py` لتفادي
  محفّزات كاذبة من ذكر وفاة شخص آخر في سيرة صاحب الملف.
