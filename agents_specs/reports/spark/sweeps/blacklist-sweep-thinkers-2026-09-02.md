# سويب القائمة السوداء على `thinkers/` — الجولة 1

التاريخ: 2026-09-02 · الإذن: المستخدم صريحاً (القاعدة 1 في SPARK.md تمنع تعديل المعتمد؛ الإذن يغطي
هذه العمليات الثلاث فقط)

## الخلفية
مراجعة `REVISION/revision-2026-09-02.md` وجدت **953 ملفاً معتمداً** في `content/ar/thinkers/`
يحمل جملة القائمة السوداء الأولى `لا يوجد اقتباس مباشر موثوق متاح` — 74% من مفكري Spark
(a–l)، مقابل **صفر** في نطاق MiniMax (m–z). سويب 2026-08-27 غطّى `works/studies/instruments`
فقط ولم يُوسَّع لـ`thinkers` قط.

## العمليات الثلاث (لا غيرها)
1. جملة القائمة السوداء — في `gaps` و/أو في المتن تحت `## اقتباسات مختارة`.
2. العنوان اليتيم `## اقتباسات مختارة` إذا فرغ بعد الحذف.
3. السطر القالبي `المصادر الأولية تحتاج مراجعة وتوثيقاً إضافياً` (شرط القبول 5 يرفضه صراحةً).

**لم تُلمس** `related` ولا `edges` ولا التواريخ ولا الجنس النحوي ولا النثر — بقرار صريح، لأن
هذه أنواع عيوب أخرى تملكها دفعة لاحقة.

## قالب الإصلاح — منقول عن MiniMax لا مبتكَر
الجملة **لا تُحذف** بل **تُحوَّل إلى فجوة مُخصَّصة تسمّي سبب غياب الاقتباس**، وهو ما يطلبه شرط
القبول 5. ثلاثة مخارج فقط:
- **(أ)** تسمية العمل/حاجز اللغة: `لا يوجد اقتباس مباشر موثوق من *العنوان* (السنة) بترجمة عربية معتمدة.`
- **(ب)** تسمية السبب البنيوي: `لا يوجد اقتباس منسوب بمصدر وسنة.` / `بسبب ضياع كتبه.` /
  `لا يوجد نص أصلي منشور له يمكن الاقتباس منه.`
- **(ج)** حذف البند كلياً حين يحمل الملف فجوة أخرى تغطي الغياب فعلاً.

## ⚠️ الاكتشاف الأهم: الجملة كانت **كذباً صريحاً** في عدد من الملفات
في هذه الملفات المتن نفسه يحمل اقتباساً موثّقاً بمصدر وسنة، والفجوة تقول «لا يوجد اقتباس»:
`thk-adorno` (Minima Moralia 1951) · `thk-augustine` (Confessiones) · `thk-bataille`
(L'Érotisme 1957) · `thk-arendt` · `thk-aristotle` (**ثلاثة** اقتباسات موثّقة) · `thk-biko`
(I Write What I Like 1978) · `thk-cheng-hao` · `thk-chrysippus` · `thk-cioran` ·
`thk-claude-levi-strauss` · `thk-cleanthes` · `thk-bodhidharma`.

في كل هذه الحالات حُذف البند (أو أُعيد صياغته ليسمّي العيب الحقيقي: إسناد متأخر لا نص مصدري)،
و**أُبقي العنوان `## اقتباسات مختارة`** لأنه يحمل اقتباسات حقيقية. وكذلك أُبقي العنوان في
`thk-anaxagoras` و`thk-anaximenes` و`thk-archytas` و`thk-aristippus` و`thk-al-kawakibi` و
`thk-albertus-magnus` (شذرات أو اقتباسات فعلية).

**الاستنتاج: السويب ليس تجميلاً أسلوبياً — هو إزالة تصريحات غير صحيحة عن محتوى الملف نفسه.**

## الأرقام (الجولة 1 = أول 260 ملفاً، 10 دفعات متوازية × 26)
| المؤشر | قبل | بعد |
|---|---|---|
| إجمالي مخالفات preflight على الـ260 | **645** | **129** |
| مخالفات القائمة السوداء (متن + `gaps`) | **516** | **0** |
| ملفات تحتوي الجملة نصاً (grep) | 260 | **0** |
| الجملة في `thinkers/` كله | 953 | **693** |
| تعارضات slug | 22 | **22** (بلا تغيير) |

الـ129 الباقية **كلها خارج النطاق عمداً وبلا أي زيادة**: 73 `edges` نص حر · 33 تضارب
`title`/`id` · 11 سنة بعد `active_end` · 6 جنس نحوي (صنف معروف بالإيجابيات الكاذبة) ·
5 `gaps` تؤكد حقيقة (سابقة للسويب) · 1 YAML مكسور (`thk-benthall`).

## ملاحظات تقنية للدفعات القادمة
- `thk-benthall`: كتلة `gaps` مخزَّنة كسلسلة YAML واحدة بمحارف `\n` حرفية — أحد الملفين
  المعروفين بـfrontmatter مكسور. أُزيل البند المستهدف جراحياً بلا إدخال فجوة جديدة في السلسلة المعطوبة.
- **تجنّب إنهاء أي فجوة جديدة بـ`موثّق`/`موثّقة`** — فحص «سطر يؤكد حقيقة» يطابق هذه اللاحقة
  فيتحوّل الإصلاح إلى مخالفة جديدة.
- الفحص يستثني مجلد `drafts` عند حلّ الـslugs (سطر 91)، فلا معنى لتشغيله على المسودات بحثاً عن روابط.
- في zsh لا يحدث word-splitting للمتغيّرات غير المقتبسة: استخدم `cat list | xargs python3 …`
  لا `python3 … $VAR` (وإلا مرّت الـ260 مساراً كاسم ملف واحد).

## المتبقي
693 ملفاً على 3 جولات (261–953 في `sweep/all954.txt`) بنفس المنهج.

---

# الجولة 2 — الملفات 261–520

| المؤشر | قبل | بعد |
|---|---|---|
| إجمالي مخالفات preflight على الـ260 | **681** | **165** |
| مخالفات القائمة السوداء | **516** | **0** |
| الجملة في `thinkers/` كله | 693 | **433** |
| تعارضات slug | 22 | **22** |

الـ165 الباقية خارج النطاق عمداً: 68 `edges` نص حر · 44 تضارب `title`/`id` · 26 سنة بعد
`active_end` · 18 جنس نحوي · 9 `gaps` تؤكد حقيقة (سابقة للسويب).

## حالات «الفجوة كانت كذباً» في هذه الجولة
`thk-democritus` (3 شذرات مُسندة) · `thk-diogenes-sinope` · `thk-dogen` (شوبوجينزو) ·
`thk-feuerbach` · `thk-francis-bacon` (اقتباسان) · `thk-fromm` (فن الحب 1956) ·
`thk-emilecoue` · `thk-erigena` · `thk-gadamer` (الحقيقة والمنهج 1960) · `thk-ge-moore`.
في كلها حُذفت الفجوة وأُبقي العنوان.

## تصحيحان بعد المراجعة الذاتية للدفعات
- **`thk-dscharff`**: الجملة كانت في المتن فقط بلا بند في `gaps`، فحُذفت بلا بديل — أي **ضياع
  معلومة**. أُضيفت فجوة مُخصَّصة تسمّي العمل: *Object Relations Family Therapy* (1987).
  **الدرس المُضاف لتعليمات الجولات التالية:** الجملة الموجودة في المتن وحده تُحوَّل إلى فجوة، لا تُحذف.
- **`thk-fosha`**: حُذفت معها عبارة «يلزم التحقق من نصوص فوشا الأصلية». **فُحص ولم يُصحَّح**: الفجوة
  الجديدة (`لا يوجد اقتباس مباشر موثوق من *The Transforming Power of Affect* (2000) بترجمة عربية
  معتمدة.`) تغطي المعنى نفسه **وأدقّ منه** لأنها تسمّي العمل. لا ضياع معلومة.

## ملاحظة تقنية إضافية
عند حذف قسم في نهاية الملف يسقط سطر السطر الأخير (`\ No newline at end of file`) — دفعتان
رصدتاه وأعادتا السطر. يجب التحقق منه في كل جولة.

---

# الجولة 3 — الملفات 521–780

| المؤشر | قبل | بعد |
|---|---|---|
| إجمالي مخالفات preflight على الـ260 | **679** | **163** |
| مخالفات القائمة السوداء | **516** | **0** |
| الجملة في `thinkers/` كله | 433 | **173** |
| تعارضات slug | 22 | **22** |

## حالات «الفجوة كانت كذباً» في هذه الجولة
`thk-gorgias` (اقتباسان من «مديح هيلين») · `thk-hipparchia` · `thk-hippias` · `thk-holderlin`
(Patmos 1803) · `thk-huineng` (سوترا المنصّة) · `thk-ibn-hazm` (طوق الحمامة) ·
`thk-ibn-khaldun` (اقتباسان من المقدّمة) · `thk-jean-francois-lyotard` (الوضع ما بعد الحداثي).

## ⚠️ أثر قياسي يجب معرفته قبل قراءة الأرقام
حذف بند القائمة السوداء من `gaps` **يكشف** مخالفة `gaps: سطر بيؤكد حقيقة` كانت موجودة أصلاً ولم
يكن الفحص يبلّغ عنها لذلك الملف. تأكّد هذا في `thk-hsteadman` (نصّ السطر مطابق لـHEAD حرفياً).
لذلك **ارتفاع هذه الفئة عبر الجولات ليس دليلاً على أن الدفعات أدخلتها** — لازم المقارنة بـHEAD
قبل نسب أي سطر منها للسويب. عدد الفئة: 5 (ج1) → 9 (ج2) → 11 (ج3)، وكلها مكشوفة لا مُستحدَثة.

## 🚨 اكتشاف تشغيلي: المستودع ليس تحت كاتب واحد
أثناء الجولة 3 ظهر أن **مسار MiniMax (أو عملية أخرى) يكتب في المستودع بالتوازي**:
- **170 ملفاً معدَّلاً** خارج قائمة الجولة، كلها في `branches/` و`concepts/` و`relations/` — وهي
  مجلدات MiniMax حسب SPARK.md لا Spark.
- **ملفان معتمدان جديدان** غير متتبَّعين: `thk-hsacks` (هارفي ساكس) و`thk-rputnam` (روبرت بوتنام)،
  وكلاهما `part: "sociology"` بحقول `sociological_tradition`/`paradigm` وبمعرّفات متسلسلة حقيقية
  (THK-6336/6337) — أي من مسار سوسيولوجي منفصل، لا من دفعات هذا السويب.

**والنتيجة العملية:** الـcommit `352bb9de` (الجولة 2) استُخدم فيه `git add -A` فالتقط **23 ملفاً
لـMiniMax** (18 `branches` + 5 `relations`) ضمن commit عنوانه عن سويب Spark. **لا فقد محتوى** —
تغييرات MiniMax محفوظة، لكن الإسناد في التاريخ مختلط. (الجولة 1 `510bb29e` كانت نظيفة: 260 ملف
thinkers فقط.)

**الإجراء المتخذ من الجولة 3 فصاعداً:** لا `git add -A` بعد الآن — يُستخدم staging انتقائي بقائمة
ملفات الجولة فقط، والملفان السوسيولوجيان تُركا بلا لمس ولا حذف (القاعدة 6) لصاحب مسارهما.

---

# الجولة 4 (الأخيرة) — الملفات 781–953

| المؤشر | قبل | بعد |
|---|---|---|
| إجمالي مخالفات preflight على الـ173 | **453** | **119** |
| مخالفات القائمة السوداء | **334** | **0** |
| **الجملة في `thinkers/` كله** | 173 | **صفر** |
| تعارضات slug | 22 | **22** |

# ✅ الحصيلة النهائية للسويب

| | |
|---|---|
| ملفات عولجت | **953** في 4 جولات × 10 دفعات متوازية |
| مخالفات القائمة السوداء | **1,882 → صفر** |
| الجملة في `content/ar/thinkers/` | **954 → صفر** |
| تعارضات slug | 22 → **22** (بلا تغيير عبر الجولات الأربع) |
| ملفات معتمدة أُنشئت أو حُذفت | **صفر** |

**حالات «الفجوة كانت كذباً»** (المتن فيه اقتباس موثّق بمصدر وسنة والفجوة تنفي وجوده): **≥40 ملفاً**،
منها أرسطو (3 اقتباسات)، ابن خلدون (2)، بيكون (2)، غورغياس (2)، ديموقريطس (3 شذرات)، أدورنو،
أوغسطين، باتاي، أرندت، فروم، غادامير، مور، بوبر، ليوتار، ألتوسير، ابن حزم، هوي-نينغ، لوقيبوس،
يوستينوس الشهيد، هولدرلين، سيوران، ليفي-ستروس، كليانثس، تشنغ هاو، إريجينا، كويه، بوذيدارما،
هيبارخيا، هيبياس. في كلها حُذفت الفجوة **وأُبقي العنوان والاقتباس**.

## ما تبقّى في المعتمد خارج `thinkers/`
بعد اكتمال السويب: `_merged` **48** · `contexts` **7** · `techniques` **6** · `experiences` **3**.
(`branches` و`relations` و`concepts` كانت 97 و91 و30 عند بداية الجلسة وأصبحت **صفراً** — نظّفها
مسار MiniMax بالتوازي أثناء نفس الجلسة، وهو ما يفسّر الـ170 ملفاً المعدَّلة المرصودة في الجولة 3.)

الـ16 في `contexts/techniques/experiences` أرض Spark وتُعالَج. الـ48 في `_merged` مجلد أرشيف دمج
غامض الملكية — **يحتاج قراراً بشرياً** قبل لمسه.

## الأنواع الباقية في `thinkers/` a–l (خارج نطاق هذا السويب عمداً)
`edges` نص حر · تضارب `title`/`id` · سنة بعد `active_end` · جنس نحوي (صنف معروف بالإيجابيات
الكاذبة) · `gaps` تؤكد حقيقة. هذه هي دفعة الإصلاح التالية المقترحة.

---

# الذيل — الـ16 ملفاً الباقية في أرض Spark خارج `thinkers/`

`contexts` 7 · `techniques` 6 · `experiences` 3 — عولجت كلها (22 موضعاً: 16 في `gaps` + 7 في المتن،
وملف واحد كانت الجملة في متنه وحده). preflight على الـ16: **45 → 14**، والقائمة السوداء **31 → 0**،
والـ14 الباقية كلها تضارب `title`/`id` سابق (13) وهدف `edges` حر واحد.

**الصياغة كُيِّفت لنوع المدخل** لا لشكل «مفكر»: `ctx-` تسمّي التقليد النصي الغائب (نصوص هوسرل
وهايدغر، المصادر الهيلينستية، الأدبيات التأسيسية للمجتمع العلاجي)، و`tec-` تسمّي الدليل التأسيسي
(نص بوتزن 1972 للتحكم بالمنبّهات، *Human Sexual Inadequacy* 1970 للتركيز الحسّي).

**حالتان تستحقان التسجيل:**
- `exp-alienation-marx`: **لم** تُستخدم صيغة «لا ترجمة عربية معتمدة» لأن «المخطوطات الاقتصادية
  والفلسفية» (1844) لها طبعات عربية معروفة — فالادّعاء كان سيكون كذباً. استُخدمت صيغة الإسناد.
- `exp-abyss-gaze`: فجوة **شبه كاذبة** — المتن يحمل اقتباس نيتشه عن الهاوية ويسمّي *ما وراء الخير
  والشر*، لكن بلا طبعة ولا صفحة ولا سنة. فأُعيدت الصياغة لتسمّي الناقص فعلاً بدل نفي وجود الاقتباس.

# 🏁 الحصيلة على مستوى الأطلس المعتمد كله

فحص **الجمل العشرة** في القائمة السوداء عبر `content/ar/` كامل (باستثناء `drafts/`):
**48 ملفاً، كلها في `content/ar/_merged/`** (4 في الجذر + 44 في `_merged/thinkers/`).

**كل مجلد حيّ في الأطلس أصبح نظيفاً تماماً من القائمة السوداء** — `thinkers` و`works` و`studies`
و`instruments` و`techniques` و`disorders` و`syndromes` و`events` و`contexts` و`experiences`
و`metaphors` و`branches` و`relations` و`concepts` و`schools`.

**⛔ قرار بشري مطلوب:** `_merged` مجلد أرشيف دمج — هل ملفاته لا تزال جزءاً من البناء (فتحتاج سويباً)
أم أنها نسخ محفوظة قبل الدمج (فلمسها يفسد قيمتها كأرشيف)؟ **لم يُلمَس بانتظار القرار.**
