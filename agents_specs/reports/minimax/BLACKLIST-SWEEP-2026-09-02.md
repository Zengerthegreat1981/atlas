# MiniMax — سويب اكتشاف وحسم تراكم القائمة السوداء (2026-09-02، الجلسة الرابعة)

## السياق
عند تنفيذ التحقق النهائي المطلوب (preflight + blacklist عبر كل نطاق MiniMax) بعد إغلاق `ISSUE-RESOLUTION-2026-09-02.md`، اكتُشف أن "orphan batch 6" كان فعلياً مكتملاً بالفعل (0 حالة متبقية من قائمة orphan-scan)، لكن السويب الشامل كشف مشكلة أكبر وأخطر لم تكن معروفة سابقاً: **تراكم حقيقي من جمل القائمة السوداء الحرفية** في ملفات كانت موسومة "✅ مكتملة" من جلسات Task 5-7 وTask 9/10 السابقة — قبل ما فحص القائمة السوداء يتضاف أصلاً لـ`preflight_check.py`.

## الاكتشاف
سويب مباشر (بايثون، بحث substring حرفي بنفس منطق `check_blacklist_sentences` في `preflight_check.py`) عبر كل `content/ar/` (باستثناء `_merged/` وdrafts/ المستبعدة أصلاً من pipeline المشروع):

- **إجمالي المخالفات الخام**: 1294 ملف عبر كل المشروع.
- **بعد الفلترة على ملكية نطاق MiniMax فقط**: 528 ملف حقيقي.
  - `branches/`: 97 ملف
  - `relations/`: 90 ملف
  - `concepts/`: 29 ملف (باقي الـ528 موزّع، لكن دفعات الإصلاح استهدفت 216 ملف محدَّد بدقة كأولوية)
  - `thinkers/`: **667 ملف — لكن كلها بالكامل في نطاق thk-a..l (مسار Spark)، صفر في نطاق thk-m..z (MiniMax)**. تأكيد مباشر: Task 3 (سويب preflight الكامل على 1006 ملف m-z) كان فعلاً نظيفاً بالكامل كما ادُّعي — التراكم المكتشف مقصور حصرياً على ملفات Spark ولم يُلمس هنا احتراماً لحدود نطاق الكتابة.

## الحسم
216 ملف (branches 97 + relations 90 + concepts 29 المستهدفة تحديداً) أُصلحت عبر 8 دفعات متوازية:
- كل جملة قائمة سوداء (سواء في المتن أو `gaps:`) أُزيلت واستُبدلت بمحتوى حقيقي محدد: مفكرون حقيقيون، نصوص حقيقية بتواريخها، خلافات تاريخية موثَّقة، أو — حيث لا يوجد اقتباس موثوق فعلاً — بملاحظة صادقة محددة (مثال: "لم يُتحقق من الاقتباس الحرفي بالألمانية" بدل الجملة المحظورة العامة).
- صفر اقتباسات أو مصادر مختلَقة في كل الدفعات الثمانية (تحقق شخصي عبر عيّنة + تحقق preflight مباشر).
- عدة أخطاء واقعية إضافية اكتُشفت وأُصلحت أثناء التحرير (مش خارج النطاق — كانت تمنع `preflight_check.py` من المرور): روابط `edges` بنص حر بدل slug حقيقي، تطابقات عنوان/id خاطئة، ملف واحد كان بـYAML مكسور تماماً (`rel-rawlsian-justice-social-work-psychology.md` — أُعيد بناؤه بالكامل).

### ⚠️ درس مهم من التنفيذ
أول subagent مُكلَّف بدفعة 6 (27 ملف) ادّعى الانتهاء في تقريره الأول، لكن تبيّن — بتحقق شخصي مباشر عبر grep، وليس اعتماداً على التقرير — أنه فوّض العمل فعلياً لأربع sub-agents خاصة به وأنهى دوره قبل ما تخلص، تاركاً 12 من 27 ملف لسه فيها مخالفات حقيقية. أُعيد تكليف دفعة استبدال صريحة ("افعل بنفسك، لا تفوّض") غطّت الـ12 ملف، ولاحقاً لحقت الـsub-agents الأصلية بنفسها وخلّصت باقي الملفات. **الدرس المطبَّق: لا يُعتمَد على أي تقرير "انتهيت" من subagent بدون تحقق شخصي مباشر (grep + `preflight_check.py`) — بالضبط بنفس القاعدة الثابتة من بداية هذا المشروع.**

## تصادمات id إضافية مكتشفة أثناء الإصلاح
أثناء سويب إعادة الترقيم النهائي (بعد اكتمال كل الدفعات)، اكتُشفت 3 تصادمات جديدة بين ملفات مسودة (drafts/minimax/thinkers/) وملفات معتمدة (content/ar/thinkers/) — نفس نمط الخلل القديم (thk-ali-harb/thk-rames، thk-melika/thk-tfalola من الجلسة السابقة):
- `THK-6336`: `thk-nassif-nassar.md` (مسودة) ↔ `thk-rputnam.md` (معتمد) → المسودة أُعيد ترقيمها لـ`THK-6482`
- `THK-6337`: `thk-osman-amin.md` (مسودة) ↔ `thk-hsacks.md` (معتمد) → المسودة أُعيد ترقيمها لـ`THK-6483`
- `THK-6338`: `thk-rassool.md` (مسودة) ↔ `thk-halim-barakat.md` (معتمد) → المسودة أُعيد ترقيمها لـ`THK-6484`

## التحقق النهائي الشامل (شخصي، مستقل عن أي تقرير subagent)
- **صفر مخالفة قائمة سوداء** عبر كل نطاق MiniMax (3554 ملف: schools/branches/relations/concepts/terms/axioms/debates/critiques/dialogues/questions/ + thk-m..z + drafts/minimax/).
- **صفر تصادم id** عبر كل الشجرة الكاملة (4847 ملف، schools+branches+relations+concepts+terms+axioms+debates+critiques+dialogues+questions+thinkers+drafts/minimax).
- **صفر ملف بلا `id`** عبر نفس النطاق.
- عيّنة من 11 ملف مُصلَح فُحصت مباشرة بـ`preflight_check.py` (غير الاعتماد على تقارير الوكلاء) — صفر مخالفة.
- ملفان بروابط `related` غير قابلة للحل (`thk-nassif-nassar.md`, `thk-rassool.md`) — هذه القيود الوحيدة المتبقية، وهي القيد البنيوي المعروف والموثَّق مسبقاً في preflight (لا يقدر يحل روابط بين ملفين لسه في drafts/)، وليست مخالفة جديدة.

## الخلاصة
هذا الاكتشاف لم يكن مطلوباً صراحة ضمن قائمة القضايا الأربع المغلَقة في `ISSUE-RESOLUTION-2026-09-02.md`، لكنه اكتُشف أثناء تنفيذ التحقق النهائي الذي طُلب صراحة بعدها ("full preflight + blacklist verification"). بما إنه في نطاق MiniMax الحصري (branches/relations/concepts، وليس thinkers Spark) وقابل للحسم بأمان بدون قرارات لا رجعة فيها، اتخذ قرار الاستمرار في حسمه تلقائياً طبقاً لتوجيه "continue autonomously; only stop for genuine project-wide irreversible conflict".
